# AI Agent 自主迭代账本（目标 ≥100 轮）

方向：1) 智能化(更聪明) 2) 人性化(更好用) 3) harness 增强。每轮一个聚焦改进，编译/验证后提交，git 历史即轮次账本。部署批量化（每若干轮一次）。

> 全程 100 轮已完成（R1–R100）。git 历史 `grep -c 'iter R'` 即权威轮次账本；下表为代表性里程碑摘要。

## 代表性里程碑（按主题）
- **智能化/引擎**：结果呈现智能(R1)、数仓分层+命名规范、连续相同只读调用复用防自循环(R71)、工具失败按错误特征定向纠错(表/字段/语法/权限/超时, R76/R80)、接近步数上限优雅收尾(R78)。
- **harness/健壮**：/health 可观测(aiConfigured/配置快照/运行态, R56/R60)、LLM 瞬时错误退避重试(R85, 与应用层 callLlmWithRetry 双层)、LLM token 用量日志(R88)、每日清理超期 AI 生成文件(R65)、自主轮询连续失败熔断(R92)。
- **文件体验(用户专项)**：已上传区只显示用户文件(R63)、输入框 @ 补全文件名+回车选首项+省略号(R64/R67/R69)、accept 过滤+类型图标(R79/R97)。
- **预览/Artifact**：整卡可点预览(R84)、加载超时兜底(R59)、复制链接(R55/R86)、数据表点列头排序+CSV/MD导出跟随排序+行号列固定(R72/R73/R83/R87)。
- **人性化/交互**：相对时间(R82)、智能滚动不打断上翻(R77)、? 快捷键说明(R74)、代码块语言标签(R90)、数据中心折叠记忆(R98/R99)、含占位示例填入待改(R95)。

## 起始记录
| 轮次 | 方向 | 改进 | 状态 |
|---|---|---|---|
| R1 | 智能化 | 结果呈现智能：多行/对比/结构化数据主动用 create_artifact(csv/图)/chart，不堆文本 | done |
