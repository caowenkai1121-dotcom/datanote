# 性能优化 backlog (六角色, 89项)


## sync-etl (15)
- [H/l/risk:high] 表级串行同步（应并行） | src/main/java/com/datanote/domain/integration/engine/FullSyncEngine.ja | 引入表级并行执行：任务内部维护动态线程池(poolSize按表数&CPU核数)，各表独立获取源/目标连接并行运行。按优先级/表大小分组提高缓存命中。配合任务级线程池隔离资源竞争。
- [H/l/risk:med] 无断路器重试机制（网络抖动未覆盖） | src/main/java/com/datanote/domain/integration/service/SyncJobExecutor. | 添加断路器：按(源库IP, 目标库IP)维护失败计数+时间窗口，连续N次失败或周期内失败率>M%时断路快速失败(10s内直接返回失败)，待冷却期自动半开试探。同时引入任务级背压检测(捕获SQLException含'too many connections')主动backoff。
- [H/m/risk:med] 分页读不支持真正的游标式背压 | src/main/java/com/datanote/domain/integration/engine/FullSyncEngine.ja | 改MySQL驱动参数：defaultFetchSize改为0(仅行流模式)，改ReadTimeout容忍拉取时间。测试后端游标(MySQL不支持FETCH/LIMIT)改用keyset分页确保内存恒定。大表必须走keyset+游标，禁止scanSql流式(增量引擎已用游标)。
- [H/l/risk:high] CDC事件处理无背压反馈（Debezium缓冲可能溢出） | src/main/java/com/datanote/domain/integration/engine/cdc/CdcSyncEngine | handleBatch内改为批SQL：收集一批DML后用preparedStatement.addBatch()执行，减往返。同时引入背压指标：当队列占用>80%时主动sleep(poll_interval_ms*倍数)给下游喘息。监控queue大小实时调整poll_interval。
- [M/m/risk:med] 表级连接每次新建（应池化） | src/main/java/com/datanote/domain/integration/engine/FullSyncEngine.ja | 同步前全局复用连接：在SyncEngine.sync()层按表数申请连接对象池，各表从池取而非getConnection()，sync()完全局close。或改源/目标连接为全局字段，表级存活期内保持open。需处理异常时连接回池、死连接检测。
- [M/m/risk:low] StreamLoad批量写内存峰值（大行宽场景） | src/main/java/com/datanote/domain/integration/engine/StreamLoadWriter. | 流式CSV编码：改为逐行写入HttpURLConnection的OutputStream(不缓存String)，或改用流式编码库。监控batchSize vs内存，当行宽>5KB时自动降批。或分两阶段：字段级CSV转义缓存+流式连接。
- [M/m/risk:low] HTTP连接未复用（StreamLoad批写） | src/main/java/com/datanote/domain/integration/engine/StreamLoadWriter. | 改用OkHttp/HttpClient配置ConnectionPool(默认5-8个连接)，或改HttpURLConnection使用http.keepAlive=true+Connection:keep-alive Header。BatchWriter持有StreamLoadChannel实现时传
- [M/s/risk:low] 脏行DLQ未批量落库（单行INSERT） | src/main/java/com/datanote/domain/integration/engine/BatchWriter.java: | 坏行改为批量缓冲：BatchWriter收集脏行到内部队列(上限1000)，定期或flush时批量DLQ落库。DnSyncErrorRow改支持batchInsert()，同时保证DLQ表有合理索引(jobId+runId)快速查询。
- [M/l/risk:high] 增量同步的复合游标转字符串成本高（仅MySQL支持） | src/main/java/com/datanote/domain/integration/engine/FullSyncEngine.ja | 非MySQL源改用数据库原生游标机制：PG FETCH/OFFSET游标、Oracle ROWNUM分页、SQLServer OFFSET FETCH。改FullSyncEngine支持各库游标语法，或改游标存储为二进制(Long型PK直接存序列化字节)。
- [M/m/risk:med] BatchWriter缓冲固定上限（不自适应内存） | src/main/java/com/datanote/domain/integration/engine/BatchWriter.java: | 改为内存自适应批：监控buffer当前内存占用（约 sum(row.length) * 行数），当达到配置阈值(如100MB)时提前flush()，而非等待batchSize行数。根据行宽动态调整实际批处理大小。
- [M/m/risk:med] 目标表DDL漂移检测单表单连接查询（高并发并查） | src/main/java/com/datanote/domain/integration/engine/cdc/CdcSyncEngine | 改为本地缓存+TTL：维护列缓存(HashMap<db.table, Set<cols>>)存活10min，过期后单线程更新(用ReentrantLock防thundering herd)。元数据查询改为异步后台更新，数据事务内用陈旧缓存。
- [L/s/risk:low] JDBC批写prepareSql重编译（无缓存重用） | src/main/java/com/datanote/domain/integration/engine/FullSyncEngine.ja | SQL不变，问题在驱动配置：确保ConnectionManager.buildJdbcUrl()已配 cachePrepStmts=true & prepStmtCacheSize=250。若仍慢，改prepareSql缓存HashMap<(db,table,columns),sql>避免重复bui
- [L/s/risk:low] 源表元数据查询未缓存(全量/增量同步首行都查schema) | src/main/java/com/datanote/domain/integration/engine/FullSyncEngine.ja | 源库元数据缓存：任务开始时单次批查源库所有需同步表的schema，缓存到Map<table, TableMeta>。表级同步时直接取缓存。或改DbConnector支持listTableMeta(List<table>)批查接口。
- [L/s/risk:low] 写入模式校验无预判(运行时才发现不支持UPSERT) | src/main/java/com/datanote/domain/integration/engine/BatchWriter.java: | SyncJobExecutor.doRun()初始化阶段预检：按目标库类型校验UPSERT/INSERT/INSERT_IGNORE支持度，若无则强制改或拒绝启动任务。Stream Load作为可选加速，失败安全回退。
- [L/s/risk:low] 无预加热连接池(冷启动首个表慢) | src/main/java/com/datanote/domain/integration/connector/ConnectionMana | SyncJobExecutor.doRun()前置初始化连接：执行前调connectionManager.getConnection(sourceDsId).close()预热池。同时调整HikariConfig.minimumIdle=1改为=3提高预建。

## scheduler (14)
- [H/m/risk:high] 调度线程池 MAX_CONCURRENT_TASKS 硬编码 4 导致高并发下堆积 | TaskSchedulerService.java:59, init():70-87 | 线程池参数化(配置文件 datanote.schedule.executor-pool-size, executor-queue-capacity 等)；拆分任务执行队列(快路径短任务/长任务池分离)；队列满时 RejectedExecutionHandler 可切换为 DiscardOldestP
- [H/m/risk:med] processWaitingTasks 中 CronExpression.parse 每次调用逐任务 DB 查询，N+1 查询性能衰减 | TaskSchedulerService.java:316-370, getTaskCron():402-410 | 批量化：在 processWaitingTasks 入口统一查询所有 taskId 对应的 cron，用 selectBatchIds() 一次取全部；缓存策略：@Cacheable 或本地 LoadingCache(TTL 1min) 缓存 taskId->cron，减少 selectById；优
- [H/m/risk:med] 重试时 executor.submit() + Thread.sleep(delayMs) 占用调度线程，导致长任务堆积 | TaskExecutionService.java:222-229 | 使用 ScheduledExecutorService.schedule() 替代 sleep，避免占用工作线程；或用 virtual threads (Java 21+) 改写 Thread.sleep；或使用定时任务队列(Timer/Quartz)补偿，每分钟检查过期重试。建议引入独立的 Sch
- [H/s/risk:low] tick() DISTINCT 查询无索引覆盖，扫描全表 DnSchedulerRun，大表(百万级)劣化至秒级 | TaskSchedulerService.java:202-225 | DB 端添加索引：CREATE INDEX idx_waiting_dates ON dn_scheduler_run(status, run_date, run_type)；若表大于 10M 行，建议分区(按 run_date 分月)；或在 Service 层维护内存缓存(ConcurrentHa
- [H/m/risk:med] Semaphore 与 LinkedBlockingQueue 双重背压，堵塞任务堆积 | TaskSchedulerService.java:70-78, processWaitingTasks():348-355 | Semaphore 改为 FairSemaphore(或用 Semaphore 的 fair=true 构造)，保证 FIFO；添加队列堆积监控日志(周期性 executor.getQueue().size() 告警)；任务优先级分离(快路径/慢路径线程池)；或使用 ThreadPoolExecut
- [H/l/risk:med] allUpstreamsCompleted() 每次 tick 轮询，无变化推送机制，导致依赖就绪时延迟 15s | TaskDependencyService.java:137-165, TaskSchedulerService.java:316-370 | 改为 push 模式：上游任务成功后，在 resumeDownstreamAfterSuccess() 中直接调用 processWaitingTasks()，无需等 tick；或引入事件驱动(Spring ApplicationEvent 或 MQ)，上游发送任务完成事件，下游监听并立即检查；若保
- [H/m/risk:med] getOnlineScripts() & getOnlineSyncTasks() 全表扫描无分页，大规模(数千任务)时慢 | TaskSchedulerService.java:813-825 | 改为分页扫描(LIMIT 500)，逐页创建 run 记录；批量 insert(batchInsert)替代逐条 insert；若调度任务数固定，改为增量更新(仅处理新增/启用的任务)，减少每日处理量；对 getOnlineScripts 做缓存(启动时装载，任务变更时刷新)；若任务确实达数千，考虑
- [M/l/risk:high] refreshAllDependencies() 全表删除+重新扫描 SQL 解析，无并发控制，高并发期间阻塞依赖检查 | TaskDependencyService.java:61-124 | 改为增量更新(仅处理脚本变更)而非全量重扫；去掉全表 delete，改为按 taskId 增量 upsert；正则解析改为异步后台任务队列(Kafka/RabbitMQ)；添加执行时间日志/监控，超过 500ms 告警；SQL 解析结果缓存(脚本 content hash)，内容未变就跳过解析。
- [M/m/risk:med] estimateRowsByConnection() 同步调用 information_schema 估算源表行数，网络抖动致调度超时 | TaskExecutionService.java:328-337 | 异步化：在后台定时任务(缓存进程)预先估算所有源表行数，缓存到 Redis(TTL 1h)，同步路径秒级 lookup；添加超时控制：estimateRowsByConnection 加 timeout 参数(默认 2s)，超时自动降级为基础 DataX 配置；降级策略：若估算失败，使用上次缓存值或
- [M/m/risk:low] truncateLog() 截断日志到 1MB，超大 DataX 任务日志丢失关键信息，排查困难 | TaskExecutionService.java:599-605, saveTaskExecution():620-654 | 改为分片存储：关键日志(头部 256KB + 尾部 256KB) 分别存入 DB，中间日志存 S3/文件系统；或压缩日志(gzip)再存；或改为滑动窗口(保留最新 1MB)而不是简单截断，确保最后一行(完成标记)完整；对 DataX 输出做逐行分类(INFO/WARN/ERROR)，仅保留 ERRO
- [M/m/risk:med] processWaitingTasks 无去重，多次触发(手动重试+自动 tick)导致任务重复提交 | TaskSchedulerService.java:330-369 | 添加 run 记录粒度的状态锁(乐观锁版本号，或数据库 UNIQUE KEY 约束)；改为在 processWaitingTasks 入口处，对 (runDate, runType) 加分布式锁(Redis Setnx 或 DB 悲观锁)，确保同一天的扫描串行；或将去重键从 taskKey 改为 r
- [M/l/risk:high] Future.get(timeout) 后无进程清理，若任务强制终止进程仍驻留，导致僵尸进程和资源泄漏 | TaskExecutionService.java:139, 169-172 | 在 ProcessUtil.exec() 中增强进程清理：1) 记录 Process pid，超时时用 jdk.management.PlatformManagedObject 或 ProcessHandle.children() 递归杀死子进程；2) 添加进程监控线程，定期扫描孤儿进程；3) 设定
- [M/l/risk:high] executeScript() 逐条 SQL 执行无事务控制，中间失败时数据不一致 | TaskExecutionService.java:509-531 | 在脚本执行前添加 BEGIN TRANSACTION，失败时 ROLLBACK，但需检查 Doris 是否支持完整事务(或限制 SQL 到单个语句)；若不支持，改为幂等设计(DROP TABLE IF EXISTS + CREATE)，或版本分片(v1/v2 表)，完成后切表；加前置检查(创建临时表
- [L/s/risk:low] collectDownstream() DFS 递归深度限制 100 无提前告警，深 DAG 可能无法触发全部下游 | TaskDependencyService.java:238-253 | 预检测 DAG 深度，启动时计算最大深度并日志告警；若超过阈值(80)，显式记录警告；改为广度优先搜索(BFS)替代 DFS，避免栈溢出和深度限制；允许配置最大深度，默认 100，生产环境可调至 200。

## db-query (15)
- [H/m/risk:med] 血缘BFS一次性加载全表边(表级+列级)无分页 | /d/data/datanote/src/main/java/com/datanote/domain/orchestration/Linea | 按depth/源表分层分页或流式处理:①页面返回TOP N节点+limit;②改selectList为流式Cursor或游标分页;③按jobId分片异步构建邻接表缓存;④对查询加timeout与maxNode硬限(已有MAX_DEPTH=10，缺maxEdges限)。
- [H/l/risk:high] 向量索引全表扫一次性装内存+外部API调用阻塞 | /d/data/datanote/src/main/java/com/datanote/platform/ai/vector/VectorI | ①改selectList为分页:Page<T> select(offset,limit);②embedding改async+回源超时降级;③每BATCH完成即时flush向量库，不全量accumulate;④加内存使用监控与GC触发;⑤改@Async为分布式队列(kafka/redis list)可
- [H/m/risk:med] 标准稽核全表遍历(selectList+内存循环)事务过长 | /d/data/datanote/src/main/java/com/datanote/domain/governance/Standard | ①改@Transactional为仅insert部分;②selectList改分页loop处理(batch 10K列);③每batch的detail写库后清空，避免内存accumulate;④考虑MapReduce/Spark下推计算;⑤前置加scoped范围过滤sql where clause，而
- [H/m/risk:low] 指标消费层无上限查询+N+1查询(metricMapper/valueMapper) | /d/data/datanote/src/main/java/com/datanote/domain/consumption/MetricV | ①enabledMetrics()改分页:select status=1 orderBy updated_at LIMIT 1000;②latestSuccessMap()改:select metric_id, max(created_at) group by metric_id用聚合避免全load
- [H/m/risk:low] 治理健康分计算无分页+多表汇总查询 | /d/data/datanote/src/main/java/com/datanote/domain/governance/HealthSc | ①dimensionWeights()改:select dimension, SUM(weight) from dn_governance_metric where enabled=1 GROUP BY dimension(SQL聚合);②lineageScore()改:SELECT COUNT(D
- [H/s/risk:low] HikariCP连接池配置偏小(最大仅20)与无线程隔离 | /d/data/datanote/src/main/resources/application.yml:43-50 | ①改maximum-pool-size为50~100(需根据实际QPS压测定);②加线程隔离池，如引HikariCP提供的HikariDataSource包装多个小池(业务库20+调度库10+分析库10)，或用Spring Cloud Circuit Breaker isolate长查询;③把lon
- [H/s/risk:low] 业务线程池太小(schedule.pool.size=4) | /d/data/datanote/src/main/resources/application.yml:29-32 | ①改pool.size为10~20;②拆分线程池按优先级(critical-pool(size=4):心跳/控制,normal-pool(size=8):指标,background-pool(size=4):清理);③改fixedDelay为fixedRate+timeout守卫(任务超时cance
- [H/m/risk:low] 无对外接口速率限制与超时保护 | /d/data/datanote/src/main/java/com/datanote/common/GlobalExceptionHand | ①加全局RateLimit:@RateLimiter(value='50/s')或Sentinel QPS限流;②关键endpoint加Timeout:@Timeout(duration=10, unit=SECONDS)或Resilience4j.Timeout;③长查询改异步+队列:如reind
- [M/m/risk:med] 同步任务列表无权限过滤分页(内存removeIf) | /d/data/datanote/src/main/java/com/datanote/domain/integration/service | ①改selectList(null)为select id,job_name,... (除tableConfig/fieldMapping外的字段);②权限filter下沉SQL:数据库层按user角色过滤(join dn_user_role/dn_role_perm)或改为权限ID黑名单Where 
- [M/s/risk:low] 项目资产捆绑查询无分页(5个selectList(null)) | /d/data/datanote/src/main/java/com/datanote/domain/project/ProjectAsse | ①预加载+缓存:应用启动时全表load缓存到本地(sync后invalidate);②改selectList(null)为select id,name(仅2列);③数据量达100K后改Cache@Cacheable(cacheNames='projectAssets', unless='#resul
- [M/m/risk:med] 生命周期无用表识别全表扫+内存BFS | /d/data/datanote/src/main/java/com/datanote/domain/governance/Lifecycl | ①改selectList为分页:SELECT * FROM dn_table_meta LIMIT ? OFFSET ? batch处理;②lineage汇总为SQL:INSERT INTO dn_asset_stat SELECT tm.*, ... FROM dn_table_meta tm L
- [M/s/risk:low] 数据模型实体属性级联删除(内存query+循环delete) | /d/data/datanote/src/main/java/com/datanote/domain/datamodel/DataModel | ①where删除(SQL级联):DELETE FROM dn_model_attribute WHERE entity_id IN (SELECT id FROM dn_model_entity WHERE model_id=?);DELETE FROM dn_model_entity WHERE 
- [M/s/risk:low] Redis连接池最小闲置0(无预热) | /d/data/datanote/src/main/resources/application.yml:24-28 | ①改min-idle为2~4(保持2-4个长连接预热);②改max-active为20(适应高并发);③改timeout为3000ms(避免无限等待);④加Redis监控:阈值cpu>80%时告警,已连接连接数>max-active*0.8触发告警;⑤application中加@Configurat
- [M/s/risk:low] MyBatis日志无条件打印(生产环境应关闭) | /d/data/datanote/src/main/resources/application.yml:53-57 | ①移除${MYBATIS_LOG_IMPL}环保配置，硬编码NoLogging;②若需debug，改为仅输出slow query (>1000ms):如FakeSqlLogImpl自定义，门槛判定(durationMs>threshold时log.warn);③生产改:logging.level.c
- [M/s/risk:low] MapUnderscoreToCAamelCase配置全局生效(过度转换) | /d/data/datanote/src/main/resources/application.yml:56 | ①维持开启(便于Java代码POJO映射);②但selectList()/selectMaps()大结果集改手动构造resultMap(resultType=LinkedHashMap)避免反射;③关键高频查询(lineage、向量、指标)改原生sql.xml或@Select注解精确指定column

## web-concurrency (16)
- [H/m/risk:med] ProjectFavoriteService全局synchronized锁竞争 - 高并发查询阻塞 | /d/data/datanote/src/main/java/com/datanote/domain/project/ProjectFavo | 改用条纹锁(striped lock)按userId分片,避免全局锁.用ConcurrentHashMap + 原子操作(putIfAbsent/replace)替代synchronized,该操作缓存收藏状态按需回写,收藏/置顶操作异步化(@Async)不阻断HTTP响应.
- [H/l/risk:med] TaskDependencyService refreshAllDependencies全局synchronized - 血缘刷新阻塞 | /d/data/datanote/src/main/java/com/datanote/domain/orchestration/TaskD | 改为分页增量刷新,用版本号/时间戳变更检测增量部分.血缘刷新后缓存到Redis(TTL 1h),提供缓存版本化接口.操作本身改为异步(@Async)后台定时或事件驱动,不阻断主业务.
- [H/m/risk:med] TaskSchedulerService调度线程池满 - CallerRuns导致调度线程膨胀 | /d/data/datanote/src/main/java/com/datanote/domain/orchestration/TaskS | executor改为DiscardOldestPolicy或AbortPolicy,任务满时拒绝立即返回而非由调度线程跑.tick/processWaitingTasks改为异步(@Async)独立线程执行,不占用@Scheduled线程.queue改为无界LinkedBlockingQueue->
- [H/s/risk:low] 数据库连接池配置 - maximum-pool-size仅20导致长事务竞争 | /d/data/datanote/src/main/resources/application.yml:43-50 | maximum-pool-size改为min(CPU核心数*5, 40-50),根据实际长事务并发数动态调整.最大lifetime 580s偏短,长事务常见时改为900-1200s.增加connection-test-query生效前的超时,引入HikariCP maxWaitMillis(默认30
- [H/m/risk:med] @Scheduled调度池仅4线程 - 多个定时任务互相阻塞 | /d/data/datanote/src/main/resources/application.yml:31-32 | pool.size改为min(CPU核心数, 8),给予更多并行度.关键路径任务(tick/健康分)改使用独占@Bean ThreadPoolTaskScheduler不共享.长耗时任务改@Async后台线程不占用调度线程.应该区分优先级(调度>计算>外部调用).
- [M/s/risk:low] ProjectReleaseService submit方法synchronized锁全局 - 版本号递增竞争 | /d/data/datanote/src/main/java/com/datanote/domain/project/ProjectRele | 用原子操作AtomicLong维护version递增,或改用数据库sequence + MySQL last_insert_id(),避免synchronized.submit方法改为业务逻辑同步、DB插入异步的模式.
- [M/m/risk:low] LoginAttemptService内存兜底memRecordFailure - synchronized全局锁防暴破不力 | /d/data/datanote/src/main/java/com/datanote/platform/iam/LoginAttemptS | 改用条纹锁按username哈希分片(username.hashCode()%(2^n)),采用无锁的AtomicLong计数器维护失败次数.用ConcurrentHashMap<String,AtomicLong>+ConcurrentHashMap<String,Long>分别记录失败计数和锁定
- [M/s/risk:low] VectorStoreClient同步配置热刷 - 多线程竞争available()方法 | /d/data/datanote/src/main/java/com/datanote/platform/ai/vector/VectorS | reloadConfig改成写锁(ReadWriteLock.writeLock()),available改读锁,降低大量高频可读操作的锁竞争.或用volatile boolean+AtomicLong实现无锁ready/lastProbe检查(避免synchronized), CAS更新lastP
- [M/m/risk:low] CdcEngineManager start/stop/resetAndRestart全局synchronized - CDC多任务启停竞争 | /d/data/datanote/src/main/java/com/datanote/domain/integration/service | 改用ConcurrentHashMap提供的compute/computeIfPresent原子操作替代synchronized+手动检查.put/remove改为putIfAbsent/remove已是原子,无需synchronized包装.数据库操作(updateStatus/createExe
- [M/m/risk:med] SyncJobService并发编辑未用乐观锁 - updateTableCheckpoint/saveChunkCursor冲突 | /d/data/datanote/src/main/java/com/datanote/domain/integration/service | 加version/update_at乐观锁字段到表,updateTableCheckpoint/saveChunkCursor改为WHERE version=old_version的原子更新,避免synchronized.失败重试或用事件驱动单线程持久化(消息队列缓冲更新).
- [M/s/risk:low] RateLimiter synchronized单线程 - CDC/DataX限速成为瓶颈 | /d/data/datanote/src/main/java/com/datanote/domain/integration/util/Ra | 改为AtomicLong存储纳秒级上次更新时间,用CAS自旋更新避免synchronized.令牌余额改为volatile double配合时间戳原子性即可.或改成无锁令牌桶设计(分片令牌库).
- [M/s/risk:low] Redis连接池配置过小 - 并发高频操作等待连接 | /d/data/datanote/src/main/resources/application.yml:25-28 | max-active改为min(CPU核心数*2, 32),min-idle改为max-active的30%,提高连接复用率.开启连接超时重试和自动回收机制(keepalive-time).可考虑连接池分片(多个连接池客户端)分散并发.
- [M/s/risk:low] DashboardController stats接口无缓存查询多表 - 多次selectCount同步串行 | /d/data/datanote/src/main/java/com/datanote/platform/portal/DashboardC | 5个selectCount改成CompletableFuture并行查(三路并发),每路1s超时防数据库慢查.或用单条聚合SQL一次取全部count.缓存TTL改120-300s,配合缓存预热(应用启动时或定时refresher).
- [M/s/risk:low] OverviewService overview聚合调用未并行 - 健康分/资产/质量4个调用串行 | /d/data/datanote/src/main/java/com/datanote/domain/governance/Overview | 四个调用改CompletableFuture.allOf并行执行,每个独立2s超时.单个维度计算缓存独立(如gov:health、gov:assets),hit率更高且粒度细.或用ReactiveX/Reactor非阻塞调用.
- [M/m/risk:low] AiAgentService会话锁条纹数固定64 - 高并发用户会话哈希碰撞 | /d/data/datanote/src/main/java/com/datanote/platform/ai/agent/engine/A | 条纹数改为2的整数次幂更大值(256-1024),根据峰值并发会话数设置.改用ConcurrentHashMap<String, ReentrantLock>懒初始化,按sessionId精准锁无碰撞(注意内存溢出,可用LRU淘汰).或改为ReentrantReadWriteLock允许只读操作并发
- [L/s/risk:low] LogBroadcastService ownerCache无容量管理 - 内存泄漏风险 | /d/data/datanote/src/main/java/com/datanote/common/LogBroadcastService | 改用LinkedHashMap(accessOrder=true)实现LRU,或用Guava Cache with expireAfterAccess(1h).周期性清理已删任务的缓存项.建立任务归档表,缓存查库时优先查归档避免全表扫描.

## ai-perf (14)
- [H/l/risk:high] ReAct 循环 LLM 多轮串行调用阻塞长任务 | AiAgentService.java:232-602, 主循环每轮必调 LLM | 1)批量工具一阶规划：首轮 LLM 一次规划出3-4个工具链，放在 plan_json，后续仅校验+并发执行无需每步回询(借鉴 o1 tree-of-thought)。2)LLM 轻量评估：非关键路径工具(如 asset_detail)直接发射无需 LLM 预判，仅记 trace。3)异步队列化：T
- [H/m/risk:low] 向量库 EmbeddingService 单条嵌入串行调用无缓存/连接复用 | AiAgentService.java:214-216 建 RAG 向量，AiMemoryService.java:86 嵌入，Embedd | 1)连接池：HttpClient with PoolingHttpClientConnectionManager 替代每次新建 HttpURLConnection。2)大批嵌入合并：buildRagText 先收集全部候选，embedBatch 一次性 MIN(100,texts.size()) 条
- [H/m/risk:low] 内存记忆 selectList 无分页/缓存，全表扫导致 OOM | AiProfileService.java:76-79, 122-124, 92-94 selectList 无 LIMIT/offset， | 1)显式分页：selectList 都加 LIMIT + offset，批量处理(如 LIMIT 500 loop)，释放中间结果。2)缓存热点记忆：Redis 存 owner 最近 100 条 active memory，24h TTL，pruneOwner 仅扫增量新增部分。3)字段查询合并：l
- [M/m/risk:med] 会话级互斥锁 ReentrantLock 导致高并发竞争锁等待 | AiAgentService.java:110-119 SESSION_STRIPES[64]，172-180 tryLock with t | 1)乐观并发：用数据库版本号(version + CAS update)替代互斥锁，session update WHERE version=old AND... (无锁等待)。2)条纹数增加：SESSION_STRIPES 从 64 提升到 1024，冲突率降至 1-2%。3)读写分离：run()
- [M/m/risk:med] 上下文压缩强制重试 LLM 导致火焰山般再耗时 | ContextCompressorService.java:39-78, AiAgentService.java:276-281 | 1)主动分段：trace 达 100k 就预主动压(而非被动超窗再救)，分段保存。2)压缩缓存：同 goal_intent+owner，3天内压缩结果复用，相同长任务类型直接套用。3)增量压缩：仅压最早 30% 早期步骤，保留中后 70%(已有 keep_first 与 protect_tail_r
- [M/s/risk:low] 工具查询结果 N+1：逐条 selectById 而非 selectIn 批量查 | AiMemoryService.java:100-102 向量回查 selectById，ApprovalGate.java:85 sele | 1)批量回查：selectList(in(mysqlIds)) 一次返回，按 id HashMap 存，再按原顺序组装。2)向量库 payload 丰富：不查 title/content，直接存在 Qdrant payload(已有，仅缺 status)，MySQL 失败直接用 payload 兜底
- [M/m/risk:med] 并行工具执行缺乏超时/降级，单条慢工具卡整批 | AiAgentService.java:335-373 并行工具批，357行 get(PARALLEL_TOOL_TIMEOUT_SEC,  | 1)分级超时：高风险查询(血缘/图查询) 30s，低风险(库表检索) 60s；可配工具粒度超时。2)快速失败：工具执行 10s 无返回则预检 health，检测堆积/IO 瓶颈，超 5 个预检失败则该轮整批并行降级为串行(缓解级联)。3)资源隔离：数据库连接池为工具类型分组(查询/写各独立)，单类型
- [M/m/risk:low] 内存学习 learn/reviewAsync 无界队列堆积，高频会话成本失控 | AiAsyncConfig.java:17-30 aiLearnExecutor queueCapacity=100，AiAgentServ | 1)优先级队列：learn 改为 PriorityBlockingQueue，会话 usage 高(调用工具多)的优先蒸馏，低频琐碎会话延迟/舍弃。2)采样学习：不是每次都 learn，而是 5% 采样或仅 ≥3 工具的会话才 learn；review 也改为 10% 采样。3)批量蒸馏：不是单条 
- [M/l/risk:med] SSE 推送机制 EventBus 连接长占，多会话并发 socket 耗尽 | AiAgentService.java:273-274 callLlmStreaming，AgentEventBus(未显示但被引用 lin | 1)非流式优先：仅 web 前端首次问答时走流式(用户可见)，多步工具轮改为非流式+event 事件(step 完成即发)，释放 SSE 连接。2)连接超时与清理：eventBus.emit 若目标订阅者已断线，30s 内自动移除(心跳检测)。3)事件批处理：不逐字推送 token(token 数百
- [M/m/risk:med] Token 预算计数无粒度控制，长任务随意消耗无上限 | IterationBudget.java(未读)，AiAgentService.java:222 IterationBudget(maxPr | 1)Token 预算：在 IterationBudget 加 token 计数，估算每步 token(prompt+输出)，达预算则直接停(而非仅步数)。2)成本隔离：按 user/sessionId 统计日成本，达阈值则返回 'quota_exceeded'。3)限流降级：429/Rate Lim
- [M/s/risk:low] 工具发现 tool_search 每次全表扫库表元数据，无缓存 | 工具实现(未详读)，但触发工具发现时需遍历所有库/表/字段，SemanticSearchService.java:83-104 keywor | 1)元数据缓存：DnTableMeta 启动加载热点 top-1000 表到内存 HashMap(按访问频次缓存)。selectList(LIKE) 先本地内存匹配，再回库查(L1cache)。24h 自刷新。2)工具列表渐进加载：tool_search 返回仅 10 常用工具+搜索接口，用户搜索时
- [M/m/risk:high] 自主执行驱动器 driveAutonomous 心跳轮询粗糙，容易误杀或漏驱 | AiAgentService.java:971-1023 driveAutonomous 后台循环，987-988 更新 last_hear | 1)心跳同步：run() 执行中每隔 30s 更新一次 heartbeat(通过中间点检查点，line255)，而非 driveAutonomous 驱动时才更新。2)分布式锁：用 Redis 分布式锁替代 last_heartbeat，同一时刻仅一实例驱动，无重复。3)周期上限智能：guard 上
- [L/s/risk:low] 记忆召回分散多次嵌入调用，未批处理 | AiAgentService.java:214-216 fRag/fMem/fInd 三个 CompletableFuture 分别调 em | 1)共享嵌入缓存：queryHash -> float[] 本地缓存(HashMap，4KB/项，1000 项占 4MB)。line214-216 中 RAG/Memory/Industry 三路用同一 fRag CompletableFuture 返回的 query 向量，不重复嵌入。2)嵌入池预
- [L/s/risk:low] 工具调用时参数重复序列化为 JSON 字符串多次 | AiAgentService.java:364-365 argsToStr(a) 序列化，599-600 再序列化一次 toJson(res | 1)使用 JsonNode 透传：不转 String，直接在 step 对象中保存 JsonNode，mapper 层统一序列化一次。2)延迟序列化：step.resultDataString 改为 lazy 属性，仅在落库时触发 toString()。3)压缩存储：大结果(>100KB)改用 ZS

## frontend-perf (15)
- [H/s/risk:low] 首页仪表盘并发Promise.all未降级导致首屏阻塞 | src/main/resources/static/js/home-dashboard.js:121-126 | 将 Promise.all 改为 Promise.all(...map(then-catch降级))，各源catch返回 {}，确保网络抖动不卡首屏。参考：医疗HIS自动化测试报告提到熔断机制
- [H/s/risk:low] 表格搜索输入框防抖延时过长导致高频重排 | src/main/resources/static/js/dn-common.js:543, gov-consumption.js:207- | 防抖延时改为 300-350ms（电商标准），搜索过滤优化：缓存 querySelectorAll/textContent，用 CSS 类名切换 display 而非style.display操作。参考：Alibaba DataV搜索500ms标准
- [H/m/risk:med] 大表格全量客户端分页导致1000+行一次性DOM操作 | src/main/resources/static/js/dn-common.js:613-636 | 实施虚拟滚动(仅DOM化viewport内行数，滚动时动态刷新)，或改分页为 server-side paginate。短期：DocumentFragment 批量插入(减重排)。参考：Material-UI虚拟滚动方案
- [H/s/risk:low] 首页4张卡片串行加载+骨架，阻塞关键路径 | src/main/resources/static/js/home-dashboard.js:157-174 | 改为并发加载各卡片，去掉 BATCH 分帧(4张卡片无需BATCH节流)。缓存getOverview/getSyncSummary到单轮，两处共享同一Promise。参考：电商秒杀直播流媒体预热并发接口
- [M/s/risk:low] 首页60s自动刷新轮询未清理导致内存泄漏 | src/main/resources/static/js/home-dashboard.js:334-342 | 模块卸载时强制 clearInterval(_timer); _timer=null。用 AbortController 给 fetch 链停止，避免旧异步完成后还要处理。参考：React 组件 useEffect cleanup 做法
- [M/m/risk:low] 治理工单表格行过滤逐行遍历导致大表卡顿 | src/main/resources/static/js/gov-health.js:298-309 | 第一次加载时构建 Map{id→status}，后续复用。搜索用预建索引(inverted list) 或 Trie，不逐行逐字段匹配。参考：Lucene 倒排索引
- [M/s/risk:low] 血缘图谱waitTable轮询4s未超时导致卡UI | src/main/resources/static/js/gov-lineage.js:203-219 | 改 setTimeout 递归为 Promise race(DOMReady, timeoutPromise(3000))，超时自动降级。clearTimeout 前一个 centerWaitTimer 防重叠。参考：Promise.race 超时模式
- [M/m/risk:low] 孤儿表检测并发受限但未进度反馈导致卡顿感 | src/main/resources/static/js/gov-lineage.js:174, 152 | 加 progress bar (已处理 idx / items.length)，toast 定时更新。并发从6改为 3-4(平衡内存与延迟)。catch 时立即标记失败行，不堆积 null。参考：操作系统分页加载进度反馈UX
- [M/s/risk:low] 消费排行 forEach+slice 无去重导致重复处理 | src/main/resources/static/js/gov-consumption.js:96-106 | 改为 filter(x => x.cnt>0) 前置，再 slice(0,20).map()。减少不必要的对象构造。参考：函数式编程 filter-first 惯例
- [M/m/risk:med] 数据模型列表一次性渲染1000+行DOM | src/main/resources/static/js/datamodel.js:166-192 | 分页（pageSize=20），搜索改为 querySelectorAll + classList.add('hidden') 而非重排。或改 innerHTML 为 DocumentFragment 批量插入。参考：Ant Design Table 分页方案
- [M/s/risk:low] 治理工单快照轮询后未及时清理导致内存占用 | src/main/resources/static/js/gov-classification.js:470 | 用 WeakMap 关联 timer id，callback 执行前检查元素是否仍在 DOM（document.contains()）。或改为 AbortController 中止。参考：React 组件卸载检查 isMounted
- [M/s/risk:low] 质量规则近期执行记录逐条动态插入导致重排N次 | src/main/resources/static/js/dn-common.js:329-336 | 用 DocumentFragment 一次插入: var frag=doc.createDocumentFragment(); runs.forEach(rn=>frag.appendChild(line)); body.appendChild(frag)。减少重排次数从 n 到 1。参考：MDN 
- [L/s/risk:low] 治理健康分维度表格行 map 创建重对象未缓存 | src/main/resources/static/js/gov-health.js:171-182 | 缓存已渲染的 dims table rows，浅比较后决定是否重排。或用 useMemo 思路。参考：React useMemo 缓存行对象
- [L/s/risk:low] 消费数据集运行结果 colums map 重复遍历 | src/main/resources/static/js/gov-consumption.js:169-171, 177 | 一次遍历生成 {colDef, rows}，后续复用。或预构建列索引 Map{colIdx→colName}。参考：数据库查询计划优化避免多次扫表
- [L/s/risk:low] 主数据域/实体/属性表格编码唯一性检查用 .some() 无短路 | src/main/resources/static/js/mdm.js:91-95 | 预构建 codeLower 索引 Set = new Set(rows.map(r=>r[key].toLowerCase()))，codeTaken 改为 codeLower.has(lc && 'selfId' not equal)。参考：哈希表加速查找