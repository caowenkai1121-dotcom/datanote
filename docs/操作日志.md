# 操作日志

> 过程文档曾于 d8a29a4 全量清理, 本文件自 2026-06-12 重建, 只记关键决策与动作。

## 2026-06-12 全站迭代升级工程收官(11路评审101发现 → PM 25项全清)
- 批1: 开发模块5项(运行快捷键/删除门禁/版本去重/重命名落库/未保存守卫) + 数据目录4项(表摘要修根/主题域死端点/系统库黑名单/裸ALTER收敛) + 数据同步2项(调度断头接通/CDC死信合并) + AI助手会话状态机四合一 + MDM 审批门禁。
- 批2: 补数链路全收敛统一调度(删旧 BackfillService 全家6文件, 开发右栏/运维面板改走 /api/scheduler/backfill[-runs], 运维历史按批次分组); 质量口径修根(FAIL→failed)+规则弹窗补五字段(维度/阈值/cron/强阻断/启停)+治理页编辑启停; 质量分三处同数(computeScore); 资产清单分页; 血缘事件自动重建(SyncJobChangedEvent+@Async+02:40兜底); SysAlertScheduler 系统告警巡检(5min, heap/disk/db danger→通知中心, 1h去重); 首页同步卡改今日口径。
- 批3: 通知埋点四扩展(SCHED_FAILED 调度终败/QUALITY_ISSUE 建单+指派/MDM_REVIEW 审批终态/SYNC_FAILED 同步终败[项目owner→createdBy→admin回退链])+铃铛深链补 operations/mdm/dbsync/settings 路由; 数据预览脱敏接入(fail-closed, MASK/HASH/REPLACE/RANGE 四函数与 SQL 改写语义对齐, profile 无值泄露不接)。
- 附带修复: 存量规则旧词 ruleType 编辑回填静默置空 bug(qrNormalizeType 归一+severity 白名单), 规则9数据修正。
- 版本 ?v=u33, 本地构建+E2E 全绿(console 0 错误), 未部署服务器(遵 2026-06-08 停部署令), git 未提交(待业主指令)。
- 下一批候补(终审落选项): 基线管理做实 / 健康分隐形维度还原 / 审计专项。

## 2026-06-12 批4: 候补三项收官
- 基线做实: 新增 BaselineCheckService(每 5 分钟巡检, 启用基线的关联任务须在承诺时间前完成当日调度, met/broken/pending/empty 四态), 打破→通知中心 BASELINE_BROKEN(同基线同日去重); 新端点 /api/baseline/status-today; 基线面板加"今日达成"列(打破悬停可见未达任务)。
- 健康分还原真五维: 删除 DCMM 成熟度汇入(主观问卷不入客观分, Domain 方案 b), 健康分输出加 scoreVersion="v2-五维" 口径标注(总分跳变可解释); DCMM 自评经查前端零调用属纯孤岛, 整块下架(Service/模型/Mapper/三端点), 表数据未动。
- 审计专项: 敏感只读操作(导出/下载/数据预览/探查)纳入审计留痕, 审计日志导出自身不再豁免; 新增 DATA_PREVIEW 动作类型; 新端点 /api/gov/audit/types, 治理审计中心类型下拉动态化(自动纳入 AI_AGENT_WRITE 等新类型)。
- 版本 ?v=u34, 构建+单测+E2E 全绿, git 未提交。

## 2026-06-12 批5: 全站运行时交互 bug 系统排查(业主报告"系统很多功能有问题")
业主报告三个具体现象(删除弹窗不弹→卡死 / 指标页无法下滑 / 顶部菜单溢出藏掉 AI助手),指出"系统应该有很多这样的问题"。主线先真机定位三个根因,再用 Workflow 五维静态审查 + 两个补审 agent 找出全部同类问题,全修并真机验证。

**业主报告的三个根因(铁案,真机实测)**:
1. 删除/确认弹窗卡死(双层根因): (a) #msgBox 内层 class 含 dn-modal,而 .dn-modal{opacity:0} 仅靠 .dn-modal-mask.show 恢复,父级是 .intg-modal-overlay 不匹配 → 弹窗透明; (b) .intg-modal-overlay z-index 仅 200,远低于抽屉(9000)/弹窗(10000),在抽屉上触发 msgConfirm 被埋在下方。两层都导致"遮罩盖屏但按钮点不到 → Promise 永挂 → 卡死"。修: 删 dn-modal class + z-index 提到 var(--z-spotlight) 100000。
2. 指标/项目页无法下滑: app.css 第1971行有孤立 CSS 声明 `font-size:13px;}`(缺选择器),破坏紧随的 .view-quality 规则解析,flex:1 失效 → 内容高度不受限,ops-main overflow:auto 形同虚设。修: 删孤立声明(CSS 解析恢复,view-quality/viewMetrics/viewProject 滚动全恢复)。
3. 顶部 AI助手被裁: header-tabs 11 个 tab 溢出(1227>1086),overflow:auto 把最右"AI助手"裁掉不可见。修: 压缩 tab padding 20→11、gap、logo/header-right 间距,标准宽度放下全部 11 tab。

**Workflow 五维审查(CSS健康/弹窗/滚动/布局溢出/交互死链)+ 补审,共修**:
- 弹窗类: z-index 提升(critical)、msg 单例 _msgBoxResolve 覆写致旧 Promise 永挂(加入口收尾)、sched-log-overlay querySelector 误删别的 overlay(改 btn.closest 关自身)、removeChild→remove()、quality-modal-overlay 显隐范式统一(display:none+.show, z 提到 --z-modal)。
- 布局溢出(同 header-tabs 根因): datamap-detail-actions(9按钮)flex-wrap、datamap-detail-tabs/dbsync-drawer-tabs overflow-x 横滚、sched-stats/log-toolbar wrap。
- 交互死链(7 处裸 fetch/逐请求无 catch 致"点了卡住/无反馈"): startBackfill 逐日提交改 settle 模式(任一失败也汇总)、syncModalConfirm 下拉永卡"加载中"补 catch 复位、submitCreateBaseline/doBackfill/schedSaveConfig/saveEnvConfig/saveDatasourceConfig 补 catch。
- onclick 死链: 281 函数全有定义,0 死链。CSS 健康: app.css 3890行+dn-design.css 括号平衡,无其它孤立声明。
- 保留(已知低危): ai-agent.js dn-ai-mask append #aiAgentRoot(潜在 transform 包含块,当前不触发)、ops-main 隐式滚动链(agent 警示改前须全回归)。
- 版本 ?v=u36, 构建+真机全面验证全绿(弹窗 z=100000 可见、指标注入超高内容真能滚、AI助手可见、单例保护、console 0 错误), git 未提交。

## 2026-06-12 批6: 抽屉系统全局治理(业主报告"抽屉跳转后样式坏/抽屉里弹窗有问题/多入口内容重复")
全站三套抽屉实现: DN.drawer(.gov-drawer 动态单例, gov/mdm/ai 共15模块用)、静态 dbsync-drawer(项目详情 #projDetailDrawer / 同步详情 #dbsyncDetailDrawer, .show 切换)、AI 抽屉(.dn-ai-drawer)。真机逐项定位根因并修复(?v=u39)。
1. **跳转后样式坏 = 抽屉残留(铁证)**: navigateTo(workspace.html:7020) 切视图时只关项目详情抽屉(projCloseDetail), 不关 DN.drawer/dbsync/AI 抽屉 → 从这些抽屉点跳转链接后, 遮罩(z=9000)+抽屉残留盖住新视图, 且 DN._drawerKey 键盘监听泄漏。修: dn-common.js 新增 DN.closeAllDrawers()(清三套抽屉 DOM + 解绑监听 + 调各自 close), navigateTo 入口统一调用。真机验证: 跳转后 mask/drawer/监听全部清除。
2. **抽屉里弹窗有问题 = g-modal 被抽屉埋(铁证)**: showModal/msgBox/confirmBox/promptBox 共用的 .g-modal-overlay z-index 仅 2250 < 抽屉 9001, 抽屉内调用时弹窗被埋在抽屉下点不到 → 卡死。修: app.css .g-modal-overlay z-index → var(--z-spotlight) 100000(与 #msgBox 同级)。真机验证 g-modal z=100000 > 9001。(注: DN.confirm 10000、msgConfirm 100000 本就在抽屉上, 仅 g-modal 漏了)
3. **多入口内容重复 = 详情切换残留**: dmOpenTable(数据地图表详情) 切换表时只清了 detailBody 加载态, 标题/副标题(dmDetailTitle/dmDetailSub)未即时清空 → 远程库 information_schema 取数慢时(数秒), 标题栏残留上一张表名/行数 → 看似"内容没变"。修: 入口立即把标题设为新表名、副标题设"加载中"。真机验证: 切表标题立即更新+加载态。(对比: dbsyncOpenDetail 本就立即清头部+所有 pane 加载态, 是标准范式; dmOpenTable 漏标题已补齐)
- 完整 z 层级体系核对: 抽屉 9000/9001 < DN.confirm·quality-modal 10000 < msgBox·g-modal 100000 < toast 100010, 所有弹窗现都在抽屉上层。
- DN.drawer 30 个调用全部正确传对象标识到标题(db.table/bizKey/id), 内容按对象区分, 无"忽略参数"bug。
- 架构观察(专业视角, 本次未动): 三套抽屉实现风格/机制不统一(建议长期统一到 DN.drawer); DN.drawer 是单例(抽屉内再开 DN.drawer 会替换当前抽屉, 抽屉内二次确认应用 DN.confirm, 已在抽屉上层)。
- 版本 ?v=u39, 构建+真机验证全绿, console 0 错误, git 未提交。Workflow 抽屉调研因 API 临时故障失败, 改主线真机+grep 自行完成。

## 2026-06-12 批7: 三套抽屉统一到 DN.drawer(业主指令)
全站三套抽屉实现:DN.drawer(.gov-drawer 动态工厂)、静态 dbsync-drawer(项目/同步详情, 复杂多 tab)、AI 抽屉(.dn-ai-drawer)。专业判断:复杂 8/7-tab 详情抽屉重写为 DN.drawer 工厂风险过高且违反极简原则, 正确的统一是收敛三套到一套视觉/层级/动画/关闭标准(以 DN.drawer 为基准)。
- **AI 抽屉 → 真正改用 DN.drawer 工厂**(ai-agent.js openDrawer): 经验/定时/审批内容是简单列表, 改 DN.drawer(title, body) 后获得统一的 .gov-drawer 外壳/滑入滑出动画/Esc·遮罩·×关闭/焦点陷阱 a11y/被 closeAllDrawers 管理。真机验证: 点经验按钮产生 .gov-drawer(z9001) 而非旧 #aiDrawer, 标题/内容正确, Esc 关闭生效。
- **dbsync-drawer 层级+背景统一**(app.css): mask z 2200→var(--z-drawer)9000、drawer z 2210→calc(+1)9001(原硬编码远低于 DN.drawer, 同屏会被压); 背景 bg-body→bg-card。真机验证项目/同步详情 z=9001/9000、bg=白。
- **项目详情抽屉补 Esc 关闭**(project.js): 原 Esc 只关弹窗不关详情抽屉, 补齐后三套抽屉 Esc 行为完全一致(同步详情本就有 Esc)。真机验证 Esc 关闭生效。
- 结果: 全站抽屉视觉/层级/动画/关闭行为完全统一(一套设计语言)。实现上剩两套(DN.drawer 简单内容 + dbsync-drawer 复杂 tab), 后者保留静态 DOM 便于多 tab 状态管理, 是合理的内容适配而非重复。
- 版本 ?v=u40, 构建+真机验证全绿(三套抽屉 z9000/9001 统一/AI 用工厂/项目详情 Esc/console 0 错误)。
- 本次按业主指令提交全部累积工作到 github(caowen remote)。tp/(14M 参考 PDF)加入 .gitignore 不入库。

## 2026-06-12 批8: 登录界面 + 用户权限管理大工程(第1+2轮)
业主令: 做登录界面+彻底完善用户权限(模块+功能+数据级粒度, 做完强制登录)。调研发现后端 RBAC 数据层早已完整(platform/iam + sql/36 预置 admin/admin123), 缺登录页/权限点体系/接口鉴权/前端权限控制四块。
**第1轮(?v=u41)**: PermCatalog.java 权限点清单(11模块×view+功能) + GET /api/rbac/perms/catalog; login.html 登录页(墨青品牌区+表单, 已登录自动进); workspace 前端守卫(dnBootGate 未登录跳 login/401跳login/dnHasPerm/dnApplyMenuPerms 菜单按权限隐藏/navigateTo 路由守卫/顶部用户名+登出); sql/79 标准角色(DATA_DEV/DATA_GOV/PROJECT_MGR/VIEWER)已应用库。
**第2轮(?v=u42)**: PermInterceptor 接口级鉴权拦截器(写操作按 URL→权限点有序映射[审批/运行类特例优先], 敏感 GET 特例[审计导出/RBAC列表], 普通 GET 只要求登录防跨模块误伤, 权限集30s缓存, 403返JSON带所需权限点) + WebMvcConfig 注册; requiredPerm 纯函数单测; 前端 403 toast; 用户管理权限配置从手输 textarea 改为 11 模块分组树形勾选(组头全选/全部权限*); 新增 governance:manage 权限点。
E2E 真机: viewer/viewer123(VIEWER角色)登录→写操作 403 带"需要 xxx"提示/普通读 200/菜单只显 10 个(系统管理隐藏); admin 权限树 11组33点正确勾选。**启动新增环境变量 DATANOTE_PASSWORD(设了即强制登录, 默认账号 admin/admin123)**。
待续: 数据级权限(按 owner 行级过滤, 涉及各模块 list 接口, 渐进); 按钮级前端隐藏; 用户自助改密。
**第3轮(?v=u43, 三轮收官)**: 数据级权限落地项目维度(无 project:all-data 的用户项目列表只见自己负责/创建/参与的, 直开他人项目详情被拒; 新权限点 project:all-data 给 PROJECT_MGR); 自助改密(POST /api/auth/change-password 校验原密码, 顶部用户框→账号抽屉[改密+登出]); 按钮级隐藏机制(dnApplyBtnPerms 扫 [data-perm], 6 个主"新建"按钮挂权限, viewer 全隐/admin 全显); 顺手修回 IA v3 丢失的质量"新建规则"按钮入口。E2E 全绿(admin 3 项目/viewer 0/详情拒/改密往返/按钮显隐/console 0 错误)。

**收尾轮(?v=u44)**: git 欠账还清(commit 1b625e9 共114文件 + 622b85e, push github caowen/master 双向验证); 动态行按钮挂权限(质量/指标/基线行内 编辑/删除/启停 按 data-perm 显隐, 只读按钮保留), viewer 视角 E2E 验证; 改密链路复测全通(此前一次失败判定为远程库网络抖动孤立事件)。

## 2026-06-12 批9: 多用户身份贯穿(权限工程第4轮, ?v=u45)
权限体系建好后补身份贯穿洞: 核心资产(脚本/同步任务/质量规则)新建完全不记创建人、基线/收藏/评论硬编码 default —— 四类失败通知(SCHED_FAILED/SYNC_FAILED/QUALITY_ISSUE/BASELINE_BROKEN)的接收人路由全打空。
- 新增 CurrentUserUtil 统一取当前登录用户; 7 个写入点接真实用户(脚本/同步任务/质量规则/基线 createdBy, 指标 owner 默认创建者, 收藏/评论作者)。
- 新增 GET /api/rbac/usernames 轻量选人端点(仅 username/nickname, 登录即可); 修复权限工程引入的回归: 项目添加成员"从用户列表选"调 /users 需 settings:user 致普通负责人 403, 改调 usernames; 单测防误拦。
- E2E: viewer 调 usernames 200(无敏感字段)/完整 users 仍 403/admin 建规则 createdBy=admin 真实落库。commit 65b55c4 已 push github 验证。

## 2026-06-12 批10: 身份贯穿 UI 收口(权限工程第5轮, ?v=u46)
- 全局用户 datalist(登录后由 /api/rbac/usernames 填充): 任何"负责人/用户名"输入框挂 list="dnUserList" 即可下拉选真实用户。已挂 5 处: 指标负责人 + 治理工单 指派/批量指派/转任务/新建工单。
- 质量规则表与基线表新增「创建人」列(资产归属一眼可见, 存量为 anonymous 属真实历史)。
- E2E 全绿, commit 58b3be0 已 push github 验证。

## 2026-06-12 批11: 用户权限深挖·安全加固轮(ultracode, ?v=u49)
业主令"继续深挖 用户权限管理"(/effort ultracode 开)。用 Workflow 4 维并行审计(security/audit/usermgmt/authz)得 24 缺口, 实现全部 critical+high(commit 5ea74de 后端 + e7d077e 前端浮现, 均 push caowen 双向验证远程=本地)。
- **防暴力破解**: 新建 LoginAttemptService(内存滑窗 5 次失败/10min→锁 15min)。login 前置锁检查(423+剩余秒)/成功失败均审计(LOGIN/LOGIN_FAIL 带剩余次数/LOGIN_LOCKED)。
- **会话固定防护**: 登录成功先 old session.invalidate() 再建新会话。
- **口令强度策略**: 新建 PasswordPolicy(≥8 位/≤64/弱口令黑名单/字母数字符号≥2 类), createUser/updateUser/changePassword 统一校验。
- **内存 admin 兜底收口**(防同名无角色账号白拿超管 *): 3 处 '*' 兜底仅 `isEmpty && !existsInDb` 才给; 停用 DB 用户走 DisabledException(AccountStatusException)语义, ProviderManager 终止不回退内存 admin。
- **用户管理护栏**: 不可停用/删除自己、不可停用/删除最后一名启用超管; assignRoles 越权自提升防护(非超管不得授予自己不具备的权限点→403); 改密/删用户/分配角色/设角色权限全 PERM_CHANGE 审计。
- **会话硬化**(application.yml): session timeout 8h + cookie http-only + same-site lax(CSRF 兜底)。审计去重: shouldAudit 跳过 /api/auth/login(AuthController 自审计)。
- **有效权限浮现**: 新增 GET /api/rbac/users/{id}/perms(跨角色去重); listRoles 加 userCount; dn_user 加 last_login_at(sql/80 已应用库)。前端: 角色表「使用」列 userCount 徽标 + 删角色提示绑定数; 用户行「权限」入口按 PermCatalog 模块分组只读展示有效权限; 重置密码提示补口令要求; 用户列表加最后登录列。
- 单测: LoginAttemptServiceTest(4)+PasswordPolicyTest(5)+PermInterceptorRuleTest(5)+AuditPureFnTest(21) 全绿。E2E 真机: 5 次失败锁定/弱口令拒/停用自己拒/role userCount 徽标/viewer 有效权限抽屉 10 view 点分组渲染/AI 配置写需 settings:config/自提升 403 全验证。
- 候补 medium/low: 各模块 list 行级 owner 过滤(仅 project 已做, effort L)/用户分页 N+1/批量操作/首登强制改密/CSRF token/perms-catalog 端点收紧。

## 2026-06-12 批12: 首登强制改密(用户权限深挖续, ?v=u50)
管理员建号/重置密码后用户首次登录强制改掉初始密码(管理员长期知晓他人密码是安全洞)。
- 后端: dn_user 加 must_change_pwd 列(sql/81 幂等, information_schema 判断兼容 MySQL 8, 已应用库); DnUser 加字段; RbacService.createUser/updateUser(传密码时)置 1、changePassword 清 0、新增 mustChangePwd(username) 查询; AuthController.status 暴露 mustChangePwd(内存兜底账号 false)。
- 前端: dnBootGate 读标志→渲染后弹不可关闭全屏遮罩 dnForceChangePwd(z99999, 唯一逃生口=登出, 改密成功 reload); 顺手对齐账号抽屉改密提示 ≥6位→≥8位(与 PasswordPolicy 后端口径一致)。
- E2E(curl 全链路 + Playwright 真机): 建号 flag=1 / login status mustChangePwd:true / 遮罩四要素渲染 / UI 填表改密清 flag / reload 不再弹 / 新密码登录通过; 既有 admin·viewer flag=0 不受扰; 测试号 tmpuser 已清。commit bb97996 push caowen 双向验证(远程=本地)。

## 2026-06-12 批13: 批量用户操作 + 权限点清单端点收紧(用户权限深挖续, ?v=u51)
- 批量用户操作: 用户表加复选框列(全选/行选/select-all 联动) + 批量工具栏(已选 N + 批量启用/停用/追加角色/删除/清除选择)。后端 3 端点 POST /users/batch-status|batch-delete|batch-roles —— 逐个复用单操作护栏(跳过自己/最后超管, 返回 success+skipped 明细); batch-roles 追加式(现有角色 ∪ 新角色, 不覆盖) + 越权自提升防护; 经 WRITE_RULES rbac 前缀自动鉴权 settings:user。
- 安全搭车: GET /api/rbac/perms/catalog 收紧到 settings:user(原仅要求登录, 泄露全站权限 taxonomy; 仅 umPermModal/umViewUserPerms 用, 均 admin 可达), 单测断言同步更新。
- E2E(curl + Playwright 真机): batch-status 停用含 self→skip / batch-roles 追加 VIEWER→2 用户得 10 view 点 / batch-delete success 3 / viewer 调 perms/catalog 与 batch-status 均 403 / admin perms/catalog 200 / 前端复选框+全选联动+追加角色 modal 全链路。测试号已清(仅 admin+viewer)。commit 6924136 push caowen 双向验证(远程=本地)。

## 2026-06-12 批14: 停用/删除用户存量会话立即失效(用户权限深挖续, 纯后端)
自查发现真洞: 鉴权只查角色从不看 status —— 停用/删除用户后其已登录 session 完全不受影响(写权限照旧、普通 GET 仍可读全站), 离职/盗号紧急停用形同虚设。
- PermInterceptor 缓存条目加 active(表内用户须 status=1; 表外仅内存兜底引导账号有效), 已认证请求先做账号有效性检查(含普通 GET): 不活→invalidate session + 401 "账号已停用或删除"(前端既有 401 跳 login, 零前端改动); 查询异常 fail-open 读不踢会话。
- 加 evictAll(), RbacController 在 停用/删除/分配角色/设角色权限/删角色/三个批量端点 后主动清缓存 → 变更立即生效(原权限变更最迟延迟 30s)。
- 行级 owner 过滤推广候补已否决(Domain): 脚本/同步任务/质量规则是团队共享资产, 个人级隔离反协作; 空间隔离已由项目维度承担。
- E2E: 停用→旧 session 下个请求即 401 / 重新启用旧 session 仍死(须重新登录) / 删除同样即时 / 受保护端点验证彻底失效 / admin·viewer 回归正常; 单测 35 绿; 测试号已清。commit 3d7f213 push caowen 双向验证(远程=本地)。

## 2026-06-12 批15: 权限细分——登录直进首页 + 全站按钮无权限不显示(ultracode Workflow, ?v=u53)
业主令①登录进来直接进首页②没权限的功能不显示(无运行权限不放出运行按钮)。
- A 登录直进首页: login.html 登录成功/已登录预检均 replace 'workspace.html#/home'。
- B 按钮权限机制升级(根治覆盖盲区): 新增 dnObserveBtnPerms —— MutationObserver 监听 DOM 新增, 含 [data-perm] 即 rAF 防抖自动隐藏。各模块动态渲染按钮无需每处手动调 dnApplyBtnPerms, 重渲染后也不漏。
- C 全站操作按钮挂 data-perm(两阶段 Workflow): Phase1 审计 26 Explore agents 得 228 按钮; Phase2 实施 23 agents 挂 230 按钮(20 js 并行+workspace 三段串行)。写/运行/危险操作挂, 查看/筛选/导航不挂。坑: DN.h 创建元素须 'data-perm' 带引号 key(含连字符, 否则语法错废文件; Phase1 片段 68/228 裸键 bug 故 Phase2 强约束规范挂法), HTML 模板用 data-perm=""。覆盖 workspace.html(117)+20 js。
- 验证: node --check 全 js OK+无裸键; admin(*) 直进#/home/11模块console 0错/全显; viewer 9模块写按钮全隐+菜单隐藏settings+console 0错; observer 动态注入 develop:run/mdm:manage 隐藏、governance:view 显示; 开放模式不隐藏。commit 6606d5a push caowen 双向验证(远程=本地)。
