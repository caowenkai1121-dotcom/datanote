# DataNote 重构 — 操作日志

> 仅记关键决策与部署动作，便于复盘。凭据绝不入此文件。

## 2026-06-07

### 01:50 启动 · 现状基线
- 业主指令：彻底重构（推翻重建）+ 全程无人值守。已就"系统实为成熟系统(627文件/72轮迭代/已上线)、推翻风险不可逆且冲突全局红线"向业主明确提示；业主在知情下确认 **字面执行推翻重建 + 完全无人值守**（测试环境、已备份、出问题无所谓）。据此推进。
- 远程服务器基线（38.76.183.50）：
  - `datanote` 服务 active，HTTP 8099 返回 302（登录重定向，健康）。
  - uptime 1.5 天，load ~0.7。
  - **磁盘 /opt 90% 满（44G/49G，剩 5G）**，backups 已堆 159 个 → 反复部署前需清理。
  - **内存 15G 用 13G，剩 912M** → 编译尽量本地、远程仅跑 jar。
  - MySQL 在 3307、Doris FE 9030 在听。
- 本地工具：mvn 3.9.10、java、python3.13+paramiko 可用；无 sshpass（用 `deploy_jar.py` paramiko 方案部署）。

### 01:50 启动深度调研工作流（ultracode/Workflow）
- 5 并行清点 agent（后端API / 数据模型 / sync引擎 / 前端SPA / 基建部署）→ 汇聚 → 3 角色分析（Domain / Architect / Product）。
- 产出将写入 `docs/00-现状调研.md` 与 `docs/01-重构总体方案.md`（含≥5轮路线图），随后进入第1轮。

### 01:52 清理远程 backups 释放磁盘
- backups 占 12G / 159 份 → 保留最近 10 份，删除 149 份旧 jar 备份。
- 磁盘 /opt：**90% → 68%**（剩 16G）。为后续反复部署腾出空间。删除仅限 jar 备份，测试环境且业主已备份。

### 02:30 调研完成 + 重构方案定稿（关键决策）
- 调研工作流返回：5 域清点 + 3 角色分析（8 agent，718k token）。
- **关键决策**：团队三角色（Domain 行使一票否决权）一致反对"字面删光重写"。PM-Lead 按 charter 岔路口机制拍板 **绞杀式渐进重构**（达成彻底重构目标 + 守住全局红线），详见 `待我决策.md` 头条。
- 产出 `docs/00-现状调研.md` + `docs/01-重构总体方案.md`（10 轮路线图）。
- 待业主拍板项（DataX 去留 / MDM 冻结 / 项目协作裁剪 / Dolphin / Vue3）已记入 `待我决策.md`，先做无争议部分。
- 进入 **R1：治理接点① 质量→工单自动联动**。

### 02:15 R1 实现 + 全量回归
- 改 `IssueService`(自动建单+去重+5纯函数)/`QualityService`(收口调用)/`gov-quality.js`(toast提示+修复存量红测 workspace.html#/quality 链接)/新增 `QualityIssueRaiseTest`(8 单测)。
- 全量回归 **493 跑 0 失败 0 错误 1 跳过 BUILD SUCCESS**（顺手修复了 HEAD 上既存的 `GovernanceQualityUiTest` 红测）。

### 02:16 部署 R1 到生产
- `deploy_jar.py`：80MB jar 分块上传 md5 校验通过 → 备份旧 jar → systemctl restart → **active / http 302 / pid 756305**。

### 02:25 R1 服务器端 E2E（QA 通过）
- 造必失败质量规则→执行→**工单自动生成(QUALITY/MEDIUM/OPEN)**→复跑**去重(恒1条)**→清理测试数据(issues=0/rules=0 回干净基线)。
- 代码零缺陷；2 项测试侧问题(table_name 测试数据缺字段、删除路径笔误)已闭环。
- **R1 收口完成，进入 commit。**

### 02:35 R1 提交 + 推送
- commit `4f5ee9a`（11 files, +619），push 到 caowen `9cc16da..4f5ee9a`。

### 02:40 R2 治理闭环加固：质量恢复→自动关单
- 范围重排（PM-Lead）：原 R2 的"脱敏统一编排"与"分类→标签回写"因需前置设计（SELECT* 列展开/用户上下文集中化；tags 格式未定）拆为专门轮 R3/R4；本轮只做安全加法"恢复→自动关单"。
- 改 `IssueService`：成功分支 `closeQualityIssueOnRecovery`（仅自动关 OPEN 单、追加关单说明，FIXING/RESOLVED 等人工流转单不动）+ 纯函数 `shouldAutoCloseOnRecovery`；+1 单测。全量回归 **494 绿**。
- 部署：active/http302/**pid 763936**。
- E2E：S1 失败→OPEN→达标→**自动CLOSED**；S2 人工 FIXING 单达标后**仍 FIXING（人工保护）**；清理残留 0/0。**QA 通过。**
- commit `3e7fe0a`，push caowen `4f5ee9a..3e7fe0a`。

### 02:50 R3 治理接点③：分类确认→表级敏感标签回写
- tags 格式核实为 CSV(逗号/中文逗号),可安全幂等增删。`ClassificationService.confirm` 末尾调 `syncTableSensitiveTag`(按全表敏感列计数自愈增删"含敏感字段"标签)+纯函数 `applySensitiveTag`+5 单测。全量回归 **499 绿**。
- 部署：active/http302/**pid 776674**。
- E2E：确认列敏感→表 tags=含敏感字段；降级→标签移除；MySQL 清理 r3_e2e_tbl 测试行(残留=0)。**QA 通过。**
- 注：服务器 DB 凭据在 `/opt/datanote/datanote.env`(DB_USERNAME/DB_PASSWORD),清理测试行用 MYSQL_PWD 免交互。

### 03:30 方法论纠偏（业主反馈）
- 业主指出：R1–R4 是"加小功能"，**不是重构**。要求：每轮**架构师深度主导**、调整 **50+ 处现有代码**、消除重复/重划边界、改完"像新系统"——即执行 `01-重构总体方案` 的结构性轮次。
- R4（工单运营端点，纯加法）收尾提交 `a927d5d` 清树（随下轮 jar 部署），**自此转入真·结构重构**。
- 启动**架构师主导设计工作流**：深读三大重复域（调度／元数据资产／横切告警-审计-偏好）→ 架构师评分推荐首个结构轮 + ≥50 处变更蓝图（绞杀式保行为）→ Domain 验行为不变／Product 验功能不丢。蓝图回来后执行。

### 第4轮 · 调度域结构重构（首个真·结构重构轮）
- 架构师蓝图（6 agent/467k token）推荐**调度域首发**（ROI5×可靠5×单轮可绞杀5），蓝图存 `docs/blueprints/R4-调度域重构蓝图.md`，Domain/Product 有条件通过。
- 执行 51 条变更：新建 `ScheduleTargetType` 枚举 + `ScheduleLifecycleService`（统一脚本/同步 8 种上下线为 4 方法，消 ~150 行 copy-paste）+ 拆 3 门面 `ScheduleLifecycle/Execution/Monitor Controller`（迁 23 端点逻辑，路径一字不变）+ **删除旧巨类 `LocalSchedulerController`(-527行)** + 5 行为单测。**零表变更、零前端改动**。
- 全量回归 **507 绿**（@SpringBootTest 上下文加载成功=无 Ambiguous mapping）。
- 部署：active/http302/**pid 863812**。
- E2E：**24 端点 24/24 可达(0 个 404)** + cron-preview 真返回 + 真实脚本 local-online→online+版本快照、local-offline→offline、清理残留 0/0。**QA 通过，行为一字不变。** commit `2b1d237` 推送。

### 第5轮 · 元数据/资产域结构重构（DataMapService god-service 拆分）
- 架构师 #2 ROI 靶点：`DataMapService`(648行/27方法)"既连库探查又读离线目录"拆为 `DatasourceExploreService`(在线探查,唯一直连库出口,14方法)+ 瘦身 `DataMapService`(离线目录,648→~280行,跨用委托 explore,不再持 DB 连接);`MetadataController` 21 调用点按在线/离线分流两服务,**端点一字不变、前端零改动**。3 个测试随迁。全量回归 **507 绿**。
- 部署：active/http302/**pid 882706**。**零表变更**。
- ⚠️ 部署时发现 **Doris FE(9030) 宕机**(与重构无关,历史亦有 BE 宕机)：先验行为不变(databases 返 R.fail 与重构前一致、离线端点正常),再**救活 Doris FE**(`/opt/doris/apache-doris-3.0.8-bin-x64/fe/bin/start_fe.sh --daemon`,9030 恢复,数据地图复活)。
- E2E(Doris 恢复后)：**13 个元数据端点全 code=0**(在线 tables/columns/preview/ddl/profile/partitions + 混合 table-detail + 离线 all-tables/search/popular/favorites/comments)。**QA 通过,行为一字不变。** commit `a74e4d9` 推送。

### 第6轮 · 前端整体重构(UI设计师主导, 业主红线升级后首个前端大重构)
- 6-agent 工作流(UI设计师+架构师+业务专家)测绘+设计 → 全新设计系统 **"Slate & Indigo 石墨靛蓝"**(主色 #1890ff→#3457d5),重定义 :root 令牌值(不改名,级联全站含gov/mdm)+WCAG AA+暗色主题预留。
- 字节级处理 workspace.html(保 4 个 NUL):内联 `<style>`(L8-3317,3310行)外置到 `css/app.css`(124KB,legacy+设计系统覆盖层)+替换为 `<link>`+全站 `?v=` 统一 q100r80;删 6990 行孤儿 `dev.html`;新 `docs/设计规范.md`。**零 DOM/class/id/事件/路由/端点改动**。
- 全量回归 **507 绿**(WorkspaceDbSyncUiTest 的 CSS 断言随迁 app.css)。clean 打包(确保 jar 无 dev.html 残留)。
- 部署:active/http302/**pid 926028**。E2E:**app.css 200含新主色#3457d5 / gov-modern 200 / workspace 引用app.css / dev.html 404 / 端点全200**。**QA 通过,行为不变,10模块视觉换新。** commit `479e8bb` 推送。

### 第7轮 · 后端按8限界上下文全面分层重构(架构师+业务专家主导)
- 4-agent 蓝图(3清点+架构师)。扁平 `controller/service` → 领域驱动:`platform.{iam,config,audit,alert,ai,portal}` + `domain.{datasource,metadata,integration,develop,orchestration,governance,mdm,project}` + `common`。
- 本轮范围(务实降风险):**只移 102 类(44 controller+58 service)**,model/mapper/dto/sync/config/util **保持原位**(→@MapperScan 不变、CdcSyncEngine 字符串FQN 不受影响)。
- 脚本化执行:校验全映射→移动+改package→全局FQN替换(44文件)→**跨包import注入(61条)**→测试随迁(12个,访问包私有的单测迁到服务新包)。编译器为网。
- **507 全量绿**(@SpringBootTest 上下文启动=Spring扫描/注入/@MapperScan 新包结构全正常)。clean打包。
- 部署:active/http302/**pid 936742**。E2E:**12 域端点全 200/302**(iam/config/datasource/metadata/develop/orchestration/governance/mdm/project/common/integration)。**QA 通过,行为零改,后端转领域驱动分层。零表变更。** commit `3b5db2b` 推送。

### 第8轮 · model/dto/exception/util 全面归域(≥50% 系统,业主红线"每轮≥半个系统")
- 接 R7,移 model(89)+model.dto(12)+dto(3)+exception(3)+util(5)=112 类归域。R/异常/实体被~全部文件 import → **223 文件 FQN 改写 + 74 注入 + 13 通配清理**。mapper 留原位(@MapperScan 不变)。
- **git 统计本轮 342 文件变更 = 627 系统的 55%,达 ≥50% 红线**。脚本+编译器网,507 全量绿。commit `2bb317f`。

### 第9轮 · mapper/sync/config/filter 全面归域(后端 100% 领域驱动)
- 移 mapper(87,镜像实体域,13目标包)+sync(68→domain.integration 前缀子包化)+config(8→platform.config)+filter(1→platform.audit)=164类。@MapperScan 改显式列 13 包(45/87 无@Mapper不可广义扫描);CdcSyncEngine 字符串FQN随前缀迁移。
- 161 FQN/前缀替换+51注入+9通配清理。**本轮 300 文件变更=48%系统**。507 全量绿(@SpringBootTest 验@MapperScan/CDC)。
- 部署 active/http302/**pid985733**。E2E:13端点全200(含 integration sync-job/sync-folder/cdc、config doris)。**QA通过,行为零改。后端 controller/service/model/dto/exception/util/mapper/sync/config/filter 全部领域驱动分层,仅 DataNoteApplication 留根。** commit `dc4a86b`。

### 第10轮 · 新增「数据消费层」域(填补断点④,业主"重构=改造+新增功能")
- 6-agent 工作流(产品/架构师/业务)深挖断点④。业务专家裁决 MVP(指标值引擎+查询+看板+导出+审计)+砍伪需求(API密钥/外部接入/订阅/计费/GraphQL)。
- 新增 `domain.consumption`:sql/66(dn_metric_value+dn_consumption_log 幂等已应用)+模型/Mapper(入@MapperScan)+`MetricValueService`(复用 HiveService 跑 calc_formula 取值,只消费 status=1,全审计)+`ConsumptionController` **11 端点**(calc/calc-all/value/history/values/freshness/zombies/overview/log-list/log-heat/export)。
- 511 全量绿(+4 引擎纯函数单测)。部署 active/http302/**pid996703**。
- E2E:建指标→calc=42→latest/history/overview/freshness/zombies/审计2条/导出CSV200→清理。**QA通过,断点④消费出口填补。**

### 第11轮 · 数据消费层前端看板 + 指标值定时计算
- 新 `js/gov-consumption.js`(治理子模块「数据消费」指标驾驶舱:概览/指标值表+计算+导出/一键全量计算/消费热度/僵尸指标,复用 DN.*+app.css)+ workspace.html GOV_MODS 加 consumption + 脚本标签(字节安全)。
- 后端 `MetricValueScheduleService` @Scheduled 每日01:30 计算全部启用指标落快照(复用 calc)。
- 511 全量绿。部署 active/http302/**pid1007412**。E2E:gov-consumption.js 200+模块注册+overview 200。**QA通过,消费层用户可见可用。** commit 见下。

### 第12轮 · 指标预警 — 阈值规则 + 指标值异常自动建治理工单(闭合 消费→治理)
- 新 `dn_metric_alert_rule`(sql/67,幂等已应用)+`DnMetricAlertRule`/mapper(在 consumption.mapper @MapperScan 内)+`MetricAlertService`(isBreach 7符 GT/LT/GE/LE/NE/OUT/IN+越界经 IssueService.create 建 METRIC 工单 objectRef=metric:{ruleId} 去重,兜底不抛)+`MetricValueService.calc` 成功取值后钩子 + ConsumptionController +3 端点(规则 CRUD)。
- 515 全量绿(+4 预警判定单测)。部署 active/http302/**pid1012676**。
- E2E:规则GT50→calc=100越界→**自动 METRIC/HIGH/OPEN 工单**→改GT200再算工单仍1条(去重)→清理。**QA通过,消费→治理闭环成立。** commit 见下。

### 第13轮 · 数据集/数据产品 — 精选SQL注册为受治理可复用查询
- 新 `dn_dataset`(sql/68幂等已应用)+`DnDataset`/mapper(consumption.mapper)+`DatasetService`(CRUD+query:validateReadOnly只读守卫→MaskingService脱敏改写fail-closed→HiveService执行→DATASET消费审计)+`DatasetController` 5端点+前端数据集卡(列表/运行抽屉展示/新建)。
- 518全量绿(+3只读校验单测)。部署 active/http302/**pid1055427**。
- E2E:注册SELECT7→query rows=[[7]]→写SQL(DELETE)被拒→DATASET审计1条→清理。**QA通过,数据产品不绕治理。** commit 见下。

### 第14轮 · 指标预警规则前端配置 UI(补全 R12 闭环可见)
- gov-consumption.js 指标行加「预警」→ 规则抽屉(列表+严重度药丸+删除 / 新增表单 op GT-LT-GE-LE-NE-OUT-IN+阈值+区间+严重度),复用 R12 端点+DN.drawer/pill。版本 q100r82→q100r83。零后端改动。
- 518 全量绿。部署 active/http302/**pid1063454**。E2E:js v83 200+workspace引用+alertRulesDrawer+规则 round-trip 通。**QA通过,预警闭环用户可配。** commit 见下。

### 第15轮 · 指标-资产影响联动 + 指标消费排行(跨域新功能)
- 后端(domain.consumption,零新表):MetricValueService.assetImpact(db,table)经 governance DnMetricRef 反查消费某表的指标+最新值; metricRanking()按 dn_consumption_log 计数 Top20+回填指标名。ConsumptionController +/asset-impact +/metric-ranking。consumption→governance 单向依赖无环。
- 前端 gov-consumption.js +「指标消费排行」卡+loadRanking(DN.bars)。版本 q100r83→q100r84。
- 518 全量绿+BUILD SUCCESS。部署 active/http302/**pid1071898**(80MB分块 md5 一致)。E2E:js v84=200+loadRanking+metric-ranking code0+asset-impact round-trip 命中(hit=True)清理0。**QA通过,资产↔消费跨域接通。** commit 见下。

### 第16轮 · 资产影响面进数据地图(R15 联动收口, 纯前端)
- workspace.html 表详情页加「消费指标」Tab(dmSwitchTab impact 分支 + dmLoadImpact())→ 调 R15 /api/consumption/asset-impact?db=&table= 列出消费此表的指标(名称/编码/关联类型/最新值/取值时间)+空态+错误态。零后端/零表/零新端点。
- 33 UI 测试绿(解析 workspace.html 的断言未破)。部署 md5 match/active/http302/**pid1113533**。E2E:workspace200+dmLoadImpact×2+impact Tab+asset-impact code0。**QA通过,影响面落到用户看资产处。**
- ⚠️**部署链路故障与修复**:本地↔38.76.183.50 SSH 降级,分块上传静默损坏(块 size 对内容错,连两次整传 md5 各异)。原 deploy_jar.py 仅 size 校验+recv_exit_status 无超时(socket 死时永久阻塞)。新 **deploy_jar3.py**:自建 socket 绕 getaddrinfo 抖动 + keepalive + exit_status_ready watchdog + **每块写后远端 md5 校验+不符重传(按 md5 续传)**。今后大 jar 部署用 deploy_jar3.py。

### 第17轮 · MDM 11→4 Controller 合并(结构重构, 行为不变) + 本地运行工作流
- 脚本化合并 11 controller→4(MdmCore/Golden/Reference/Governance):每方法映射改全路径、删类级基路径、注入字段并集去重、同名 handler 改名(steward.overview→stewardOverview; survivorship/hierarchy/xref 的 list/save/delete 加前缀)、修内部调用(tree→hierarchyList)。删11写4。
- 行为不变守门:合并前后各抽 51 端点 **diff=空**;编译通过;518 全量绿(Spring 上下文无重复映射冲突)。
- **⚠️工作流升级(业主建议)**:DB+依赖留远端(38.76.183.50:3307/9030 公网可达),**App 本地跑**(env: DB_HOST=38.76.183.50 DB_PORT=3307 root/<DB_PASSWORD> + DORIS 默认 + CRYPTO_KEY=<DATANOTE_CRYPTO_KEY> + DATANOTE_PASSWORD 空免登录),`java -jar target/datanote-1.0.0.jar`,18 秒启动,**免 30 分钟上传**。E2E 打 localhost:8099 + 与线上旧码 38.76.183.50:8099 逐字对照证行为不变。
- **QA通过**(本地 vs 线上逐字一致)。远端 jar 暂留 R16(测试环境,按需再同步)。commit 见下。

### 第18轮 · 指标输入质量联动(消费↔治理质量跨域新功能) + 本地运行
- 后端(domain.consumption 零新表):MetricValueService.inputQuality(metricId)经 R15 DnMetricRef 取来源表→dn_quality_rule(启用)→每规则最新 dn_quality_run 通过率→整体信号 HEALTHY/AT_RISK/NO_RESULT/NO_RULES。ConsumptionController +/metric/{id}/input-quality。consumption→governance 单向无环。
- 前端 gov-consumption.js 指标行+「输入质量」→inputQualityDrawer(信号药丸+来源表分组+规则通过率)。q100r84→q100r85。
- 518绿+编译通过。**本地运行验证**(App 本地连远端 MySQL):static js v85+inputQualityDrawer×2; round-trip 插指标+ref+质量规则+失败run→端点 code:0 signal=AT_RISK ruleFail=1 tables=1→清理0。**QA通过。**
- ⚠️运维:验证期 Doris FE+BE 双宕(历史重现,9030/8040 不监听 Communications link failure),就地救活 start_be(先 ulimit -n 655350)+start_fe --daemon,/api/metadata/databases 复返 4 库。

### 第19轮 · 全新数据资产驾驶舱首页 + 全局视觉升级(可见大重构, ultracode+Workflow)
- 业主反馈"改动看不见"→转向可见大重构。Workflow 7 代理(82 端点摸排/627K tok):并行 Understand→Design 蓝图→并行 Build 出 home-dashboard.js(21K)+redesign.css(12K)。
- 新首页 #homeCockpit: 品牌渐变 Hero + 8 KPI + 12 可视化卡(gauge/雷达/折线/环形/条形/热力/表),绑 14 真实端点,每卡独立 fetch+骨架/空/错误态。gov-modern 作用域 124 处加 #homeCockpit。workspace.html: viewHome→homeCockpit + loadDashboard 调 renderHomeDashboard + link/script。
- 缺陷闭环:redesign.css 头注释含 `--text-*/` 的 `*/` 提前闭合注释吞掉令牌块→Hero 渐变失效;修为 `* /`,注释平衡 37/37。教训:静态改必 bump 版本(忘 bump 致缓存旧文件);CSS 注释勿含 `*/` 字面。
- 518 全量 + 33 UI 绿。**本地运行 + Playwright 截图实证**:8 KPI+12 卡渲染真实数据(424表/7581字段/28.1健康/66.7%质量)+Hero 蓝渐变+无新错误。**QA通过,用户一眼可见。** commit 见下。

### 第20轮 · 全局视觉升级铺到治理/MDM 全模块(可见续)
- redesign.css 20 处 `#homeCockpit` → `:is(#homeCockpit,#govModuleContent,#mdmModuleContent)`(令牌块+.gov-stat/.gov-card 升级)。一处改动→治理11+MDM7 共18模块页同时焕新(色边玻璃卡/阴影/悬浮/大数字)。版本 q100r88。
- Playwright 实证治理总览页:KPI 色边玻璃卡+真实数据(28.1/424/7581/14)。518+33UI 绿。**QA通过,全模块视觉统一。** commit 见下。

### 第21轮 · 跨模块深链骨架 + 六大治理模块联动(消除孤岛大版本, ultracode+双Workflow)
- 业主新红线:每轮架构师深度全盘+消除孤岛+业务员评估+≥20轮自续+每轮大版本。
- Workflow1(7代理普查):126功能/39孤岛+实体关系图+5闭环链+R21..R44 24轮路线图(docs/blueprints/R21-R44-联动消除孤岛蓝图.md)。架构师结论:三大跳转入口不收ctx是孤岛根因。
- 基建(workspace.html):switchGovModule/govGoModule(+__govCtx)、switchMdmModule/mdmGoModule(+__mdmCtx)、navigateTo(+__navCtx);ROUTES init 消费ctx;数据地图表详情头加治理按钮组(血缘/质量/敏感/资产);影响面指标行「查看指标」→消费驾驶舱聚焦。
- Workflow2(6代理并行)gov-*.js接深链:quality(prefillRule/table过滤+查工单)、health(issueFilter/dim定位+查对象+去改进)、lineage(table自动画图+节点双击设中心)、classification(table聚焦+查表)、assets(table开抽屉+成本下钻)、consumption(focusMetric高亮+僵尸立即计算/编辑+newDataset预填)。6文件q100r89。
- 6文件node --check OK + 33UI绿。**Playwright实测深链全通**:navigateTo设__navCtx、switchGovModule设__govCtx、切到数据质量、gov-quality显示按X.Y过滤、govGoModule consumption切换、catalog openTable回跳。**QA通过,大批孤岛消除。** 注:workspace.html入口无版本号需Ctrl+F5。commit见下。

### 第22轮 · 数据质量闭环+资产中枢交叉联动(大版本)
- workspace.html: 数据探查字段表加「建质量规则」(dmBuildRule→质量prefillRule)。
- Workflow 3代理: gov-assets 资产详情「治理联动」5向按钮(质量/工单/血缘/敏感/消费); gov-consumption 输入质量来源表/规则行下钻质量; gov-quality 规则行+看来源表血缘/数据地图。q100r90。
- 3文件 node--check OK+33UI绿。Playwright: 资产详情治理联动按钮渲染+dmBuildRule就位。**QA通过。** commit 见下。

### 第23轮 · 指标三视图互通+历史趋势(大版本)
- workspace.html: 指标列表行加「驾驶舱」(metricGotoBoard→消费focusMetric); metrics init 消费 __navCtx.editId 自动开编辑框。
- gov-consumption: 驾驶舱行加「趋势」(metricTrendDrawer→/history DN.line)。q100r91。
- node--check OK+33UI绿。Playwright 建临时指标实测:列表「驾驶舱」+驾驶舱「趋势」+输入质量无回归,已清理。**QA通过。** commit 见下。

### 第24轮 · 系统监控/运维统计可跳处理(大版本)
- workspace.html: 集成统计行 syncTask:id→opsGotoSyncTask→dbsync openDetail; dbsync init 消费 openDetail; 系统监控调度块→运维/审计→治理审计/服务离线→数据源; settings init 消费 __navCtx.sm 切子Tab。
- 33UI绿。Playwright:opsGotoSyncTask+settings sm深链(datasource/monitor)+监控调度/审计/服务离线跳转全验证。**QA通过。** commit 见下。

### 第25轮 · 血缘下钻+纯展示图表消死胡同(大版本)
- Workflow 5代理: gov-lineage(tableActionMenu 图谱/质量/工单/目录,删drillOrphanToGraph死代码)、gov-overview(健康日历格→health date定位+短板去改进)、gov-audit(热力格/状态码前端过滤下钻+返回全部)、gov-standard(磁贴→Tab+ctx.tab深链)、gov-subject(修tone枚举+主题树→assets subjectId)。q100r92。
- 5文件node--check OK+33UI绿。Playwright遍历5模块全渲染无报错。**QA通过。** commit 见下。

### 第26轮 · MDM 闭环联动(大版本)
- Workflow 3代理: mdm-quality(不合规行→mdmGoModule goldenrecord修复)、mdm.js(goldenrecord 消费 ctx.editId/entityId 开编辑 + 建模属性 ENUM/REFERENCE buildRefPicker 引用码表)、mdm-refdata(在建模中引用+码值明细抽屉+ctx.category深链)。q100r93。
- ⚠️mdm-refdata 代理 socket 中断遗漏 refInModeling/detailDrawer 函数定义(只写了调用点),已手工补齐。3文件node--check OK+33UI绿。Playwright 遍历4 MDM模块全渲染无报错。**QA通过。** commit 见下。

### 第27轮 · 项目资产纳管双向+多项目对比下钻(大版本)
- workspace.html: 项目绑定资产行加「查看」(projAssetGoto 按类型跳 dbsync/质量/数据源/开发/地图); 多项目对比项目名→projCmpGoDetail(关弹窗开详情)。
- 33UI绿。Playwright: projAssetGoto/projCmpGoDetail 就位+项目视图渲染。**QA通过。** commit 见下。

### 第28轮 · 全局深链返回栈/面包屑+数据地图→数据集闭环(大版本)
- workspace.html: navigateTo 带ctx深链压__navStack/普通导航清栈; window.navBack弹栈返回; 左下浮动「返回来源」chip(栈非空显示)。表详情DDL的SELECT块加「存为数据集」→consumption newDataset。
- 33UI绿。Playwright: 深链压栈=1+chip「返回数据地图」+navBack回catalog+栈空隐藏; dmTableToDataset就位。**QA通过。** commit 见下。

### 第29轮 · Cron预览接通+安全态势下钻(大版本)
- workspace.html: dbsync 新建表单 dfsScheduleCron onblur→dfsCronPreview 调 /api/scheduler/cron-preview 显示下次执行。
- gov-security(最后未联动治理模块): 脱敏/行级策略行→敏感分级/数据地图; 概览→查看全部审计; ctx.table 聚焦。q100r94。
- node--check OK+33UI绿。Playwright: dfsCronPreview就位+gov-security渲染+审计链接。**QA通过, 11治理模块全接深链。** commit 见下。

### 第30轮 · 资产冷数据/无用表/采集历史下钻(大版本)
- gov-assets: 冷数据行+资产详情/建生命周期策略(gotoLifecycle); 无用表行+资产详情/查血缘/标记销毁; 采集历史条目→openCollectLogDetail抽屉+按库筛查。q100r95。
- node--check OK+33UI绿。Playwright gov-assets渲染无报错。**QA通过。** commit 见下。

### 第31轮 · 系统联动地图(深链campaign可见中枢capstone, 大版本)
- 新 js/gov-govmap.js: 治理子模块「联动地图」(GOV_MODS第12项)。5条彩色业务闭环链(每步可点直达)+6实体枢纽卡。workspace.html 注册 q100r95。
- node--check OK+33UI绿。Playwright 截图: 5闭环链+枢纽全可见, 彩色流程图可点。**QA通过, 用户一眼可见的联动中枢。** commit 见下。

### 第32轮 · 发布订阅自动触发+扇出去重(后端真闭环, 大版本)
- 新 MdmPublishService.fanOut(golden,changeType)+normalizeChangeTypes, 供 pubsub/publish 与 golden/publish 共用(去重)。MdmGovernanceController.publish 改委托; MdmGoldenController.publish 发布后自动 fanOut("update")。
- 编译+518全量绿。E2E: golden/1/publish→发布日志自动新增=1(扇出闭环)+清理。**QA通过, 半闭环→真闭环。** commit 见下。

### 第33轮 · DCMM成熟度自评汇入健康分(后端闭环, 大版本)
- HealthScoreService 加第6维「组织成熟度」: maturityScore()取DCMM最新自评(MaturityService.latest)score均值/未评给中性60; dimensionDetail+维度; dimensionWeights putIfAbsent(组织成熟度,10)确保参与加权。
- 编译+518全量+34健康单测绿。E2E: refresh后 dimensions 6维含组织成熟度=60, totalScore 28.1→31.0(参与加权上升)。**QA通过, DCMM孤立→汇入健康分。** commit 见下。

### 第34轮 · 伪需求/死代码清理(大版本)
- 删 workspace.html 死函数 dmSaveFieldMeta(无调用+仅弹提示不落库); 删 MetadataController 冗余端点 /all-tables + /favorite/check(前端零调用, service方法保留)。均经0调用方+0测试引用核实。
- 编译+518全量绿。curl: /all-tables 404 + /favorite/check 404(已删), /databases 200 + /table-detail 200(保留无回归)。**QA通过, API面精简。** commit 见下。

### 第35轮 · 主题域挂资产(完成R25悬挂, 后端+UI 大版本)
- sql/69_table_subject.sql(幂等已应用线上): dn_table_meta +subject_id。DnTableMeta +subjectId。MetadataController +/table/set-subject +/table/subject(注入DnTableMetaMapper, insert占位datasource_id=0)。workspace 表详情+「主题域」按钮 dmSetSubject(overlay 选主题域)。
- 编译+518全量+33UI绿。E2E: set=7→get7(insert)→update9→get9(update)→清理残留0。修insert NOT NULL datasource_id。**QA通过。运维:sql/69已应用线上。** commit 见下。

### 第36轮 · 资产详情显示/设置所属主题域(主题域读侧闭环, 大版本)
- gov-assets openAssetDetail 抽屉+renderSubjectRow: GET /table/subject+/api/gov/subject 显示所属主题域, 「设置」内联下拉→POST /table/set-subject。复用R35端点纯前端。q100r96。
- node--check OK+33UI绿。Playwright: 资产详情含「所属主题域」行+R22联动按钮无回归。**QA通过, 主题域读写双入口。** commit 见下。

### 第37轮 · MDM剩余模块联动(去重/管家/层级/存活→黄金记录, 大版本)
- Workflow 3代理: mdm.js(dedup 簇记录行→goldenrecord+消费ctx.entityId; steward 待办→goldenrecord/dedup)、mdm-hierarchy(树节点/明细→goldenrecord childRecordId/parentRecordId)、mdm-survivorship(页头→该实体goldenrecord)。q100r97。
- 3文件node--check OK+33UI绿。Playwright: MDM 四模块全渲染无报错。**QA通过, MDM全模块联动收尾。** commit 见下。

### 第38轮 · 项目活动审计下钻(大版本)
- workspace.html: projRenderActs 活动表+「前往」列, projActRouteOf 按 /api/* 前缀映射模块 navigateTo。
- 33UI绿。Playwright: projActRouteOf 映射正确(sync/quality/mdm/metadata/未知null)。**QA通过。** commit 见下。

### 第39轮 · 主题域→资产筛选端到端补全(服务端, 大版本)
- MetadataCenterController.searchTables +subjectId 参数(qw.eq subject_id); gov-assets loadAssets 带 ?subjectId= 调 /tables。/tables 直返 DnTableMeta 故 R35 的 subjectId 已随返回。
- 编译+518全量+33UI绿。E2E: set表subjectId=88→tables?subjectId=88命中(数量1), =99→0→清理。**主题域→资产链 R25+R35+R36+R39 端到端通。** commit 见下。

### 第40轮 · 全盘E2E验证capstone(消除孤岛campaign收官, 达成≥20轮)
- Playwright全盘sweep: 治理12/12+MDM12/12模块渲染无报错; 深链骨架(navigateTo/govGoModule/mdmGoModule/navBack)全在。
- API闭环sweep: metric-ranking/asset-impact 200, 健康6维含组织成熟度total31.0(R33), subjectId筛200(R39), set-subject/cron-preview 200, all-tables/favorite-check 404(R34), govmap/redesign静态200。518全量+33UI绿。
- **R21-R40 共20轮大版本完成: 39孤岛系统性接通+5业务闭环链UI可走通+3后端真闭环(pubsub/DCMM/主题域)+联动地图统览。达成业主≥20轮红线。** commit 见下。

### 第41轮 · 深度优化阶段启动·治理核心5模块(大范围, Workflow 优化→对抗审查)
- Workflow 10代理(5优化+5审查 pipeline)深优 gov-overview/quality/health/assets/consumption: 每fetch骨架/空/错/重试+判空守卫+校验+confirm+去抖+分页+性能去冗余+修隐藏bug(闭包var陷阱IIFE捕获/定时器/tone枚举warning→warn/防连点)。q100r99。
- 5 node--check过+**5审查全PASS无回归**(深链/端点/功能保留逐项核实)+33UI绿。Playwright: 5模块全渲染。**QA通过, 深优阶段开篇。** commit 见下。

### 第42轮 · 深度优化·治理剩余7模块(大范围, Workflow 优化→对抗审查)
- Workflow 14代理深优 gov-lineage/standard/classification/security/audit/subject/govmap(6类加固)。q100r100。
- 对抗审查发现并修复 gov-security 真回归(line30 buildSecurityOverview 无参自取+line64 loadAllOnce 再取=8请求+渲染竞态; 删line30, 统一 loadAllOnce 驱动真8→4)。其余6审查 PASS。
- 7 node--check+33UI绿。Playwright: 7模块全渲染+security概览单次渲染。**QA通过。** commit 见下。

### 第43轮 · 深度优化·MDM模块簇7文件(大范围, Workflow 优化→对抗审查)
- Workflow 14代理深优 mdm.js+approval/pubsub/quality/refdata/hierarchy/survivorship(6类加固: safeParse/缓存getEntities+_recCache/竞态token/删前子级检查/成环断环seen+MAX_DEPTH/防重复提交)。q100r101。
- 7 node--check+**7审查全PASS无回归**(R26/R37深链/api端点/copyable导出/缓存/tone逐项核实)+33UI绿。Playwright: MDM 12/12渲染。**QA通过, MDM全簇深优。** commit 见下。

### 第44轮 · 深度优化·数据地图dm*+首页驾驶舱(大范围)
- Workflow 4代理: home-dashboard(12widget空态/重试+KPI单源降级+/gov/overview去重单次缓存+rAF分批)+workspace.html dm*区(每tab加载/空/失败+判空+doDatamapSearch 250ms去抖)。home-dashboard q100r102。
- 双审查全PASS。**workspace.html NUL字节4→4保留+63hunk全在7053-7753 dm区+154端点零改+深链全保**(审查+自验双确认)。home node--check+33UI绿。Playwright: 首页8KPI+12卡+数据地图加载。**QA通过。** commit 见下。

### 第45轮 · 深度优化·后端服务6个(大范围, 转后端)
- Workflow 12代理深优 MetricValue/MetricAlert/Dataset/MdmPublish/HealthScore/Classification Service: 校验BusinessException/异常log不静默/空安全修NPE/@Transactional仅写方法(calc/save/fanOut)/N+1改selectBatchIds内存映射/fanOut上限1000/边界(metricCode null→#id)。
- 6审查全PASS(部分实跑mvn compile+单测)+编译+**518全量绿**+运行时冒烟7端点200(含health 6维)。**QA通过, 后端深优开篇。** commit 见下。

### 第46轮 · 深度优化·后端服务第二批6个(大范围)
- Workflow 12代理深优 DataMap/DatasourceExplore/Issue/Standard/Lifecycle/SyncJob Service(校验/异常log不静默/空安全修NPE/写方法@Transactional/去重/标识符校验)。IssueService 工单去重/状态流转/自动关单语义零改动(审查核实)。
- 6审查全PASS+编译+518全量绿+冒烟6端点200。**QA通过, 后端深优第二批。** commit 见下。

### 第47轮 · 深度优化·后端安全/调度服务6个(大范围)
- Workflow 12代理深优 Rbac/Masking/ScheduleLifecycle/TaskScheduler/TaskExecution/Maturity。
- ★安全增强: MaskingService resolveRowFilters catch 由 fail-OPEN→fail-CLOSED(异常拒绝执行, 3调用方承接); 列脱敏N+1→selectBatchIds。RbacService 防全表误操作id校验。
- ★审查捕获修复真回归: ScheduleLifecycle.onlineLocal 误加@Transactional致refreshAllDependencies(跨bean REQUIRED)并入外层事务, 循环依赖连带回滚ONLINE; 已移除@Transactional恢复原状态机语义(5单测mock不抛是假绿)。
- 6审查5PASS+1修复。编译+518全量绿+冒烟6端点200。**QA通过。** commit 见下。

## 2026-06-08 远端安装向量库(Qdrant)+图数据库(Neo4j)
- 服务器 38.76.183.50(Docker)。资源核实: 磁盘30G可用/内存~5.8G可用/16CPU。
- Qdrant: 镜像 qdrant/qdrant:v1.12.4; `docker run -d --name datanote-qdrant --restart unless-stopped -m 1g -p 6333:6333 -v qdrant_storage:/qdrant/storage -e QDRANT__SERVICE__API_KEY=<密钥经env>`。
- Neo4j: 镜像 neo4j:5.26-community; `docker run -d --name datanote-neo4j --restart unless-stopped -m 1200m -p 7474:7474 -p 7687:7687 -e NEO4J_AUTH=neo4j/<密码经env> -e NEO4J_server_memory_heap_max__size=512m -e NEO4J_server_memory_pagecache_size=256m -v /opt/neo4j/data:/data -v /opt/neo4j/logs:/logs`。
- 绑 0.0.0.0(本地 app 连远端需公网可达, 同 MySQL3307 测试环境姿态), 密钥/密码经 env 注入不入提交文件。
- 验活: Qdrant /collections ok; Neo4j /db/neo4j/tx/commit RETURN 1 ok。
- 集成方式: 纯 REST/HTTP(HttpURLConnection), pom 零新依赖。血缘镜像 MySQL→Neo4j(14节点/7边)。embedding(百炼)未配→向量降级关键字。

- 2026-06-10 R126: 远端库应用 sql/74(dn_mdm_attribute加element_code + 建dn_mdm_golden_history);本地重启 r126;E2E通过(属性绑定数据元回读/黄金记录历史链);遗留 publish fanOut 同步扇出>10s 待异步化。
- 2026-06-10 R127: 黄金记录publish扇出异步化(>10s→4s);color:#hex令牌清扫21处/10文件(js全bump r127);本地重启,E2E+浏览器冒烟0报错。
- 2026-06-10 环境抢修: 数据地图表详情报错根因=Doris FE 于6月9日18:57被系统OOM killer杀死(java RSS 8.2G,机器15G无swap;BE存活,9030无监听)。SSH(paramiko)拉起 start_fe.sh --daemon,回放元数据后恢复,/api/metadata/table-detail 验证返回80列正常。风险: 无swap易复发,建议加swap或限FE堆。
- 2026-06-10 U1: UI重构工程启动,设计系统v3「墨青」定稿(设计规范.md重写+css/dn-design.css新建+DN.confirm),全站换装地基波完成,本地重启截图验证0报错。后续W2-W5波次清理标记。
- 2026-06-10 U2a: workspace.html 硬编码色机械清扫416处→v3令牌,重启验证0报错。
- 2026-06-10 U2b/U3: CSS再清267处硬编码色;全部模块JS window.confirm(23处)→DN.confirm统一弹窗,E2E验证0报错。
- 2026-06-10 W2: workspace.html深清(z-index 16处令牌化+hex 166→29+alert/confirm全converted DN组件),修Monaco主题误伤回归,11路由0报错。
- 2026-06-10 W4a: 21个模块js安全色扫(fallback移除+CSS语境hex映射,避SVG属性),js全bump u3,12路由0报错。
- 2026-06-10 W4b: AI整页dn-ai-*组件化+10文件SVG图表迁令牌,js目录hex清零,bump u4,修aiAgentRoot内联布局回归,0报错。
- 2026-06-10 W5: app.css令牌块v3同步+自引用修复,用法层hex全清(app.css/workspace),实测Chrome148 SVG属性支持var(),健康分AI钮降噪,bump u5,0报错。
- 2026-06-10 W6: 全页视觉巡检通过,运维主卡%/按钮emoji清理/弹窗chrome收口,css u6,0报错。
- 2026-06-10 W7: 业主令都做——no-cache头/3裸弹窗迁dn-modal/iw-form全站86处清零/emoji清零(DN.icon+pin,flag,bot)/diff与kv组件化,bump u7,全验证0报错。
- 2026-06-10 W8: 修业主报的搜索框焦点态(焦点系统重构:控件走边框+光环,外框只留按钮链接)+深巡修ops-layout收缩/gov-pill全局化/彩虹链接收敛/projTile双style属性bug,css u9,0报错。
- 2026-06-10 W9: IA v3信息架构重构——首页12卡→4卡,治理11→9,MDM13→10,运维7→5,砍审计热力/DCMM/矩形树等炫技报表,hidden标记摘菜单深链全保,docs/02-信息架构v3.md立契约,bump u10,全验证0报错。
- 2026-06-10 W10: 指标管理新增指标取值tab(复用consumption渲染器+作用域扩展177处),dbsync图标统一,套娃var清18处,bump u11,0报错。
- 2026-06-10 W11: 布局深巡二轮——质量概览tab摘除(直进规则)/工作台三重复信息砍横幅+磁贴统一/系统监控双style bug第三例修复(离线行破格),bump u12,0报错。
- 2026-06-10 W12: 模块页双段重复说明清族(12处定位句删除,10 builder全量判定21文件),MDM总览前往磁贴数字化,bump u13,0报错。
- 2026-06-10 W13: 资产目录主次重排(清单上移+采集历史收纳+error磁贴语义化)/血缘页三卡并一(选择器7→3,E2E走通)/gov-toolbar下拉宽度治本,bump u15,0报错。
- 2026-06-10 W14: 数据标准三重指标砍并(磁贴6个+概览卡删)/分类分级空壳卡合并/成员权限摘菜单(项目5→4),bump u16,0报错。
- 2026-06-10 W15: RBAC双处维护收敛(数据安全撤用户角色卡-200行)/操作审计三处收敛(安全设置砍检索表留入口)/修自动调用已删DOM回归,bump u18,0报错。
- 2026-06-10 W16: 结构探针无死角扫描38页面态——dn-form-select宽度策略重构(默认auto表单内100%,修5页全宽下拉)+管家橙链收敛,bump u19,0报错。
- 2026-06-10 W17: 深层魔法值清零(字号264处/圆角/纯黑阴影30处→靛染阶梯),隐蔽UI四件实测(Ctrl+K/右键菜单/行菜单/大盘弹窗)全达标,bump u21,0报错。
- 2026-06-10 W18: 修打印页var失效真bug(独立文档内嵌令牌)/transition 105处令牌化(节奏统一)/死CSS检测存档,bump u22,0报错。
