# 操作日志

## 2026-06-17 续 M-D 完整能力覆盖 + M-A 数据分析图表(分支 feature/md-capability, 从 E 分支切出建在E闸门上)
- 业主令"一起做"剩余特性。**M-D 完整能力覆盖**: 4 个 D 波次并行写面调查→逐波薄适配器工具(自动继承E的PermGate+审批, 无需接闸门)。W-D1 运维(run_sync_job/stop/run_backfill/schedule_rerun/baseline_status) · W-D2 元数据(trigger_metadata_crawl/create_subject/update_column_business_meta, 凭据类剔除撞红线) · W-D3 治理(scan_table_sensitive/classify_column/transition_issue/assign_issue/run_standard_check/rebuild_lineage[HIGH]) · W-D4 数据模型(datamodel_save_model/submit_for_approval/approve_model[HIGH], reviewer服务端捕获) · W-D5 项目(add_member/milestone/update_task/wiki/submit_release[HIGH]/approve_release[HIGH]继承成员ACL)。**MDM缓**(写逻辑全内联MdmCoreController无可调service, 需重构才能安全工具化)。每波spec+码质双审。
- **M-A 数据分析+图表**: AnalysisQueryService 只读SELECT安全栈(单语句/SELECT-only/拒OUTFILE+可执行注释/**Druid解析真实库名逐表数据ACL**[对抗审查抓出并修了跨库+逗号连接ACL绕过]/脱敏SqlMaskRewriter fail-closed/限行2000限时30s)+RunAnalysisTool(只读catalog:view)+ChartTool(纯渲染)+前端chartCard(DN.bars/line/donut/radar)。修previews补收_chart。
- **工具 44→69**(+25)。编译649测试/2预存UI失败(分支未改workspace.html)无新增。活体E2E: run_analysis真跑GROUP BY 14行 / chart _chart进previews。对抗审查(opus)裁定run_analysis只读不可绕过、ACL跨库缺口已修。
- **未合并**: 分支 feature/md-capability(栈在 E 的 PR#1 上)。下一步: 推送建PR(base=feature/me-perm-align) + 剩余 B(PDF-RAG)/C(SSE)。

> 过程文档曾于 d8a29a4 全量清理, 本文件自 2026-06-12 重建, 只记关键决策与动作。

## 2026-06-12 全站迭代升级工程收官(11路评审101发现 → PM 25项全清)
- 批1: 开发模块5项(运行快捷键/删除门禁/版本去重/重命名落库/未保存守卫) + 数据目录4项(表摘要修根/主题域死端点/系统库黑名单/裸ALTER收敛) + 数据同步2项(调度断头接通/CDC死信合并) + AI助手会话状态机四合一 + MDM 审批门禁。
- 批2: 补数链路全收敛统一调度(删旧 BackfillService 全家6文件, 开发右栏/运维面板改走 /api/scheduler/backfill[-runs], 运维历史按批次分组); 质量口径修根(FAIL→failed)+规则弹窗补五字段(维度/阈值/cron/强阻断/启停)+治理页编辑启停; 质量分三处同数(computeScore); 资产清单分页; 血缘事件自动重建(SyncJobChangedEvent+@Async+02:40兜底); SysAlertScheduler 系统告警巡检(5min, heap/disk/db danger→通知中心, 1h去重); 首页同步卡改今日口径。
- 批3: 通知埋点四扩展(SCHED_FAILED 调度终败/QUALITY_ISSUE 建单+指派/MDM_REVIEW 审批终态/SYNC_FAILED 同步终败[项目owner→createdBy→admin回退链])+铃铛深链补 operations/mdm/dbsync/settings 路由; 数据预览脱敏接入(fail-closed, MASK/HASH/REPLACE/RANGE 四函数与 SQL 改写语义对齐, profile 无值泄露不接)。
- 附带修复: 存量规则旧词 ruleType 编辑回填静默置空 bug(qrNormalizeType 归一+severity 白名单), 规则9数据修正。
- 版本 ?v=u33, 本地构建+E2E 全绿(console 0 错误), 未部署服务器(遵 2026-06-08 停部署令), git 未提交(待业主指令)。
- 下一批候补(终审落选项): 基线管理做实 / 健康分隐形维度还原 / 审计专项。

## 2026-06-12 批4: 候补三项收官
- 基线做实: 新增 BaselineCheckService(每 5 分钟巡检, 启用基线的关联任务须在承诺时间前完成当日调度, met/broken/pending/empty 四态), 打破→通知中心 BASELINE_BROKEN(同基线同日去重); 新端点 /api/baseline/status-today; 基线面板加"今日达成"列(打破悬停可见未达任务)。
- 健康分还原真五维: 删除 DCMM 成熟度汇入(主观问卷不入客观分, Domain 方案 b), 健康分输出加 scoreVersion="v2-五维" 口径标注(总分跳变可解释); DCMM 自评经查前端零调用属纯孤岛, 整块下架(Service/模型/Mapper/三端点), 表数据未动。
- 审计专项: 敏感只读操作(导出/下载/数据预览/探查)纳入审计留痕, 审计日志导出自身不再豁免; 新增 DATA_PREVIEW 动作类型; 新端点 /api/gov/audit/types, 治理审计中心类型下拉动态化(自动纳入 AI_AGENT_WRITE 等新类型)。
- 版本 ?v=u34, 构建+单测+E2E 全绿, git 未提交。

## 2026-06-12 批5: 全站运行时交互 bug 系统排查(业主报告"系统很多功能有问题")
业主报告三个具体现象(删除弹窗不弹→卡死 / 指标页无法下滑 / 顶部菜单溢出藏掉 AI助手),指出"系统应该有很多这样的问题"。主线先真机定位三个根因,再用 Workflow 五维静态审查 + 两个补审 agent 找出全部同类问题,全修并真机验证。

**业主报告的三个根因(铁案,真机实测)**:
1. 删除/确认弹窗卡死(双层根因): (a) #msgBox 内层 class 含 dn-modal,而 .dn-modal{opacity:0} 仅靠 .dn-modal-mask.show 恢复,父级是 .intg-modal-overlay 不匹配 → 弹窗透明; (b) .intg-modal-overlay z-index 仅 200,远低于抽屉(9000)/弹窗(10000),在抽屉上触发 msgConfirm 被埋在下方。两层都导致"遮罩盖屏但按钮点不到 → Promise 永挂 → 卡死"。修: 删 dn-modal class + z-index 提到 var(--z-spotlight) 100000。
2. 指标/项目页无法下滑: app.css 第1971行有孤立 CSS 声明 `font-size:13px;}`(缺选择器),破坏紧随的 .view-quality 规则解析,flex:1 失效 → 内容高度不受限,ops-main overflow:auto 形同虚设。修: 删孤立声明(CSS 解析恢复,view-quality/viewMetrics/viewProject 滚动全恢复)。
3. 顶部 AI助手被裁: header-tabs 11 个 tab 溢出(1227>1086),overflow:auto 把最右"AI助手"裁掉不可见。修: 压缩 tab padding 20→11、gap、logo/header-right 间距,标准宽度放下全部 11 tab。

**Workflow 五维审查(CSS健康/弹窗/滚动/布局溢出/交互死链)+ 补审,共修**:
- 弹窗类: z-index 提升(critical)、msg 单例 _msgBoxResolve 覆写致旧 Promise 永挂(加入口收尾)、sched-log-overlay querySelector 误删别的 overlay(改 btn.closest 关自身)、removeChild→remove()、quality-modal-overlay 显隐范式统一(display:none+.show, z 提到 --z-modal)。
- 布局溢出(同 header-tabs 根因): datamap-detail-actions(9按钮)flex-wrap、datamap-detail-tabs/dbsync-drawer-tabs overflow-x 横滚、sched-stats/log-toolbar wrap。
- 交互死链(7 处裸 fetch/逐请求无 catch 致"点了卡住/无反馈"): startBackfill 逐日提交改 settle 模式(任一失败也汇总)、syncModalConfirm 下拉永卡"加载中"补 catch 复位、submitCreateBaseline/doBackfill/schedSaveConfig/saveEnvConfig/saveDatasourceConfig 补 catch。
- onclick 死链: 281 函数全有定义,0 死链。CSS 健康: app.css 3890行+dn-design.css 括号平衡,无其它孤立声明。
- 保留(已知低危): ai-agent.js dn-ai-mask append #aiAgentRoot(潜在 transform 包含块,当前不触发)、ops-main 隐式滚动链(agent 警示改前须全回归)。
- 版本 ?v=u36, 构建+真机全面验证全绿(弹窗 z=100000 可见、指标注入超高内容真能滚、AI助手可见、单例保护、console 0 错误), git 未提交。

## 2026-06-12 批6: 抽屉系统全局治理(业主报告"抽屉跳转后样式坏/抽屉里弹窗有问题/多入口内容重复")
全站三套抽屉实现: DN.drawer(.gov-drawer 动态单例, gov/mdm/ai 共15模块用)、静态 dbsync-drawer(项目详情 #projDetailDrawer / 同步详情 #dbsyncDetailDrawer, .show 切换)、AI 抽屉(.dn-ai-drawer)。真机逐项定位根因并修复(?v=u39)。
1. **跳转后样式坏 = 抽屉残留(铁证)**: navigateTo(workspace.html:7020) 切视图时只关项目详情抽屉(projCloseDetail), 不关 DN.drawer/dbsync/AI 抽屉 → 从这些抽屉点跳转链接后, 遮罩(z=9000)+抽屉残留盖住新视图, 且 DN._drawerKey 键盘监听泄漏。修: dn-common.js 新增 DN.closeAllDrawers()(清三套抽屉 DOM + 解绑监听 + 调各自 close), navigateTo 入口统一调用。真机验证: 跳转后 mask/drawer/监听全部清除。
2. **抽屉里弹窗有问题 = g-modal 被抽屉埋(铁证)**: showModal/msgBox/confirmBox/promptBox 共用的 .g-modal-overlay z-index 仅 2250 < 抽屉 9001, 抽屉内调用时弹窗被埋在抽屉下点不到 → 卡死。修: app.css .g-modal-overlay z-index → var(--z-spotlight) 100000(与 #msgBox 同级)。真机验证 g-modal z=100000 > 9001。(注: DN.confirm 10000、msgConfirm 100000 本就在抽屉上, 仅 g-modal 漏了)
3. **多入口内容重复 = 详情切换残留**: dmOpenTable(数据地图表详情) 切换表时只清了 detailBody 加载态, 标题/副标题(dmDetailTitle/dmDetailSub)未即时清空 → 远程库 information_schema 取数慢时(数秒), 标题栏残留上一张表名/行数 → 看似"内容没变"。修: 入口立即把标题设为新表名、副标题设"加载中"。真机验证: 切表标题立即更新+加载态。(对比: dbsyncOpenDetail 本就立即清头部+所有 pane 加载态, 是标准范式; dmOpenTable 漏标题已补齐)
- 完整 z 层级体系核对: 抽屉 9000/9001 < DN.confirm·quality-modal 10000 < msgBox·g-modal 100000 < toast 100010, 所有弹窗现都在抽屉上层。
- DN.drawer 30 个调用全部正确传对象标识到标题(db.table/bizKey/id), 内容按对象区分, 无"忽略参数"bug。
- 架构观察(专业视角, 本次未动): 三套抽屉实现风格/机制不统一(建议长期统一到 DN.drawer); DN.drawer 是单例(抽屉内再开 DN.drawer 会替换当前抽屉, 抽屉内二次确认应用 DN.confirm, 已在抽屉上层)。
- 版本 ?v=u39, 构建+真机验证全绿, console 0 错误, git 未提交。Workflow 抽屉调研因 API 临时故障失败, 改主线真机+grep 自行完成。

## 2026-06-12 批7: 三套抽屉统一到 DN.drawer(业主指令)
全站三套抽屉实现:DN.drawer(.gov-drawer 动态工厂)、静态 dbsync-drawer(项目/同步详情, 复杂多 tab)、AI 抽屉(.dn-ai-drawer)。专业判断:复杂 8/7-tab 详情抽屉重写为 DN.drawer 工厂风险过高且违反极简原则, 正确的统一是收敛三套到一套视觉/层级/动画/关闭标准(以 DN.drawer 为基准)。
- **AI 抽屉 → 真正改用 DN.drawer 工厂**(ai-agent.js openDrawer): 经验/定时/审批内容是简单列表, 改 DN.drawer(title, body) 后获得统一的 .gov-drawer 外壳/滑入滑出动画/Esc·遮罩·×关闭/焦点陷阱 a11y/被 closeAllDrawers 管理。真机验证: 点经验按钮产生 .gov-drawer(z9001) 而非旧 #aiDrawer, 标题/内容正确, Esc 关闭生效。
- **dbsync-drawer 层级+背景统一**(app.css): mask z 2200→var(--z-drawer)9000、drawer z 2210→calc(+1)9001(原硬编码远低于 DN.drawer, 同屏会被压); 背景 bg-body→bg-card。真机验证项目/同步详情 z=9001/9000、bg=白。
- **项目详情抽屉补 Esc 关闭**(project.js): 原 Esc 只关弹窗不关详情抽屉, 补齐后三套抽屉 Esc 行为完全一致(同步详情本就有 Esc)。真机验证 Esc 关闭生效。
- 结果: 全站抽屉视觉/层级/动画/关闭行为完全统一(一套设计语言)。实现上剩两套(DN.drawer 简单内容 + dbsync-drawer 复杂 tab), 后者保留静态 DOM 便于多 tab 状态管理, 是合理的内容适配而非重复。
- 版本 ?v=u40, 构建+真机验证全绿(三套抽屉 z9000/9001 统一/AI 用工厂/项目详情 Esc/console 0 错误)。
- 本次按业主指令提交全部累积工作到 github(caowen remote)。tp/(14M 参考 PDF)加入 .gitignore 不入库。

## 2026-06-12 批8: 登录界面 + 用户权限管理大工程(第1+2轮)
业主令: 做登录界面+彻底完善用户权限(模块+功能+数据级粒度, 做完强制登录)。调研发现后端 RBAC 数据层早已完整(platform/iam + sql/36 预置 admin/admin123), 缺登录页/权限点体系/接口鉴权/前端权限控制四块。
**第1轮(?v=u41)**: PermCatalog.java 权限点清单(11模块×view+功能) + GET /api/rbac/perms/catalog; login.html 登录页(墨青品牌区+表单, 已登录自动进); workspace 前端守卫(dnBootGate 未登录跳 login/401跳login/dnHasPerm/dnApplyMenuPerms 菜单按权限隐藏/navigateTo 路由守卫/顶部用户名+登出); sql/79 标准角色(DATA_DEV/DATA_GOV/PROJECT_MGR/VIEWER)已应用库。
**第2轮(?v=u42)**: PermInterceptor 接口级鉴权拦截器(写操作按 URL→权限点有序映射[审批/运行类特例优先], 敏感 GET 特例[审计导出/RBAC列表], 普通 GET 只要求登录防跨模块误伤, 权限集30s缓存, 403返JSON带所需权限点) + WebMvcConfig 注册; requiredPerm 纯函数单测; 前端 403 toast; 用户管理权限配置从手输 textarea 改为 11 模块分组树形勾选(组头全选/全部权限*); 新增 governance:manage 权限点。
E2E 真机: viewer/viewer123(VIEWER角色)登录→写操作 403 带"需要 xxx"提示/普通读 200/菜单只显 10 个(系统管理隐藏); admin 权限树 11组33点正确勾选。**启动新增环境变量 DATANOTE_PASSWORD(设了即强制登录, 默认账号 admin/admin123)**。
待续: 数据级权限(按 owner 行级过滤, 涉及各模块 list 接口, 渐进); 按钮级前端隐藏; 用户自助改密。
**第3轮(?v=u43, 三轮收官)**: 数据级权限落地项目维度(无 project:all-data 的用户项目列表只见自己负责/创建/参与的, 直开他人项目详情被拒; 新权限点 project:all-data 给 PROJECT_MGR); 自助改密(POST /api/auth/change-password 校验原密码, 顶部用户框→账号抽屉[改密+登出]); 按钮级隐藏机制(dnApplyBtnPerms 扫 [data-perm], 6 个主"新建"按钮挂权限, viewer 全隐/admin 全显); 顺手修回 IA v3 丢失的质量"新建规则"按钮入口。E2E 全绿(admin 3 项目/viewer 0/详情拒/改密往返/按钮显隐/console 0 错误)。

**收尾轮(?v=u44)**: git 欠账还清(commit 1b625e9 共114文件 + 622b85e, push github caowen/master 双向验证); 动态行按钮挂权限(质量/指标/基线行内 编辑/删除/启停 按 data-perm 显隐, 只读按钮保留), viewer 视角 E2E 验证; 改密链路复测全通(此前一次失败判定为远程库网络抖动孤立事件)。

## 2026-06-12 批9: 多用户身份贯穿(权限工程第4轮, ?v=u45)
权限体系建好后补身份贯穿洞: 核心资产(脚本/同步任务/质量规则)新建完全不记创建人、基线/收藏/评论硬编码 default —— 四类失败通知(SCHED_FAILED/SYNC_FAILED/QUALITY_ISSUE/BASELINE_BROKEN)的接收人路由全打空。
- 新增 CurrentUserUtil 统一取当前登录用户; 7 个写入点接真实用户(脚本/同步任务/质量规则/基线 createdBy, 指标 owner 默认创建者, 收藏/评论作者)。
- 新增 GET /api/rbac/usernames 轻量选人端点(仅 username/nickname, 登录即可); 修复权限工程引入的回归: 项目添加成员"从用户列表选"调 /users 需 settings:user 致普通负责人 403, 改调 usernames; 单测防误拦。
- E2E: viewer 调 usernames 200(无敏感字段)/完整 users 仍 403/admin 建规则 createdBy=admin 真实落库。commit 65b55c4 已 push github 验证。

## 2026-06-12 批10: 身份贯穿 UI 收口(权限工程第5轮, ?v=u46)
- 全局用户 datalist(登录后由 /api/rbac/usernames 填充): 任何"负责人/用户名"输入框挂 list="dnUserList" 即可下拉选真实用户。已挂 5 处: 指标负责人 + 治理工单 指派/批量指派/转任务/新建工单。
- 质量规则表与基线表新增「创建人」列(资产归属一眼可见, 存量为 anonymous 属真实历史)。
- E2E 全绿, commit 58b3be0 已 push github 验证。

## 2026-06-12 批11: 用户权限深挖·安全加固轮(ultracode, ?v=u49)
业主令"继续深挖 用户权限管理"(/effort ultracode 开)。用 Workflow 4 维并行审计(security/audit/usermgmt/authz)得 24 缺口, 实现全部 critical+high(commit 5ea74de 后端 + e7d077e 前端浮现, 均 push caowen 双向验证远程=本地)。
- **防暴力破解**: 新建 LoginAttemptService(内存滑窗 5 次失败/10min→锁 15min)。login 前置锁检查(423+剩余秒)/成功失败均审计(LOGIN/LOGIN_FAIL 带剩余次数/LOGIN_LOCKED)。
- **会话固定防护**: 登录成功先 old session.invalidate() 再建新会话。
- **口令强度策略**: 新建 PasswordPolicy(≥8 位/≤64/弱口令黑名单/字母数字符号≥2 类), createUser/updateUser/changePassword 统一校验。
- **内存 admin 兜底收口**(防同名无角色账号白拿超管 *): 3 处 '*' 兜底仅 `isEmpty && !existsInDb` 才给; 停用 DB 用户走 DisabledException(AccountStatusException)语义, ProviderManager 终止不回退内存 admin。
- **用户管理护栏**: 不可停用/删除自己、不可停用/删除最后一名启用超管; assignRoles 越权自提升防护(非超管不得授予自己不具备的权限点→403); 改密/删用户/分配角色/设角色权限全 PERM_CHANGE 审计。
- **会话硬化**(application.yml): session timeout 8h + cookie http-only + same-site lax(CSRF 兜底)。审计去重: shouldAudit 跳过 /api/auth/login(AuthController 自审计)。
- **有效权限浮现**: 新增 GET /api/rbac/users/{id}/perms(跨角色去重); listRoles 加 userCount; dn_user 加 last_login_at(sql/80 已应用库)。前端: 角色表「使用」列 userCount 徽标 + 删角色提示绑定数; 用户行「权限」入口按 PermCatalog 模块分组只读展示有效权限; 重置密码提示补口令要求; 用户列表加最后登录列。
- 单测: LoginAttemptServiceTest(4)+PasswordPolicyTest(5)+PermInterceptorRuleTest(5)+AuditPureFnTest(21) 全绿。E2E 真机: 5 次失败锁定/弱口令拒/停用自己拒/role userCount 徽标/viewer 有效权限抽屉 10 view 点分组渲染/AI 配置写需 settings:config/自提升 403 全验证。
- 候补 medium/low: 各模块 list 行级 owner 过滤(仅 project 已做, effort L)/用户分页 N+1/批量操作/首登强制改密/CSRF token/perms-catalog 端点收紧。

## 2026-06-12 批12: 首登强制改密(用户权限深挖续, ?v=u50)
管理员建号/重置密码后用户首次登录强制改掉初始密码(管理员长期知晓他人密码是安全洞)。
- 后端: dn_user 加 must_change_pwd 列(sql/81 幂等, information_schema 判断兼容 MySQL 8, 已应用库); DnUser 加字段; RbacService.createUser/updateUser(传密码时)置 1、changePassword 清 0、新增 mustChangePwd(username) 查询; AuthController.status 暴露 mustChangePwd(内存兜底账号 false)。
- 前端: dnBootGate 读标志→渲染后弹不可关闭全屏遮罩 dnForceChangePwd(z99999, 唯一逃生口=登出, 改密成功 reload); 顺手对齐账号抽屉改密提示 ≥6位→≥8位(与 PasswordPolicy 后端口径一致)。
- E2E(curl 全链路 + Playwright 真机): 建号 flag=1 / login status mustChangePwd:true / 遮罩四要素渲染 / UI 填表改密清 flag / reload 不再弹 / 新密码登录通过; 既有 admin·viewer flag=0 不受扰; 测试号 tmpuser 已清。commit bb97996 push caowen 双向验证(远程=本地)。

## 2026-06-12 批13: 批量用户操作 + 权限点清单端点收紧(用户权限深挖续, ?v=u51)
- 批量用户操作: 用户表加复选框列(全选/行选/select-all 联动) + 批量工具栏(已选 N + 批量启用/停用/追加角色/删除/清除选择)。后端 3 端点 POST /users/batch-status|batch-delete|batch-roles —— 逐个复用单操作护栏(跳过自己/最后超管, 返回 success+skipped 明细); batch-roles 追加式(现有角色 ∪ 新角色, 不覆盖) + 越权自提升防护; 经 WRITE_RULES rbac 前缀自动鉴权 settings:user。
- 安全搭车: GET /api/rbac/perms/catalog 收紧到 settings:user(原仅要求登录, 泄露全站权限 taxonomy; 仅 umPermModal/umViewUserPerms 用, 均 admin 可达), 单测断言同步更新。
- E2E(curl + Playwright 真机): batch-status 停用含 self→skip / batch-roles 追加 VIEWER→2 用户得 10 view 点 / batch-delete success 3 / viewer 调 perms/catalog 与 batch-status 均 403 / admin perms/catalog 200 / 前端复选框+全选联动+追加角色 modal 全链路。测试号已清(仅 admin+viewer)。commit 6924136 push caowen 双向验证(远程=本地)。

## 2026-06-12 批14: 停用/删除用户存量会话立即失效(用户权限深挖续, 纯后端)
自查发现真洞: 鉴权只查角色从不看 status —— 停用/删除用户后其已登录 session 完全不受影响(写权限照旧、普通 GET 仍可读全站), 离职/盗号紧急停用形同虚设。
- PermInterceptor 缓存条目加 active(表内用户须 status=1; 表外仅内存兜底引导账号有效), 已认证请求先做账号有效性检查(含普通 GET): 不活→invalidate session + 401 "账号已停用或删除"(前端既有 401 跳 login, 零前端改动); 查询异常 fail-open 读不踢会话。
- 加 evictAll(), RbacController 在 停用/删除/分配角色/设角色权限/删角色/三个批量端点 后主动清缓存 → 变更立即生效(原权限变更最迟延迟 30s)。
- 行级 owner 过滤推广候补已否决(Domain): 脚本/同步任务/质量规则是团队共享资产, 个人级隔离反协作; 空间隔离已由项目维度承担。
- E2E: 停用→旧 session 下个请求即 401 / 重新启用旧 session 仍死(须重新登录) / 删除同样即时 / 受保护端点验证彻底失效 / admin·viewer 回归正常; 单测 35 绿; 测试号已清。commit 3d7f213 push caowen 双向验证(远程=本地)。

## 2026-06-12 批15: 权限细分——登录直进首页 + 全站按钮无权限不显示(ultracode Workflow, ?v=u53)
业主令①登录进来直接进首页②没权限的功能不显示(无运行权限不放出运行按钮)。
- A 登录直进首页: login.html 登录成功/已登录预检均 replace 'workspace.html#/home'。
- B 按钮权限机制升级(根治覆盖盲区): 新增 dnObserveBtnPerms —— MutationObserver 监听 DOM 新增, 含 [data-perm] 即 rAF 防抖自动隐藏。各模块动态渲染按钮无需每处手动调 dnApplyBtnPerms, 重渲染后也不漏。
- C 全站操作按钮挂 data-perm(两阶段 Workflow): Phase1 审计 26 Explore agents 得 228 按钮; Phase2 实施 23 agents 挂 230 按钮(20 js 并行+workspace 三段串行)。写/运行/危险操作挂, 查看/筛选/导航不挂。坑: DN.h 创建元素须 'data-perm' 带引号 key(含连字符, 否则语法错废文件; Phase1 片段 68/228 裸键 bug 故 Phase2 强约束规范挂法), HTML 模板用 data-perm=""。覆盖 workspace.html(117)+20 js。
- 验证: node --check 全 js OK+无裸键; admin(*) 直进#/home/11模块console 0错/全显; viewer 9模块写按钮全隐+菜单隐藏settings+console 0错; observer 动态注入 develop:run/mdm:manage 隐藏、governance:view 显示; 开放模式不隐藏。commit 6606d5a push caowen 双向验证(远程=本地)。

## 2026-06-12 批16: 数据模型三层建模工程 第1轮(ultracode Workflow, ?v=u54)
业主令: 加强数据模型, 要业务/逻辑/物理三层 + L1-L5 + 完整规范 + 模型生成 + 申请审批流转。业主拍板: 独立顶部菜单「数据模型」+ L1-L5 主题域业务分层与数仓物理分层并存。
- 调研(5维Workflow): 复用主题域/物理元数据/数据标准/MDM三层骨架/dn_mdm_change_request审批范式。
- 后端 domain/datamodel: 5表(sql/82 dn_model三类型溯源/entity[L3/L4]/attribute[L5绑标准element_code+码表dict_code]/relation/change流转) + dn_subject增强level(L1-L5)+layer_type(数仓分层)+SubjectService.create算层级(坑: controller原绕过service致level不算, 已修) + DataModelService(CRUD+流转[复用mdm禁自批+通知]+逻辑→物理生成[实体→表/属性→字段/类型映射/snake命名/溯源]+DDL生成+命名校验) + Controller + 权限点datamodel:view/edit/approve + PermInterceptor鉴权 + @MapperScan登记。
- 前端 js/datamodel.js + 独立菜单: 主题域L1-L5树(层级badge+数仓分层)+三层类型tab+模型列表(徽标/操作)+模型详情抽屉(实体+属性表格编辑器)+流转(提交/工单/审批)+生成物理+DDL预览。操作按钮挂data-perm+observer自动显隐。
- E2E: 后端全链路(主题域L1/L2 level正确→逻辑模型→实体→属性→提交→审批PUBLISHED v2→生成物理→DDL类型映射正确); 前端真机admin(菜单/L1-L5树/列表/徽标/操作/console 0错)。测试号已清。commit 3c304d9 push caowen 双向验证。
- 后续轮: ER图/逆向导入/绑标准下拉/版本对比/业务→逻辑生成/标准角色授权/规范强校验。

## 2026-06-13 批17: 数据模型第2轮 生成链补全+逆向+绑标准+授权(?v=u55)
承第1轮, 补全生成/逆向能力与规范落地:
- 业务→逻辑生成 generateLogical(L3→L4 复制属性溯源, 同 generatePhysical 模式); 三层生成链(业务→逻辑→物理)完整。
- 物理逆向导入 reverseFromTable(dn_table_meta/dn_column_meta→PHYS模型: 表→实体, 列→属性, 物理类型反归一化, PRI→主键, 可空识别)。
- helpers toCamel/normalizeType/toModelCode/uniqueCode。Controller /generate-logical + /reverse。
- 前端: BIZ「生成逻辑」+ 顶部「逆向导入」(选已采集表)+ 属性编辑器 elementCode 绑数据标准 datalist(联动带出类型/长度)。
- 标准角色授权 sql/79 给 DATA_DEV/DATA_GOV datamodel 权限。
- 验证: 编译通过+node语法OK+前端真机(按钮/datalist/console 0错)。⚠️后端 generate-logical/reverse 干净 E2E + pymysql 运行时授权因远程库 38.76.183.50 持续网络抖动未取得通过(本会话多次 MySQL 超时); 代码为已验证 generatePhysical 同模式克隆。已挂后台守护轮询等 DB 恢复自动补验+授权。commit 7063c6e push caowen 双向验证(github 可达, 仅远程库不可达)。

## 2026-06-13 批18: 数据模型第3轮 建模规范强校验 + 服务器恢复补验(?v=u56)
- 建模规范强校验: validateModel(errors 阻断/warnings 提示: 至少1实体/每实体至少1属性/逻辑物理须主键/属性须类型/逻辑未绑标准warning); submitForApproval 前置校验有 error 抛异常阻断; Controller GET /validate 预检; 前端 dmSubmit 提交弹窗预检(红块errors禁提交/黄块warnings/通过显✓)。
- 验证: 后端 E2E(无实体/无属性/缺主键阻断+补全后提交成功) + 前端真机截图(红色须修正[缺少主键]+黄色建议[未绑标准]+提交禁用)。commit cba52b7 push验证。
- 重要环境事件: 远程服务器(38.76.183.50)本会话后段曾关机, 致依赖服务全不可达, 表现为"网络抖动/MySQL超时/session丢失/HikariPool耗尽"——全是服务器关机非代码bug。业主重启后依赖自启(3307/6333/7474/9030 全 OPEN), app 重启重置池即恢复。第2轮 generate-logical/reverse/标准角色授权 恢复后全部补验通过。
- 教训: ① 前端异步 UI 验证用 poll-until-rendered 不用固定 sleep(时序竞态); ② urllib 调 POST 端点须传 body/显式 method(无 body 默认 GET→405)。

## 2026-06-13 批19: 数据模型第4轮 资产落地+ER图+版本历史+关系管理(加大力度, ?v=u57)
一次交付三大功能闭环:
- A 资产落地(流转终点): publishToAsset 已发布物理模型→实体注册 dn_table_meta(库 model_<编码>)+属性注册 dn_column_meta, 落地数据地图; 前端 PHYS+PUBLISHED「落地资产」按钮。
- B ER图可视化+关系管理: 详情「ER图」→实体盒子(属性+主键)+SVG虚线连接关系(测位置画线+箭头)+关系列表; 配套关系增删(选源/目标实体+类型1:1/1:N/M:N)。
- C 版本历史: 审批发布存模型快照 dn_model_version(sql/83, 含实体/属性/关系); 详情「版本历史」列各版本+查看快照。后端 DnModelVersion+Mapper+listVersions/publishToAsset+Controller端点。
- 验证: 后端 E2E(关系/审批→版本快照v2/物理发布→落地 model_库2表3字段注册数据地图) + 前端真机截图(ER图3实体盒子+2条SVG连线+关系列表)。测试数据(含落地 model_ 表)已清。commit 8173f9e push验证。
- 数据模型工程四轮交付完整: 三层模型/L1-L5/生成链/逆向/规范强校验/流转/ER可视化/版本/资产落地全闭环。

## 2026-06-13 运维: 远程依赖服务全面巡检 + Doris 恢复
业主提醒服务器重启后依赖可能未全启。逐一应用层巡检(非仅端口):
- MySQL 3307 ✓ / Qdrant 6333 ✓(collection dn_meta在) / Neo4j 7474 ✓(HTTP discovery) / PostgreSQL 5432 ✓ / nginx ✓ / datax ✓ (6 容器全 Up)
- **Doris 9030 异常**: 端口半通但查询中断, FE/BE 进程未起(服务器重启未自启)。
- 修复: SSH 上服务器(paramiko, deploy.env 凭据), Doris 独立部署 /opt/doris/apache-doris-3.0.8-bin-x64(deploy_mode=local 存算一体, 已初始化有数据)。启 FE(start_fe.sh --daemon → 8030/9030 LISTEN)。启 BE 首次崩(ulimit nofile soft=1024 太低, 要求 655350), `ulimit -n 655350` 后重启成功(8040/9060 LISTEN, BE 收 FE 心跳注册 cluster_id 595171440)。
- 验证: 本地 pymysql 9030 SELECT 1✓ / ods 库在✓ / SHOW BACKENDS alive=true✓。
- 持久化: limits.conf 追加 nofile 655350(防下次重启 BE 崩)。
- 注: Doris 未配开机自启, 服务器重启需手动 start_fe/start_be(或后续配 systemd)。

## 2026-06-13 批20: 数据模型第5轮 版本对比diff + 发布迭代(?v=u59)
- compareVersions(from,to): 解析两版本快照JSON, 实体级(增/删)+属性级(增/删/类型或主键变更)差异; Controller GET /compare。
- 前端: 版本历史加勾选+「对比所选两版」, diff配色(绿增/红删/黄改); 按id升序(旧→新)使方向直观。
- 修复1: submitForApproval 原拒绝 PUBLISHED 重新提交(文案矛盾), 改为允许已发布模型变更后重新提交迭代新版本。
- 修复2: deleteModel 漏删 dn_model_version, 补级联删。
- 验证: 后端 E2E(发布v2→变更→重提v3→compare识别 新增实体/新增属性/类型变更) + 前端真机截图(版本对比v2→v3 diff配色)。测试数据(含孤儿版本)已清。commit 6deac5d push验证。
- 数据模型工程五轮完整: 建模/分层/生成/逆向/规范/流转/可视化/版本/对比/落地全闭环。

## 2026-06-13 批21: 数据模型第6轮 模型即治理之源·四联动闭环(五角色碰撞, ?v=u60)
业主令"功能丰富+每个有用且联动+多角色碰撞"。Workflow 五角色碰撞(Domain/Architect/Product/Critic)强收敛于"模型即治理之源", PM拍板砍孤立(AI建模/MDM导入/真建表/无上游血缘), 落地四联动闭环:
- A 落地派生治理: publishToAsset 属性绑标准→回填 dn_column_meta 密级/敏感(分级脱敏消费)+ 约束→质量规则建议 dn_quality_rule(null_check/unique_check停用待审, 幂等)。
- B 标准强校验: validateModel 绑标准比对 dn_data_element 类型不一致→error阻断 + 词根命名 warning。
- C 标准影响分析: GET /standard-impact 反查引用数据元的模型属性; gov-standard 数据元行加「影响」入口(治理专员改标准前评估)。
- D 建模覆盖度看板: GET /dashboard 聚合(模型/类型状态分布/落地数/标准覆盖率/待审); 顶部「📊 看板」入口。
- 验证: 后端E2E(绑标准INT≠STRING→error/落地回填密级+派生2停用规则/影响分析2处引用/看板统计) + 前端截图(看板5指标卡)。commit f8d01db push验证。
- 多角色碰撞模式有效避免堆砌: 五角色独立提议+批判→PM综合+Domain否决孤立→拍板联动。

## 2026-06-14 批22: 用户任务权限 — 数据源独立鉴权(续做被中断的工作, ?v 后端)
- 背景: PermCatalog 20:21 已加 datasource:view/edit + develop:approve, 但 PermInterceptor 未接线(/api/datasource 仍走 develop:edit, GET 仅要登录)。续完成:
  - PermInterceptor: /api/datasource 写→datasource:edit(从 develop:edit 拆出独立管控连接信息); 敏感 GET 加 /api/datasource→datasource:view(列表/详情/库/表/列暴露连接配置与源库结构)。
  - 前端 workspace.html: 数据源表单"保存"按钮 data-perm settings:config→datasource:edit, "测试连接"补 data-perm datasource:edit。(注: 系统管理>数据源管理 tab 实为 Doris 仓库配置, 保持 settings:config 不动。)
  - sql/87_datasource_perm.sql: DATA_DEV +datasource:view/edit/develop:approve; DATA_GOV/PROJECT_MGR/VIEWER +datasource:view(读)。SoD: 脚本审批自批由 ScriptApprovalService 记录级拦截, 角色给 approve 安全。
  - PermInterceptorRuleTest 补 6 断言。
- 部署: 622 测试全绿 BUILD SUCCESS; 远端先应用 sql/84(baseline_type 守卫式 ADD)/85/86/87; 打 jar→SFTP→swap→restart(login.html 200 active)。
- 重要环境发现+决策: 服务器 datanote.env 原**无 DATANOTE_PASSWORD**→认证旁路, 所有 RBAC/越权检查(含本次)全部 dormant。为使权限功能真正生效, 给 datanote.env 追加 DATANOTE_USERNAME=admin / DATANOTE_PASSWORD=admin123(原文件备份 .bak)并重启。**现服务器已强制登录**。
- E2E(auth 开): viewer GET datasource/list=200(有 view) + POST test=403(无 edit, 提示需要 datasource:edit); admin GET=200/POST test 非403(*); 未登录 GET=401。全部符合预期。

## 2026-06-14 批22 续: 敏感 GET 鉴权补漏(同类洞清扫, 投资者代理审计)
- 触发: 续做时 E2E 发现 GET /api/script/changes 旁路(审批队列对任意登录用户 200, 泄 payload SQL 快照+申请人)——与 datasource 同类: WRITE_RULES 配了 develop:approve 但 GET 走 SENSITIVE_GET_RULES 未命中→仅登录。
- 投资者代理扫全部 *Controller GET, 确认 4 处真漏(逐一人工复核+读源码确认脱敏情况):
  - /api/script/changes(+pending-count) → develop:approve(队列含 SQL 快照+申请人, 仅审批人; 须先于 /api/script 匹配)
  - /api/script/{id}、/{id}/versions、/tree → develop:view(单脚本完整 SQL, 原按自增 id 可枚举读他人脚本)
  - /api/system/config/{group} → settings:config(库/仓库连接 host/port/用户名/jdbc url; 密码已脱敏; 读与写同权)
  - /api/ai/config → settings:config(provider/baseUrl/model+apiKey 前 8 位; 读与写同权)
- 顺序铁律: SENSITIVE_GET_RULES 首命中即返, 更具体规则(/api/script/changes)须排在通用前缀(/api/script)之前。submitter 查自己待审走 /{id}/has-pending、/{id}/pending-change(命中通用 /api/script→develop:view, 仍可达)。
- PermInterceptorRuleTest +9 断言(含顺序用例)。打包→上线→E2E: viewer changes=403/script/1=200/tree=200/system-config=403/ai-config=403; admin 全 200。8/8 符合预期。

## 2026-06-14 批22 续2: 写端鉴权审计(清白) + 审批闭环功能 E2E
- 写端审计(投资者代理扫全 Controller + 人工交叉核对全部 base path): 所有敏感写操作均被 WRITE_RULES 覆盖, 仅登录的写仅 /api/auth(故意)与 /api/notify mark-read(markRead 按 id+receiver 限当前用户, 无 IDOR); /api/dashboard 无写。结论: 写端无漏洞, 无需改动。
- 审批闭环线上 E2E(临时建 qatmp[DATA_DEV]+脚本 214, 测后彻底清空): 首登 mustChangePwd:true→改密清零; 提交 ONLINE→工单 pending; qatmp 自审 code:-1 被拒(有 develop:approve 过拦截器, 服务层记录级禁自批+事务回滚, 工单仍 pending); admin 同行审批 code:0→工单 approved + 脚本 schedule_status=online(审批→上线联动真执行)。残留=0。
- 验证: 用户任务权限完整闭环(模块/功能/数据/审批 SoD/首登改密)线上可用。

## 2026-06-14 批23: 删除即清理闭环(自找方向 — 三路代码勘探triangulate)
- 方向来源: 提交批22后, 三个并行勘探代理审计全仓(未完成/孤岛重复/断裂联动), 共同指向"删除操作不级联清理→数据残留"。
- A 删数据源→清元数据(HIGH, 修鬼表): 原 DatasourceController.delete 只删源记录+连接池+项目引用, 采集来的 dn_table_meta/dn_column_meta 长期残留污染数据地图(鬼表)。新增 MetadataCrawlerService.deleteByDatasource(事务): 按 datasource_id 删表元数据+其字段(table_meta_id IN)+采集日志, 并失效数据地图摘要缓存; controller 删源前调用, 提示已清理 N 张表。图库/向量镜像无单删接口, 随下次全量同步自愈(注释说明)。
- B 删质量规则→关遗留工单(MED, 防僵尸工单): 工单经 object_ref='qrule:{id}' 关联规则。新增 IssueService.closeIssuesForDeletedRule(事务): 关闭(非删除, 留历史)该规则全部未关闭工单+注[自动关单]; QualityController.deleteRule 调用, 提示已关闭 N 个工单。
- 验证: 622 测试全绿; 线上 E2E(合成 id 88888/99999, 测后清零): 删源 tables 2→0/cols 4→0/logs 1→0 提示"已清理2张表"; 删规则 工单 OPEN→CLOSED 提示"已关闭1个工单"; 残留=0。commit 待填。

## 2026-06-14 批24: 删脚本→清SQL血缘边(批23删除即清理补全) + 勘探假阳复核
- 接批23方向继续。先复核三路勘探剩余 flag, 多为假阳(自查避免瞎改):
  - "采集后不重建血缘"(HIGH): 假阳。血缘只由 sync-job(MAPPING)+script(SQL) 派生, LineageEdgeService/SqlLineageService 不读 dn_table_meta/column_meta, 采集不改血缘输入, 重建是 no-op。
  - "SysAlertScheduler 告警无渠道"(MED): 假阳。line69 已 notificationService.notify(admin,SYS_ALERT) 真发站内信。
  - "AlertService 无重试"(MED): 非缺陷。opt-in 外部 webhook(钉钉/通用, enabled=false 默认, 异步+限流+加签), fire-and-forget log.warn 失败是正确设计; 站内信为主通道。
- 真缺口(LOW, 已修): ScriptService.delete 不发 ScriptSavedEvent → 删脚本后其 SQL 血缘边残留到夜间 02:40 兜底。save(line155)发、delete 漏。补 delete 后 publishEvent(ScriptSavedEvent), rebuildFromScripts 全删重建自然排除已删脚本。
- 验证: 622 测试绿; 线上 E2E(脚本 INSERT...SELECT 可解析): save→2 边(表级+列级, 2s异步)、delete→0(1s), 残留清零。
- 结论: 删除即清理三对称完成(数据源/质量规则/脚本)。该轴勘探收益递减, 代码实际较健康(剩余假阳为主)。

## 2026-06-14 批25: 性能(血缘BFS去N+1) + 新功能(SQL片段团队共享) + 审计复核
- "都做"四项: ①push(已推caowen abed15b) ②功能审计=干净(前端代理审 robust, 无真bug) ③性能 ④新功能。
- 性能: 仅一处真N+1(其余多premature/假阳, 自查后弃)。LineageEdgeService 表级血缘 bfs() 原每访问一节点查一次库(N节点=N往返, 影响分析热路径), 与下方字段级 computeColumnBfs(一次加载内存BFS)不一致。重构为 computeTableBfs(纯函数, 一次性 selectList(level_type=TABLE)→邻接表内存BFS); 加 TableLineageBfsTest 5 断言(下游/上游/深度/环/null)。DataMapService:323=假阳(单查非N+1); MetricController/无分页=小数据premature, 弃。
- 新功能: SQL片段库团队共享(完善批批次新增的per-user隔离片段=协作缺口)。sql/88 加 shared 列(守卫式); list 改 (created_by=me OR shared=1); save 持久化 shared; incrementUse 放宽到 own∪shared; 前端: 保存表单加"团队共享"勾选+列表 团队 徽标+@创建人+本人行 共享/取消共享 切换(toggleSnippetShare 复用 save)+非本人共享片段仅显插入(改删后端 develop:edit+服务层 owner 双护栏)。
- 验证: 627 测试绿(+5 BFS); 线上 E2E: viewer 见团队共享(1)不见他人私有(0)、非owner删=403片段存活、清零。删除即清理E2E沿用。commit 待填。

## 2026-06-15 批26: 修 admin 用不了 AI 助手(越权隔离误拦)
- 现象: admin 发消息给 AI 助手报"无权访问该会话(越权隔离)"。
- 根因: 昨日启用强制登录后, 之前开放态(无认证)创建的 AI 会话 user_name='anonymous'; ownerOk 只在"调用者=anonymous"时放行, 未处理"会话属主=anonymous"且无超管豁免 → admin(caller=admin)访问 anonymous 属主会话被判越权。前端 localStorage 残留旧 sessionId 触发。
- 修复(AiAgentService.ownerOk + AiAgentController session()/assertSessionOwner 三处同款): ①超管 admin 放行任意会话; ②属主为 null 或 anonymous(开放态历史会话)放行; 非 admin 对他人实名会话仍隔离。
- 验证: 627 测试绿; 线上 E2E: admin 读 anonymous 属主会话 code0 / admin 读 viewer 属主会话 code0(超管) / viewer 读 admin 属主会话 code-1(隔离完好)。commit 待填。

## 2026-06-15 批27: 权限管理全代码扫描 + 同类"过严"bug收口
- 三路并行只读代理审计(IDOR属主一致性 / 拦截器鉴权完整性 / 提权与RBAC加固), 逐条人工复核(前轮假阳率高)。
- **无安全漏洞(无越权放行)**:
  - 拦截器覆盖: 完整(写全覆盖/敏感GET全覆盖/无permitAll泄漏/注册无过度排除)。
  - 提权与RBAC: 已加固0洞(自提升防护hasPermission/最后超管保护/内存兜底!existsInDb/停用DisabledException/fail-closed权限缓存/会话固定invalidate/口令策略/防爆破/会话硬化, 逐项确认)。
  - 代理报的 ScriptService/DatasetService/IssueService "IDOR"=**假阳**: 按批22 Domain决策, 脚本/质量规则/数据集/工单是团队共享资产, 由模块权限(develop:edit/governance:issue/metrics:edit)管控而非行级owner, 非漏洞。Project 有 project:all-data+超管 canSeeAllProjects 兜底, 正确。
- **真bug(与昨日 admin 用不了AI助手同类: 启用登录后历史匿名属主数据 + 无超管豁免 → admin被锁; 属"过严"非安全洞)**:
  - AiFileService.ownerDenied: 加超管放行 + 文件属主 null/anonymous 放行。
  - AiAgentController.ownerScope(cron 启停/删除): admin 返 null(运维历史匿名/全部任务)。
  - (AiMemory 召回 / SkillLibraryTool 列表 = 内部只读 RAG, 无害, 不动。)
- 验证: 627测试绿; 线上E2E: admin启停匿名属主cron code0 enabled0→1 / viewer启停他人cron 403(隔离完好)。AI会话(批26)同类已修。commit 待填。

## 2026-06-15 批28: 权限细化 — 用户直授权限点 + 数据权限(资源级访问控制)
- 业主令: 用户/角色权限细化 + 数据权限(A表不对B开放/C表只对D开放)。AskUserQuestion 拍板: 范围=全主要资源(本轮先库表落地, 引擎通用可扩); 粒度=角色+用户; 默认=公开/黑名单; 用户直授=要。
- ① 用户直授权限点: dn_user_perm(user_id+perm_code), RbacService.getUserPerms 改为 角色并集 ∪ 用户直授; getUserDirectPerms/setUserDirectPerms; RbacController GET/POST /users/{id}/direct-perms(自提升防护 hasPermission + PERM_CHANGE 审计 + evictAll); 前端用户行「直授」入口 umDirectPerms(复用权限点树, 保存覆盖式)。
- ② 数据权限(通用引擎): dn_data_grant(resource_type+resource_id+principal_type[ROLE/USER]+principal, resource_id 字符串键: 库表=db.table)。DataAclService: bypass(开放态/匿名/admin/'*'/data:all)→canAccess(无授权=公开默认/命中用户名或角色编码=放行)/deniedIds(类型级一次加载内存判定免N+1)/listGrants/setGrants(覆盖式)。perm点 data:grant(管理)+data:all(绕过)。DataAclController /api/data-acl(拦截器读写均 data:grant)。
- ③ 库表落地: DataMapService.searchTables/aiSearch 过滤受限表(deniedIds), getTableDetail 受限拒(canAccess); MetadataController.tableDetail catch 区分 BusinessException 如实回传(原吞成通用错致"无权访问"被掩盖)。前端表详情「数据授权」modal: 角色下拉+用户datalist 增删授权, 空=公开。
- 验证: 627 测试绿(修3个 DataMapService 构造测试 + PermInterceptorRuleTest 加 data-acl 断言); 线上E2E: 限ghost→viewer拒/admin过(超管); 授ROLE VIEWER→viewer过(角色级); 撤销→拒; viewer直授 data:all→有效权限含之+绕过(直授合并+bypass); 残留清零。commit 待填。
- 遗留(下轮"全资源"扩展): ACL 引擎已通用, 待接 项目/数据模型/指标/脚本 的列表过滤+详情守卫+授权入口(复用 DataAclService+modal)。

## 2026-06-15 批29: 数据权限扩展到 数据模型 + 指标(通用引擎复用)
- 接批28, 用通用 DataAclService 引擎扩展资源覆盖(默认公开/黑名单不变)。
- 前端: 抽出通用 openDataAclModal(resourceType, resourceId, label)(原 dmOpenDataAcl 改为薄封装调它), 任意资源复用同款授权弹窗(角色下拉+用户datalist)。
- 数据模型(MODEL): DataModelController.listModels 用 deniedIds('MODEL') 过滤受限模型, getModel canAccess 守卫(R.fail 如实回传); datamodel.js 模型详情加「🔒 数据授权」按钮(openDataAclModal('MODEL', id, name))。
- 指标(METRIC): MetricController.list deniedIds('METRIC') 过滤, getById canAccess 守卫; gov-consumption.js 指标行加「授权」入口。
- 守卫在 controller 层(不在 service), 避免破坏内部系统调用(发布落地/取值等)。
- 验证: 627 测试绿(修 MetricSaveUniqueTest 构造参数); 线上E2E: MODEL 41 限ghost→viewer列表无41+详情拒/admin过, 授ROLE VIEWER→viewer列表含41+详情过; METRIC(临时行)限ghost→viewer列表无+详情拒/admin过, 公开→可见; 残留清零。commit 待填。
- 数据权限覆盖: 库表(批28)/数据模型/指标(批29)。脚本=团队共享资产(Domain决策, 模块权限管控)、项目=自有成员制ACL(project:all-data+owner/成员), 二者按既有模型管控不重复套用。

## 2026-06-17 M-E AI Agent 权限对齐(新工程·分支 feature/me-perm-align)
- 业主新令: AI agent ①能做系统所有能做的事 ②执行权限与发起用户 RBAC 一致(功能级+数据级)。规划为 5 特性程序(E 权限对齐→D 完整能力覆盖→A 数据分析图表→B PDF/Word RAG→C SSE), spec 见 docs/superpowers/specs/2026-06-17-*.md, 计划 docs/superpowers/plans/2026-06-17-ME-*.md。**本轮交付地基 M-E(权限对齐)**。
- 子代理驱动开发: 11 任务, 每任务 实现+spec审+码质审 两阶段把关, 16 commit。
- 核心: AgentContext 携带发起人 perms/roles 快照(边界一次解析, 不读 SecurityContextHolder, 修异步 ThreadLocal 失效) + PermGate 功能闸门(缺权硬拒, 漏标写工具 fail-closed 需超管) + AgentAccessChecker 数据闸门(表作用域 canAccessAs) + DataAclService.canAccessAs/deniedIdsAs 显式入参重载 + 17 工具声明 requiredPerm + 接入 AiAgentService 主循环/并行批/resume/**子代理** 4 处 invoke 咽喉点 + 审批放开自批(确认人须真有权限点)。
- 测试: 22 新单测全绿; 全量 649 跑/2 失败(WorkspaceDbSyncUiTest, 与本分支无关——分支 0 行改 workspace.html)。
- 活体多用户 E2E(本地 8099 连远程库, DATANOTE_PASSWORD 启用认证, viewer/viewer123 仅 :view 权限): ①viewer 无 assistant:use → POST /chat 被 PermInterceptor 403; ②viewer 临授 assistant:use 建脚本 → 内层 PermGate 拒 create_script(permission_denied, 缺 develop:edit); ③viewer 查 admin 专属受限表 → table_data data_denied; ④admin 建脚本 → wait_approval → 自批(T9)→ resume → 落库 id217(验后删); ⑤**最终审查发现 Critical: 子代理 delegate_task 绕过两闸门**, 修复(ChildAgentRunner 子循环接入 PermGate+AccessChecker), 活体复证 viewer 委派读受限表 → 子代理 table_data data_denied 不泄露。E2E 后测试数据全清(脚本/表授权/临时权限)。
- 待办: M-E 合并(等最终审查复核子代理修复); 后续 D(完整能力覆盖)/A/B/C 特性。**git 未合并到 master, 在 feature/me-perm-align**。

## 2026-06-17 M-B / M-C AI Agent 文档RAG + SSE流式(分支 feature/bc-rag-sse, 从 feature/md-capability 切出)
- 收官 5 特性程序末两块(E/D/A 已交付): **B PDF/Word→RAG** + **C SSE 流式**。两特性工具/路径自动继承 E 的 PermGate+审批闸门。
- **特性B(commit ecae337)**: pom +Apache PDFBox 2.0.31(Java8, 业主授权唯一新增依赖); docx 走零依赖(DocxTextExtractor zip+SAX, 仿 XlsxTextExtractor); PdfTextExtractor(PDFBox 限页/字符)。AiFileService.readText 扩 pdf/docx + 暴露 ownerCanAccess 静态 ACL(单一事实来源)。DocIngestService: 异步 ingest(提取→切块800/重叠100→embedBatch→upsert kind=doc, owner payload)+删除级联清向量块(VectorStoreClient.deleteByFilter)+searchDocs(owner 隔离, 复用文件 ACL)+纯函数 chunk。doc_search 工具(只读/requiredPerm=null, 服务层 owner 隔离)。AiAgentService.buildRagText(q,owner) 合并资产RAG+文档片段RAG注入首轮 prompt。dn_ai_file +index_status/chunk_count(sql/81); 前端文件列表索引状态徽标 + 索引中单次重刷。工具 69→70。
- **特性C(commit 33977dc)**: AiAssistService.chatStream(msg,ctx,onToken) 真 provider 流式(stream:true, 逐行解析 OpenAI delta.content / Anthropic content_block_delta), **不动 chat() 签名**(红线), 失败且零增量→回退非流式 chat()(降级铁律)。AgentEventBus 按 sessionId 维护 SseEmitter 推 step/running/token/done(无订阅者全 no-op, 阻塞路径不变)。端点 GET /stream/{sessionId}(owner 校验 + 会话未建放行 + 5min 超时)。AiAgentService 首轮在有订阅者时走 chatStream 真流式逐字推 token, writeStep→step / RUNNING→running / 收尾→done+complete。前端 send 前开 EventSource(step 实时进度 + token 打字机, 剥 think/tool_call; POST 落定 renderTurn 替换), 失败回退 1.5s 轮询(兜底)。?v u380→u381。
- **测试**: 新增 DocIngestChunkTest(5) + DocxTextExtractorTest(3) 全绿; 全量 654 跑/2 预存失败(WorkspaceDbSyncUiTest, 仅改 ?v 数字无关)无新增回归。
- **活体E2E(本地 8099 连远程库, 开放模式)**: 启动 22.8s 全 bean 装配无误; /tools count=70 含 doc_search; **C: 开 SSE→POST /chat(deepseek)→收 open/step×2/token×54(逐字'我是'/'『'…真流式)/done{DONE}, sid honored**(注: run 仅认 [0-9a-zA-Z]{8,64} 的 client sid, 测试须用纯字母数字); **B: 上传 docx 不崩, 向量库未配置(无 QDRANT 环境变量)→indexStatus=null 优雅跳过, doc_search 降级 engine=none**; docx 提取由 DocxTextExtractorTest 验证(段落换行/多run拼接/tab-br/空档)。B 全向量 RAG 路径需 QDRANT_URL/embedding 环境(本会话无凭据), 已验降级 + 提取 + 入库守卫。
- **状态**: 5 特性程序(E/D/A/B/C)全交付。分支 feature/bc-rag-sse 已提交; 待推送 caowen + PR(栈式: base=feature/md-capability)。MDM 工具化仍待(需 MdmCoreController→MdmService 行为保持重构)。

## 2026-06-17 全栈部署到服务器 + B/C 活体 E2E(业主授权恢复部署)
- 业主给 38.76.183.50 root 凭据, 拍板"起 Qdrant + 部署全栈 + B E2E"(推翻 2026-06-08 的不部署约束)。
- 发现: server 原无 Qdrant 容器(此前为给 Doris FE 让内存停掉), vector 全程 off; 部署 jar 为 6/16 旧版(早于 E/D/A/B/C)。embedding 已配(bailian dashscope text-embedding-v3 dim1024)。
- 动作: ①`docker run -d --name datanote-qdrant --restart unless-stopped -p 127.0.0.1:6333:6333 -v /opt/datanote/qdrant_storage:/qdrant/storage qdrant/qdrant:v1.7.4`(app 在 host systemd 跑, 不在 docker 网→QDRANT_URL 必须 127.0.0.1 非 docker 名)。②datanote.env 追加 VECTOR_ENABLED=true/QDRANT_URL=http://127.0.0.1:6333/QDRANT_API_KEY=/QDRANT_COLLECTION=dn_meta。③MySQL 应用 sql/81(dn_ai_file +index_status/chunk_count)。④paramiko sftp 传 feature/bc-rag-sse 全栈 jar(96MB, 旧 jar 备份 .bak)→systemctl restart(server 启动 105s)。
- 验证: 启动日志 `[vector] 向量库已就绪 http://127.0.0.1:6333`; embedding configured; /tools(authed admin) count=70 含 doc_search。
- **B 完整向量 RAG 活体 E2E**: 上传带标记 docx→indexStatus indexing→indexed(chunkCount=1)→Qdrant dn_meta 1 个 kind=doc 点→agent 问"文档里的验证口令"→**答出 ZEBRA8842RAG**(doc_search 未进 steps=经 buildRagText 文档片段注入闭环: searchDocs→Qdrant 召回→prompt 注入)→删文件 deleteByFilter 级联→Qdrant 点归 0。
- **C SSE 活体 E2E(鉴权模式)**: 开 SSE→POST /chat→收 open/step×2/token×55/done。
- 收尾: 测试 session(ragverify%/srvsse%)+文件全清, 残留 0; server 容器 = datax/mysql/nginx/pg/**qdrant**/redis, datanote.service active(http 200), 向量库就绪。**注意内存**: 之前因 Doris FE 占满停过 qdrant, 现 FE 已降 2GB 堆, app 718MB + qdrant 共存 OK。

## 2026-06-17 向量库 + 图库 全量启用并投入使用(业主检查后补全)
- 业主问"向量库和图库是否都启用、是否运用到项目"。检查发现 enabled≠used:
  - Qdrant 虽启用, 但 dn_meta 仅 2 个 kind=memory 点(agent 记忆), **项目元数据未索引**(fullReindex 仅元数据采集后或手动 /sync 触发);
  - **Neo4j 根本没起**(GRAPH_ENABLED 缺, 日志"图数据库未启用"), 仅血缘走 MySQL fallback。
- 补全动作:
  - **向量库**: POST /api/ai/store/sync → vectorPoints=1098(table 1081 / dataelement 16 / metric 1 / memory 2)。验证 /api/ai/store/search?q=订单 → engine=vector 命中 t_sales_order(销售订单 0.72)/order_info 等真实表。列级(column)需另跑 /sync-columns(异步, 列量~万条 embedding, 暂未触发, 表级已足够 grounding)。
  - **图库**: docker 起 neo4j:5-community(127.0.0.1:7474/7687, 堆封 512m), datanote.env 追加 GRAPH_ENABLED=true/NEO4J_HTTP/NEO4J_USER/NEO4J_PWD, 重启 → 日志"图数据库已就绪"。/sync 镜像血缘 → graphEdges=1, Neo4j 2 节点 1 关系(MySQL dn_lineage_edge 当前仅 1 条血缘, 内容少属正常)。
- 验证: /api/ai/store/health 三态全绿(vector ready / graph ready / embedding available dim1024)。容器: qdrant+neo4j+redis+pg+nginx+datax+mysql 全 Up。内存 used 8.2GB/avail 7.8GB(neo4j 堆封顶不撑爆)。
- 用途: 向量库→semantic_search/doc_search 工具 + buildRagText RAG grounding + agent 自学习记忆; 图库→graph_trace/impact/neighbors 工具 + GraphRAG 血缘扩展(MetadataCrawlerService/LineageEdgeService 自动镜像)。
