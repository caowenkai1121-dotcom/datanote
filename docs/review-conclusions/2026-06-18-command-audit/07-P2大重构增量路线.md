# 07-P2 大重构增量路线(P2-01 前端壳层拆分 / P2-02 DTO 收敛)

> 本轮审查 P0/P1 + bounded P2(P2-03/04/05 + Dashboard 监控权限 + Shell env 清洗)已全部修复、测试、部署、活体验证。
> P2-01(前端壳层拆分)与 P2-02(DTO 收敛)是**纯可维护性大重构**:无安全/功能收益、回归面大,且审查明令"每次只迁一个能力"、
> CLAUDE.md 红线**禁止一次性重写 workspace.html / 整个文件**。故**不在单轮强行整体重写**,按下表逐步小迁,每步独立可回归。

## 现状(已部分模块化)
- API 层:`dn-common.js` 的 `DN.api/DN.get/DN.post` + 全局 `window.fetch` CSRF 包装,已是共享 API 客户端(本轮 CSRF 时落地)。
- WS 层:`workspace.html` 内 `connectWebSocket` IIFE(本轮 P0-02 刚改私有化 + 验证,**勿在紧邻轮次再迁,避免回归刚验证的修复**)。
- 路由层:`workspace.html` `navigateTo/parseRoute`,与权限守卫/抽屉/锁释放/DOM 耦合。

## P2-01 增量步骤(每步:抽出 → 保留兼容全局函数 → node --check + StaticFrontendSafetyTest + 真机点测 → 提交)
1. **api-client.js**:把 `workspace.html` 的薄 `window.api()` 包装迁入(复用 `dn-common.js` 已有逻辑),`workspace.html` 仅保留 `window.api = DNApi.api` 兼容垫片。最低风险,先做。
2. **ws-client.js**:把 `connectWebSocket` + 订阅迁出;`onTaskLog/onSqlLog/onTaskStatusChange` 等 handler 暂留 `workspace.html` 并 `window.*` 暴露,ws-client 经 window 调用。**待 P0-02 在生产稳定一段后再做**。
3. **router.js**:把 `navigateTo/parseRoute/ROUTES` 迁出;权限守卫、抽屉关闭、计时器清理留为 `window.*` 钩子由 router 调用。风险最高,最后做。
- 原则:一次只迁一个文件;迁后 workspace.html 行为零变化(兼容垫片);每步单独提交 + 真机回归。

## P2-02 增量步骤(从高频+安全敏感接口起,新增最小 DTO,不破坏响应字段)
1. `metadata`(MetadataController 的 `Map` 入参 → `MetadataSearchRequest` 等)。
2. `datamodel`(建模实体/属性/关系入参)。
3. `schema diff`、`project tags`。
- 原则:不全仓替换;新增 DTO 仅约束入参/补字段校验,响应保持现字段不破坏前端;每接口补契约测试。

## 进度
- [x] P2-01.1 api-client.js — 已抽出 `_handleResponse/api/apiPost` 到 `js/api-client.js`(dn-common.js 后加载),workspace.html 去内联定义、行为不变;真机验证: window.api 为 function、实时 api() 调用 ok、0 console 错误。
- [ ] P2-01.2 ws-client.js(待 P0-02 稳定后)
- [ ] P2-01.3 router.js
- [ ] P2-02.1 metadata DTO
- [ ] P2-02.2 datamodel DTO
- [ ] P2-02.3 schema diff / project tags DTO

> P2-06(隐藏模块重定向说明)已由 P1-07 路由守卫(无权点名缺失权限点 + 回首页)覆盖。
> P2-07(同步详情小屏)已有 `.dbsync-drawer{width:96vw}`@≤640px;余 emoji/旧内联为"逐步"cosmetic,随各模块迭代清理。
