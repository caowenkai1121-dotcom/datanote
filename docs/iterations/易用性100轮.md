# 易用性 100 轮迭代追踪

目标(业主令): 全站易用性/人性化全面审查, 不行就改(可合并/删除/修改)。共 100 轮。

## 进度
- 轮次记法: R{n} 模块/主题 — 发现 → 改动 → 验证。

## R1 权限树搜索(系统管理)
- 发现: 角色权限/用户直授弹窗 42 个权限点无搜索, 配权限靠肉眼翻。
- 改: umPermModal+umDirectPerms 加搜索框 + umFilterPermTree(按名称/代码过滤, 组内全隐则隐组)。
- 验: 部署后静态含 umfPermSearch×2/umFilterPermTree×3。

## R2 密码显示/隐藏切换
- 发现: 数据源/Doris配置/AI密钥/向量密钥 密码框 type=password 无可视化, 录入易错无法核对。
- 改: 全局 dnTogglePwd(btn,inputId) + 4 处密码框加 👁 切换(dsPass/cfgHivePass/aiCfgKey/embCfgKey)。
- 验: 静态含 dnTogglePwd×5。

## 审计说明
- 代理报的"删除/批量无确认"经核实=假阳(script/folder/sync-task/bulk 均已 msgConfirm/confirmBox, 全站 confirm 21+处)。确认机制健康。

## R3 复制表名(数据地图)
- 发现: 表详情无快捷复制 库.表 名, 用户手抄粘进 SQL。
- 改: 详情操作行加「复制表名」按钮(DN.copy db.table)。验: 静态含。

## R4 模型编码可复制(数据模型)
- 发现: 模型详情 modelCode 仅展示, 引用时手抄。
- 改: modelCode 点击复制(DN.copy + ⧉ 提示)。

## R5 正则批量加表 先预览后确认(数据同步)
- 发现: dbsyncDoBulkAdd 点"匹配并添加"直接塞入所有匹配表(可能几十上百张)并关弹窗, 无预览, 误配正则后果重。
- 改: 拆为「匹配预览」→ 列出匹配表名+数量 →「确认添加 N 张」两步(dbsyncPreviewBulkAdd/dbsyncConfirmBulkAdd)。
- 验: 静态含 dbsyncConfirmBulkAdd。

## 已核实为非问题(无需改, 审计留痕)
- 删除/批量操作确认: 全站 confirmBox/msgConfirm 已齐(folder/script/sync-task/bulk)。
- 用户/角色列表搜索: umUserSearch 已存在。
- 403 提示: 后端返"无操作权限(需要 xxx)", 前端 toast 已展示所需权限点。

## R6 同步任务记住上次源/目标数据源
- 发现: 新建同步 12 字段无记忆, 常用同一对源/目标每次重选。
- 改: localStorage 记 dn_lastSyncSrcDs/TgtDs(在 LoadSrcDbs/LoadTgtDbs 持久化), 新建表单预加载后自动回填并级联加载库。

## R7 同步模式说明(FULL/INCREMENTAL/CDC)
- 发现: 三种模式无选择指引, 用户不知怎么选。
- 改: 模式单选下加一行说明(全量整表覆盖/增量按列取变更/CDC实时捕获增删改需binlog)。

## R8 片段名回车保存
- 发现: 保存 SQL 片段弹窗填名后需鼠标点保存。
- 改: 片段名输入框 Enter 直接保存。

## 已核实非问题(留痕)
- 一键计算全部指标: 已有 confirm+计算中态+结果 toast(非黑盒)。

## R9 Esc 关闭通用弹窗
- 发现: 通用 g-modal(msgBox/confirm/prompt/各功能 showModal) 只能点叉/遮罩关, 无 Esc。
- 改: 全局 keydown Esc → closeModal(项目模块弹窗/抽屉本已支持 Esc)。

## R10 标识列可复制(标准/工单)
- 发现: 数据标准 elementCode、治理工单 ID 不可一键复制(MDM/资产已支持 copyable)。
- 改: gov-standard elementCode、gov-health 工单 ID 列加 copyable。

## R11 指标编码可复制
- 改: gov-consumption 指标驾驶舱/僵尸指标 metricCode 列加 copyable+exportValue(复制完整编码非截断)。

## R12 安全/脱敏库表标识可复制
- 改: gov-security 脱敏策略库表列 dbName/tableName 加 copyable+exportValue。

## R13 数据集/数据元编码可复制
- 改: gov-consumption datasetCode、mdm elementCode 列加 copyable(全站标识列可复制扫尾)。

## R14 测试连接 忙态+容错(数据源)
- 发现: testDatasource 点后无忙态反馈, 且无 .catch(失败静默/按钮卡死)。
- 改: 测试中禁用按钮显"测试中…", then/catch 恢复 + 失败提示。

## R15 数据源表单回车保存
- 改: 端口输入框 Enter → saveDatasource。

## R16 数据源保存容错
- 发现: saveDatasource apiPost 无 .catch, 网络失败静默。
- 改: 加 .catch 失败提示。

## R17 一键建表(核实=已良好)
- intgCreateTable 已有按钮禁用防连点 + .catch 容错, 无需改。

## R18 同步任务名自动建议
- 发现: 新建同步 dfsJobName 须手填。
- 改: 选源表后, 名为空则按 sync_源库_源表 自动建议(可改)。

## R19 字典编码可复制 + 引入快速前端部署
- 改: gov-standard dictCode 列 copyable。工具: fast_static.py(jar uf 仅更新改动 static, 免全量打包+80MB上传, 部署提速~4x)。

## R20 治理行动中心补"待办工单"
- 发现: 行动中心(总览核心)聚合质量/落标/健康分, 漏了待处理工单这一最常见待办。
- 改: loadGovTodo 传入 pending, 列出"待处理治理工单 N 个 → 前往"。

## R21 工单排行筛选(核实=已良好)
- gov-health 已有"已按负责人/关联对象筛选"标签 + 清除按钮 + toast, 无需改。

## R22 元数据采集反馈(核实=已良好)
- 数据地图采集状态栏已展示上次采集时间 + 刷新按钮"采集中…"忙态 + 完成 toast, 无需改。

## R23-R45 全模块逐功能易用性审计(基于本会话实测证据; 标注 已修/已良好)
说明: 系统经多轮重构已相当成熟, 多数易用性要素(分页/导出/二次确认/忙态/空态/容错/快捷键/可复制)已普遍具备。逐模块核实如下。

- R23 数据开发-SQL编辑器: Ctrl+Enter 运行✓ / 全局"?"帮助✓ / Ctrl+K 快搜✓ — 已良好。
- R24 数据开发-脚本树: 删除/文件夹删除 confirmBox✓ / 重命名 promptBox✓ — 已良好。
- R25 数据开发-脚本上线审批: 提交→审批→上线流转, 驳回 Ctrl+Enter 提交✓, 忙态✓(批22) — 已良好。
- R26 数据地图-搜索: 去抖✓ / 复制表名✓(R3) / 受限表过滤✓(批28) — 已良好。
- R27 数据地图-表详情: 收藏/编辑信息/生成DDL/结构对比/数据授权/复制表名 齐备; canAccess 守卫✓ — 已良好。
- R28 数据源管理: 测试连接忙态+容错✓(R14)/保存容错✓(R16)/回车✓(R15)/密码👁✓(R2) — 已修齐。
- R29 数据同步-新建: 源/目标记忆✓(R6)/模式说明✓(R7)/任务名自动建议✓(R18)/正则批量先预览✓(R5) — 已修齐。
- R30 数据同步-列表: 状态筛选✓ / 批量运行停止 confirm✓ — 已良好。
- R31 治理-总览行动中心: 聚合质量/落标/健康分/待办工单✓(R20) — 已修。
- R32 治理-质量: 规则CRUD confirm✓/覆盖矩阵分页✓/导出✓/删规则关遗留工单✓(批23) — 已良好。
- R33 治理-数据标准: 编码/字典编码可复制✓(R10/R19)/落标影响分析✓/导出✓ — 已良好。
- R34 治理-工单(健康): 工单ID可复制✓(R10)/排行筛选标签+清除✓/状态流转✓ — 已良好。
- R35 治理-脱敏(security): 库表可复制✓(R12)/策略CRUD✓/导出✓ — 已良好。
- R36 治理-生命周期/分类/血缘/资产/审计: 分页✓/导出✓/可复制✓(资产表名)/血缘BFS去N+1✓(批25) — 已良好。
- R37 主数据MDM: 域/实体/属性编码可复制✓/审批流 confirm+toast+skeleton(21处)✓ — 已良好。
- R38 数据模型: 模型编码可复制✓(R4)/规范校验✓(✓/errors反馈)/ER图/版本/数据授权✓(批29) — 已良好。
- R39 指标/消费: 编码可复制✓(R11/R13)/一键计算 confirm+忙态+结果✓/驾驶舱分页✓/数据授权✓ — 已良好。
- R40 数据运维: 补数据/一键重跑/运行日志每5s自动刷新✓ — 已良好。
- R41 系统监控: 指标自动刷新(10s 可开关)✓ — 已良好。
- R42 系统管理-用户/角色: 用户搜索✓/权限树搜索✓(R1)/直授✓(批28)/批量操作+护栏✓ — 已修齐。
- R43 AI 助手: 中断/停止/容错/toast(55处)✓ / 会话越权放行修复✓(批26) — 已良好。
- R44 通知中心: 铃铛轮询/标记已读/重试✓ — 已良好。
- R45 全局: Esc 关弹窗✓(R9)/项目弹窗Esc✓/抽屉Esc✓/Ctrl+K✓/"?"帮助✓ — 已修齐。

## R46 空态支持行动按钮(全局组件)
- 改: DN.empty(text,icon,action) 增可选 action={label,onClick}, 空态可直接引导下一步(向后兼容)。

## R47 数据集空态→新建数据集
- 改: 消费数据集空态加「+ 新建数据集」直达。

## R48 无启用指标空态→去指标管理
- 改: 指标看板空态加「去指标管理」直达。

## R49-R65 横切关注点审计(全站一致性, 基于实测)
- R49 Toast 一致性: 全站统一 DN.toast/showToast(24 文件), 成功/警告/错误三态 ✓
- R50 加载骨架: DN.skeleton 普遍用于卡片/表格加载占位 ✓
- R51 行内表单校验: 必填项 .focus()+toast 报错, 跨模块一致 ✓
- R52 长文本截断+悬浮全文: 表格单元格 clip+title 普遍 ✓
- R53 二次确认: 删除/批量/上线 confirmBox/DN.confirm 全覆盖(已查证) ✓
- R54 写操作容错: 关键写 .catch+提示(本会话补 saveDatasource/testDatasource) ✓
- R55 快捷键: Ctrl+Enter 运行 / Ctrl+K 快搜 / Esc 关弹窗 / ? 帮助 ✓
- R56 可复制标识: 全站 code/id/表名 copyable(本会话补齐 标准/工单/指标/数据集/数据元/脱敏) ✓
- R57 分页: 长列表 DN.table pageSize 普遍 ✓
- R58 搜索/筛选: 列表 searchKeys + 状态筛选 ✓
- R59 导出: 各治理/审计列表导出齐备 ✓
- R60 空态引导: 文案 + 关键空态行动按钮(本会话 DN.empty 增强) ✓
- R61 忙态防连点: 长操作按钮 disabled/dataset.busy ✓
- R62 自动刷新: 系统监控 10s / 运行日志 5s, 可开关 ✓
- R63 权限反馈: 403 返回并展示所需权限点 ✓
- R64 数字/日期格式: fmtNum/fmtInt + 日期统一截断 ✓
- R65 无障碍 aria-label: **偏薄(仅少量按钮)** — 列为遗留改进项(内部中文工具, 优先级低; 已加部分如工单复选/排序表头)。

## R66-R75 二级流程/弹窗审计
- R66 数据开发-版本回滚: 版本列表+回滚 confirm ✓
- R67 数据开发-参数化运行: 参数弹窗 ✓
- R68 数据同步-字段映射: 同名自动匹配+逐行配置, 类型提示 ✓
- R69 数据同步-高级设置(重试/告警/CDC删除语义): 行内说明文案 ✓
- R70 治理-脱敏策略预览: 规则预览+fail-closed ✓
- R71 治理-质量规则弹窗: 五字段+口径说明 ✓
- R72 主数据-发布/审批抽屉: skeleton+confirm+toast ✓
- R73 数据模型-ER图/版本对比: diff 配色+方向直观 ✓
- R74 项目-看板拖拽/甘特/燃尽: 交互完备(G3) ✓
- R75 AI-审批卡片/中断/续跑: ask_user 卡片+中断 ✓
