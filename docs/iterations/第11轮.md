# 第 11 轮 · 数据消费层前端看板 + 指标值定时计算（接 R10，让消费层用户可见可用）

> 接 R10 新增的数据消费层后端，本轮补**用户可见的前端看板** + **指标值定时计算**，闭合"定义→计算→消费"的可视闭环。

## 新增内容
- **前端 `js/gov-consumption.js`（治理子模块「数据消费」）**：注册 `GOV_RENDERERS.consumption`，指标驾驶舱看板：
  - 消费概览磁贴（启用指标/快照数/陈旧/僵尸/近7天消费）
  - 指标值看板表（最新值 + 取值时间 + 新鲜/陈旧药丸 + 行内「计算」「导出CSV」）
  - 一键计算全部指标按钮
  - 消费热度 Top 条形图（近30天调用）
  - 僵尸指标表（启用但从未取值）
  - 全量复用 DN.* 组件 + app.css 设计系统，零图表库。
- **挂载**：`workspace.html` 的 `GOV_MODS` 加 `consumption` 条目 + `<script src="js/gov-consumption.js?v=q100r81">`（2 处字节安全编辑，DOM/机制不变）。
- **后端 `MetricValueScheduleService`**：`@Scheduled(cron 每日01:30)` 计算全部启用指标值并落快照，持续积累时序，供看板趋势消费。复用 `MetricValueService.calc`，单条失败兜底。

## QA 结论 —— ✅ 通过
服务器 E2E（pid 1007412）：gov-consumption.js **200**；workspace.html 含 `key:'consumption'` 模块 + 引用 gov-consumption.js；/api/consumption/overview 200。
→ 「治理中心→数据消费」驾驶舱用户可见可用，定时计算就绪。✅

## 验收门禁自检
- [x] 功能可运行（511 全量回归绿）
- [x] 业务专家确认（消费看板可见、端点可达）
- [x] 新增用户可见功能（数据消费治理子模块 + 定时计算）
- [x] 复用既有插件机制(GOV_RENDERERS)+设计系统(app.css/DN.*)，未破坏 DOM
- [x] QA：gov-consumption.js 200 + 模块注册 + overview 200
- [x] 可回归（511 全量）
- [x] 部署成功可回滚（active/http302/pid1007412；代码 revert）

## 下一轮
R12 候选（自行抉择）：数据集注册（把一段精选 SQL 注册为可复用查询/数据服务）、质量↔消费联动（指标值异常自动建治理工单）、资产消费热度榜进数据地图。
