# 第 46 轮 · 深度优化·后端服务第二批 6 个(大范围)

> 深优阶段第六轮。深优 DataMapService/DatasourceExploreService/IssueService/StandardService/LifecycleService/SyncJobService。Workflow 12 代理(6 优化→6 对抗审查)。

## 深优内容(每服务 6 类)
校验(BusinessException 具体 msg)、异常(log 不静默, 并发冲突 catch 由静默重查改 warn 命中返回/未命中上抛)、空安全(selectXxx 判空、消除既有 NPE 如 qualityIssueDescription rule==null)、事务(写方法 @Transactional, 只读不加)、性能(getPopularTables 去重、批量化)、边界(标识符校验 requireValidDbTable、null 防御)。
**IssueService 治理工单引擎语义严格保持**: objectRef=qrule:{ruleId} 去重、ne("status","CLOSED") 防重、shouldAutoCloseOnRecovery 自动关单逻辑零改动(审查逐项核实)。

## QA —— ✅ 通过(对抗审查 + 编译 + 518 + 冒烟)
- **6 对抗审查全「PASS 无回归风险」**: public 签名全未变、import 齐全、@Transactional 仅写方法、IssueService 去重/状态流转/自动关单语义保留、异常类型对调用方 catch 一致(IllegalStateException/BusinessException)、新增均为 NPE 收敛。
- 自验: 编译 BUILD SUCCESS; **518 全量绿**; 运行时冒烟 6 端点(databases/tables/issues/lifecycle/sync/glossary)200。

## 验收门禁
- [x] 大范围深优(6 后端服务×6类)
- [x] 对抗审查深度(12 代理全 PASS, IssueService 语义重点核实)
- [x] 深度优化 + 不破坏(签名/行为/518/工单语义保留)
- [x] 功能可运行(编译+518全量+冒烟6端点200)
- [x] 自续(R47 继续)

## 下一轮 R47
深优剩余后端服务(调度域 ScheduleLifecycle/Execution、CDC/同步引擎、RBAC、MaskingService 等)+ 控制器层校验。
