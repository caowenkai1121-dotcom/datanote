# 第 45 轮 · 深度优化·后端服务 6 个(大范围, 转后端)

> 深优阶段第五轮, 转入后端。深优 MetricValueService/MetricAlertService/DatasetService/MdmPublishService/HealthScoreService/ClassificationService。Workflow 12 代理(6 优化→6 对抗审查)。

## 深优内容(每服务 6 类)
1) 输入校验: public 方法入参 null/空/范围校验 → BusinessException 具体 msg。
2) 异常处理: 消除空 catch; 降级处 log.warn 带上下文(如 maturityScore 取数失败记日志而非静默)。
3) 空安全: selectById/selectOne/selectList 结果判空; Map.get 判空; 修既有 NPE(如 MetricAlert rules==null/r.getId()==null)。
4) 事务: 多写方法加 @Transactional(rollbackFor=Exception.class)(MetricValueService.calc / DatasetService.save / MdmPublishService.fanOut), 只读不加。
5) 性能: 消除 N+1(MetricValueService freshness/inputQuality 改批量 selectBatchIds + 内存映射 latestSuccessMap/latestRunMap); MdmPublishService fanOut 加 MAX_SUBSCRIPTION_FANOUT=1000 上限。
6) 边界正确性: 除零/空集合/时间空/枚举非法/objectRef 去重(MetricAlert metricCode 为 null 由 "null" 改 "#id")。

## QA —— ✅ 通过(对抗审查 + 编译 + 518 + 运行时冒烟)
- **6 对抗审查全「PASS 无回归风险」**(部分 reviewer 实跑 mvn compile + 单测): public 方法签名全未变(controller 依赖)、import 齐全、@Transactional 仅加写方法、异常语义不破坏现有单测、批量改写等价、无新 NPE。
- 自验: 编译 BUILD SUCCESS; **518 全量回归绿**; 运行时冒烟 7 端点(consumption/overview+ranking、health/dimensions 6维、quality、classification、gov/overview、dataset)全 200。

## 验收门禁
- [x] 大范围深优(6 后端服务×6类, 含事务/N+1/NPE 修复)
- [x] 对抗审查深度(12 代理全 PASS, 部分实跑编译测试)
- [x] 深度优化 + 不破坏(签名/行为/518 测试保留)
- [x] 功能可运行(编译 + 518全量 + 运行时冒烟 7端点200)
- [x] 自续(R46 继续后端服务深优)

## 下一轮 R46
深优更多后端服务: DataMapService/DatasourceExploreService/IssueService/QualityService/ScheduleLifecycleService 等。
