# 第51轮 —— 项目模块服务深度优化（收尾：任务/成员/Wiki/标签/设置/活动）

## 本轮目标
完成**项目模块 13 个 service 的深优收尾**。本批审查剩余 6 个 service：4 个需硬化（任务/Wiki/标签/设置），2 个审查后确认已干净（成员/活动）。写方法事务原子化 + 防 NPE + 入口校验，**行为语义不变**。

## 各角色产出

### 【Architect】优化范围（剩余 6 service 全审）
| 服务 | 优化点 | 红线 |
|---|---|---|
| ProjectTaskService | `saveTask`/`saveMilestone` 入参对象判空（防 NPE on getTitle/getName） | 状态/优先级枚举归一、归属校验、deleteMilestone 事务+解关联不变 |
| ProjectWikiService | `savePage` 入参 `p` 判空 | 树 parent_id 语义、列表裁字段、deletePage 事务+子页提根不变 |
| ProjectTagService | **setProjectTags 加 `@Transactional(rollbackFor)`**：原先删后插非原子，中途失败致标签关联不一致 → 整体原子；`tagMapper.selectList` 结果判空 + 元素判空 | 仅接受真实存在标签、去重、deleteTag 事务级联删映射不变 |
| ProjectSettingService | `saveQuota`/`saveEnvParam` 入参判空；`deleteEnvParam` 的 `p.getProjectId().equals(projectId)` 改 `Objects.equals`（getProjectId 可能 null 防 NPE） | 配额 1:1 upsert、环境参数 key upsert+synchronized、归属校验不变 |
| ProjectMemberService | **审查确认已干净**（角色校验/dup/末位 OWNER 守卫/count 判空齐全），无需改动 | — |
| ProjectActivityService | **审查确认已干净**（limit 限界 1-200/projectId 校验/路径前缀精确匹配无串项目），无需改动 | — |

### 【QA】结论 —— ✅ 通过
- 编译通过（无新增 import，Tag 用 FQN `@org...Transactional` 与同文件 deleteTag 风格一致；Setting 用 `java.util.Objects`）。
- 全量回归：`mvn -o test` **518 run / 0 fail / 0 error / 1 skip** → **BUILD SUCCESS**。
- 事务正确性：setProjectTags 加 `rollbackFor=Exception.class` 仅增强「删旧+插新」原子性，无业务分支改动。
- **项目模块 13 service 深优全完成**（R48 ProjectService + R50 六服务 + R51 六服务）。

## 验收门禁自检
- [x] 功能可运行（518 全量回归绿 + BUILD SUCCESS）
- [x] 业务专家确认（纯防御/原子性，无业务行为改动）
- [x] 不绕治理/不重复（复用既有 mapper，零新表零迁移零新端点）
- [x] 契合架构（service 层内优化，依赖方向不变）
- [x] 可回归（518 全量 + 编译）
- [x] 可回滚（纯 service 层 4 文件，git revert 即复原）

## 遗留问题
- 项目模块 13 service 已全深优；后续转编排层（Backfill/Dolphin/SqlLineage/AssetDetailService）与横切层。

## 下一轮
R52 候选（自行抉择）：编排层 service（BackfillService/DolphinService/SqlLineageService）+ AssetDetailService、或 common 层（GroupService/SubjectService/DataCleanupService）。
