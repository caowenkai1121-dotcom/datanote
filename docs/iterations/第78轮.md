# 第78轮 —— 全站品牌色统一(旧 Ant 天蓝 → 靛蓝 Slate&Indigo)

> 设计令牌主色早已从 Ant 天蓝 #1890ff 迁到石墨靛蓝 #3457d5(`docs/设计规范.md`), 但 JS 图表/徽标/强调与 workspace.html 内联样式里仍散落 **175+ 处硬编码旧天蓝**, 导致全站"链接/按钮靛蓝、图表/标签天蓝"色调割裂。本轮全量收敛, 让整个系统统一靛蓝。

## 一、替换范围(品牌蓝 → 靛蓝, 保语义色与多系列调色)
- `#1890ff`/`#1890FF` → `#3457d5`(主品牌蓝→靛蓝)
- `#40a9ff` → `#4f6ef0`(hover 蓝→靛蓝 hover)
- `rgba(24,144,255,.x)` → `rgba(52,87,213,.x)`(带透明品牌蓝→靛蓝)
- `#e6f0ff`/`#69a9ff`/`#bcd7ff` → `#eaeefc`/`#7e97ee`/`#c3cef5`(浅蓝底/热力梯度→靛蓝系)
- **保留不动**: 语义色 `#52c41a 绿 / #faad14 橙 / #ff4d4f 红 / #13c2c2 青 / #722ed1 紫`(状态语义 + 多系列图表区分色)。

## 二、改动量
- js/*.js + app.css + gov-modern.css: **175 处**(23 文件; app.css 58 / gov-modern 23 / 各 gov-*·mdm-* 余)。
- workspace.html 内联样式: **54 处**(develop/dbsync/datamap/metrics/project/system shell)。
- 共 **229 处** 旧蓝 → 靛蓝。残留仅 1 处(redesign.css 注释内的 #1890ff 说明文字, 无影响)。
- 工程: python 替换全程 `newline=''` 保 LF, workspace.html diff 干净(72/72 行, 无全文件爆炸)。

## 三、QA(Playwright 真机, 0 console error, 全量测试绿)
逐页扫所有元素计算色(color/bg/border/fill/stroke):
- **治理页**: 旧天蓝 rgb(24,144,255) 出现数 **16 → 0**, 靛蓝 rgb(52,87,213) 758 处。
- **数据地图页**(workspace.html 内联): 旧蓝 **0**, 靛蓝 544 处。
- 全量回归 527/0fail/1skip。

## 运维插曲(已恢复)
本轮多次重启 app 期间, 被强杀的多个 JVM 瞬时建 Hikari 池冲高远程 MySQL 连接, 触发 1040 Too many connections 启动失败。诊断: max_connections=151 实际仅 42 占用(我的 churn 峰值已回落; 30 个为外部 IP 非我)。杀光本地 java 后单次干净启动即恢复。**教训: 频繁重启间留间隔, 避免多 JVM 并发建池冲高远程连接。**

## 验收门禁自检
- [x] 全站品牌蓝统一为靛蓝(治理/数据地图实测旧蓝 0)
- [x] 保留语义色与多系列图表区分色(不误伤)
- [x] 0 console error + 527 测试绿
- [x] 工程整洁(LF 保留, diff 不爆炸)
- [x] 可回滚(纯色值替换 + 版本号)

## 下一轮
- 字体字符感(离线优先 system-ui 优化 + 数据区 tabular-nums 普及); 逐模块表单/表格留白巡检; 报表内嵌样式抽 print.css(仍硬编码已转靛蓝, 但结构仍内嵌)。
