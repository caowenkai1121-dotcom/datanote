# 第72轮 —— sync_dashboard 只读工具(闭环补齐 agent 自报缺口)

> R71 首页简报中 agent 主动反馈"工具链缺数据同步全局概览接口"。本轮据此补 `sync_dashboard` 只读工具——AI 自己发现缺口→人补工具→AI 下次能用, 形成"用→反馈→增强"闭环。

## 一、改动
- 新增 `sync_dashboard`(只读 LOW, group=sync): 聚合 SyncJobService.list → 同步任务总数 + 按状态分布 + 按同步模式分布 + 已上线调度数。**工具数 33→34(9写25读)**。

## 二、QA 结论 —— ✅ 通过
- 全量回归 `mvn -o test` **527 / 0fail / 1skip** → BUILD SUCCESS。工具数 34, sync_dashboard 已注册。
- **E2E**: 问"数据同步整体态势..."→ agent **自主调 sync_dashboard** → 答: 7 任务总数 / 状态分布(成功4·停止2·已创建1) / 模式分布(FULL 5·CDC 2) / 上线调度 0, 并给出"无调度→自动化监控缺失"的风险结论。

## 验收门禁自检
- [x] 功能可运行(527 绿 + E2E)
- [x] 闭环改进(agent 简报自报缺口 → 本轮补齐 → 同步全局问答可答)
- [x] 安全(纯只读聚合, 无写副作用)
- [x] 无重复(复用 SyncJobService.list, 不碰执行表重逻辑, 与 sync_job_detail[单任务] 互补不重叠)
- [x] 可回滚(纯新增 1 工具文件)

## 下一轮
- 向量列索引(7629列分批); 记忆抽屉归档/置顶; 首页简报可缓存为常驻卡片。
