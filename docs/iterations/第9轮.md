# 第 9 轮 · mapper/sync/config/filter 全面归域 — 后端 100% 领域驱动（行为不变）

> R7（controller/service）+ R8（model/dto/exception/util）的收尾轮。本轮把**持久层 mapper、同步引擎 sync、配置 config、过滤器 filter** 全部归域。
> 至此后端**全部** controller/service/model/dto/exception/util/mapper/sync/config/filter 已按 8 限界上下文分层，仅 `DataNoteApplication` 留根 → **后端 100% 领域驱动**。
> **本轮 300 文件变更（48% 系统）**；行为不变=纯物理重组。

## 范围与映射
- **mapper(87)** → 镜像其实体所在域：`DnXxxMapper` → `model_pkg(DnXxx)` 的 `.mapper`（如 DnQualityRuleMapper→`domain.governance.mapper`）。共落 **13 个 mapper 目标包**。
- **sync(68)** → 前缀移 `com.datanote.sync.*` → `com.datanote.domain.integration.*`（保子结构 cdc/connector/dialect/dto/engine/engine.cdc/engine.incremental/schema/service/util/controller）。
- **config(8)** → `platform.config`（SecurityConfig/HiveConfig/CorsConfig/WebMvcConfig/WebSocketConfig/OpenApiConfig/AuthProperties/DbUserDetailsService）。
- **filter(1)** AuditFilter → `platform.audit`。

## 关键难点处理（架构师）
1. **@MapperScan**：45/87 mapper 无 `@Mapper` 注解、依赖扫描；且存在非 mapper 接口（HiveService/DbConnector/SqlDialect/SyncEngine 等）→ **不可用广义 `@MapperScan("com.datanote")`**（会误注册接口为 mapper）。改为**显式列 13 个各域 mapper 包**：`@MapperScan({"com.datanote.domain.*.mapper", "com.datanote.platform.*.mapper"})`。
2. **sync 字符串 FQN**：CdcSyncEngine:199/202 Debezium 反射用字符串 `"com.datanote.sync.cdc.JdbcOffsetBackingStore/JdbcSchemaHistory"` → 前缀替换 `com.datanote.sync.` → `com.datanote.domain.integration.` 自动同步（类与字符串同步迁移，反射仍命中）。
3. **通配 import**：`com.datanote.sync.*` 通配被前缀替换自动修正为 integration；`com.datanote.{mapper,config,filter}.*`（包已空）清除 9 处 + 注入具体 import。

## 执行（脚本 + 编译器网）
- 164 类移动 + 161 文件 FQN/前缀替换 + 51 import 注入 + 9 失效通配清理 + @MapperScan 13 包。
- 一次过 test-compile；507 全量回归绿（@SpringBootTest 启动验证 @MapperScan 无 BindingException、组件扫描、CDC 反射）。

## QA 结论 —— ✅ 通过
服务器 E2E（pid 985733）：13 端点全 **200** —— iam/config(HiveConfig→/api/system/config/doris)/datasource/metadata/develop/orchestration/governance/mdm/project/common/**integration(sync-job)/sync-folder/cdc**，root 302。
→ mapper 13 包 @MapperScan 无 BindingException、sync 引擎归 domain.integration 生产可跑、config 迁移后 Doris 配置正常。**后端 100% 领域驱动,行为零改。** ✅

## 验收门禁自检
- [x] 行为不变（507 全量绿；@SpringBootTest 上下文启动正常）
- [x] 业务专家确认（13 端点含 sync/cdc 全可达）
- [x] 后端 100% 领域驱动（mapper/sync/config/filter 归域闭环）
- [x] 大幅代码调整（本轮 300 文件变更 ≈ 48% 系统）
- [x] 零表变更
- [x] QA：13 端点全 200/302（含 sync/cdc）
- [x] 可回归（507 全量）
- [x] 部署成功可回滚（active/http302/pid985733；纯代码 revert，无 SQL）

## 下一轮
后端结构闭环已成。转向**功能去重/合并**(架构师靶点)：MDM 11→4 controller、横切 告警(2套)/审计(3套)/用户偏好(4表) 归一、调度运行表 4 合 1(DnRunInstance,含迁移)。
