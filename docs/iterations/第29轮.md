# 第 29 轮 · Cron 预览接通 + 安全态势下钻(大版本)

> R21..R44 第九轮。补 dbsync 新建表单 Cron 实时预览(后端 cron-preview 端点此前无此入口)+ 接通最后一个未联动的治理模块「安全态势」。

## 改动
- **workspace.html**: dbsync 新建表单 `dfsScheduleCron` 输入 onblur → `dfsCronPreview()` 调 `/api/scheduler/cron-preview` 实时显示下次执行时间(绿/红校验反馈), 填错即知。
- **gov-security.js**: 安全态势接入深链(最后一个未联动 gov 模块)——脱敏策略行/行级权限行加「敏感分级」(→classification table)+「数据地图」(→catalog openTable); 概览加「查看全部审计」(→audit); 入口收 ctx.table 聚焦该表安全信息 + 顶部聚焦条。

## QA —— ✅ 通过(Playwright)
gov-security node--check OK; 33 UI 绿; 本地运行。Playwright: `dfsCronPreview` 就位; gov-security 渲染无报错 + 「查看全部审计」链接存在。版本 gov-security q100r94。

## 验收门禁
- [x] 大版本(Cron 预览新特性 + 安全态势全模块联动收尾)
- [x] 架构师全盘(至此 11 个治理模块全部接入深链)
- [x] 消除孤岛(安全态势脱敏/行级策略可下钻; Cron 输入可预览)
- [x] 功能可运行(node--check + 33UI + Playwright)
- [x] 自续(R30 继续)

## 下一轮 R30
采集历史/元数据明细下钻(蓝图 R42) + 冷数据/无用表→生命周期/工单一条龙(蓝图 R43)。
