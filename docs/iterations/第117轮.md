# 第117轮 —— 主数据+数据治理 多角色优化(第1轮): 修真bug + 砍报表堆砌 + 补断裂闭环

> 业主: 主数据 和 数据治理 两模块多角色优化, 要求【所有功能可用、有意义】, 【减少各种报表, 要具体需要, 新增统计报表不是需要】。

## 一、【QA/Architect】全模块盘点(两个 code-explorer 并行)
逐功能分类 [CONCRETE-OP 具体操作] / [REPORT-CHART 纯可视化] / [PLACEHOLDER-BROKEN 占位坏]:
- **MDM 12 模块**: 建模/黄金记录/去重/存活规则/层级/参考数据/交叉引用/审批 = 真实CRUD(保留); 总览/数据管家 = 纯聚合报表(可压缩); 审批缺"提交申请"入口(断裂); 发布订阅 publish 是模拟记账非真推送。
- **治理 12 模块**: 质量规则/工单闭环/落标稽核/数据元词根码表/分类分级打标/脱敏行级/用户管理/生命周期/血缘/指标计算/DCMM自评/术语/主题域/审计 = 真实操作(保留); 总览大屏/体检报告/雷达/趋势/热力日历/覆盖矩阵/安全态势/消费排行/联动地图 = 报表堆砌(砍); recentPassRate 大小写bug。

## 二、【QA→Dev】修真bug(功能要可用)
1. **质量通过率恒0/100% bug**: dn_quality_run.run_status 落库小写 `success`, 但 OverviewService/HealthScoreService 用 `eq("run_status","SUCCESS")` 大写匹配 → 命中0条 → 通过率恒0; 且 recentPassRate 又 `*100` 把已是 0-100 的 pass_rate 双重放大成 10000%。修: 两处改小写匹配(大小写不敏感) + 去掉 *100。验: recentPassRate 0→**100.0**(2条 success 均 100%), 健康质量维度恢复真实。
2. **MDM 变更审批闭环断裂**: 后端 `/api/mdm/approval/submit` 在但前端无"发起"入口。修: mdm-approval.js 加【＋新建变更申请】抽屉(实体下拉/变更类型/业务主键/原因/申请人/变更内容JSON, 客户端校验+JSON合法性校验)→ POST submit。验: 提交 code=0 新建 pending, 待审批+1, 闭环打通。

## 三、【Domain/Product→Dev】砍报表堆砌(要具体需要)
**治理总览 gov-overview.js 重写为【治理行动中心】**: 只留
- 可点 KPI 磁贴(健康分/表/字段/库/质量分/待办工单, 每块直达对应操作模块);
- 治理待办行动中心(聚合质量异常/落标违规/健康分偏低, 每项"前往"直达处理)。
**砍掉**(纯可视化, 各指标在子模块已有, 不再堆砌): 全屏大屏按钮、生成体检报告打印、五维雷达、健康趋势折线+周对比、趋势研判、数据资产卡、数据质量卡、治理工单条形、敏感等级/类型分布、近90天日历热力。文件 455→~120 行。验(Playwright): 总览只剩 KPI + 待办行动中心, 质量分显示 100%, 报表卡全消失。

## 验收门禁自检
- [x] 质量通过率/健康质量维度 bug 修复(大小写+双重放大), recentPassRate=100
- [x] MDM 变更审批"提交申请"入口补齐, 闭环打通(submit→pending 验证)
- [x] 治理总览砍报表→行动中心(KPI直达+待办处理), 真机验证
- [x] 编译+打包+重启+E2E+Playwright 全绿; JS r117

## 遗留(后续轮次继续砍报表/补闭环)
- 治理: 质量覆盖矩阵热力、工单分析环图、DCMM等级环图、安全态势分布环图、消费热度/排行/僵尸指标、联动地图(govmap纯导航)、标准落地概览 等仍待精简。
- 治理 [PLACEHOLDER 待查]: 脱敏/行级策略是否真被 SqlMaskRewriter 拦截应用(配置表已存, 查询链路接入待确认); 角色新增缺UI。
- MDM: 总览/数据管家纯报表可合并; 发布订阅 publish 模拟记账非真 webhook 推送(按需补真实推送)。
- 按 CLAUDE.md 多角色多轮推进, 本轮为第1轮。
