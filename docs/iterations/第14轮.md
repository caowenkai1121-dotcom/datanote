# 第 14 轮 · 指标预警规则前端配置 UI（补全 R12 闭环可见）

> 接 R12（指标预警→自动工单，后端）。本轮补**用户可见的预警规则配置 UI**，让用户在消费看板直接为指标配阈值规则，无需调 API。
> 前端为主，复用 R12 既有端点（`/api/consumption/metric/{id}/alert-rules`、`/metric/alert-rule/save`、`/metric/alert-rule/{ruleId}` DELETE）。

## 新增内容
- **gov-consumption.js 指标驾驶舱行内「预警」操作** → 打开预警规则抽屉：
  - 列出该指标现有规则（严重度药丸 + 比较符 + 阈值/区间 + 删除）。
  - 新增规则表单：比较符（GT/LT/GE/LE/NE/OUT/IN）+ 阈值下界 + 区间上界 + 严重度 → 保存即生效（下次指标计算越界自动建治理工单）。
  - 复用 `DN.drawer(title, bodyNode)`、`DN.pill`、`selectOf`/`formRow`。
- 脚本版本 q100r82 → **q100r83**（强制浏览器加载更新模块）。

## QA 结论 —— ✅ 通过
服务器 E2E（pid 1063454）：gov-consumption.js?v=q100r83 **200**；workspace 引用 q100r83；js 含 `alertRulesDrawer`；预警规则端点 round-trip（建临时指标→建规则→列表=1→删规则→清理）通。✅

## 验收门禁自检
- [x] 功能可运行（518 全量回归绿）
- [x] 业务专家确认（规则增删 round-trip 通）
- [x] 新增用户可见功能（预警规则配置 UI，补全 R12 闭环）
- [x] 复用既有端点(R12)+组件(DN.*)，零后端改动
- [x] QA：js v83 200 + alertRulesDrawer + 规则 round-trip
- [x] 可回归（518 全量）
- [x] 部署成功可回滚（active/http302/pid1063454；纯前端，代码 revert）

## 下一轮
R15 候选（自行抉择）：资产消费热度→数据地图、指标-资产影响联动、MDM 11→4 controller 合并、横切 告警/审计 归一。
