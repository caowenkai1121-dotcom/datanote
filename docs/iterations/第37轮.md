# 第 37 轮 · MDM 剩余模块联动(去重/管家/层级/存活→黄金记录, 大版本)

> R21..R44 第十七轮。R26 接通了 MDM 质量/黄金记录/参考数据; 本轮把剩余 MDM 模块(匹配去重/数据管家/层级管理/存活性规则)接入下钻黄金记录, MDM 域全模块联动收尾。

## 改动(3 个 mdm-*.js)
- **mdm.js**: dedup 重复簇记录行加「查看记录」→ goldenrecord(editId,entityId); dedup 渲染器消费 ctx.entityId 深链; steward 工作台待复核草稿→goldenrecord(entityId)、待去重簇→dedup(entityId)。
- **mdm-hierarchy.js**: 树节点/关系明细行加「查看黄金记录(子/父记录)」→ goldenrecord(editId=childRecordId/parentRecordId, entityId)。
- **mdm-survivorship.js**: 属性配置卡页头加「查看该实体黄金记录」→ goldenrecord(entityId)。

## QA —— ✅ 通过(Playwright)
3 文件 node--check OK; 33 UI 绿; 本地运行。Playwright: MDM dedup/steward/hierarchy/survivorship 四模块全部渲染无「模块加载失败」。版本 mdm/mdm-hierarchy/mdm-survivorship q100r97。

## 验收门禁
- [x] 大版本(MDM 剩余 4 模块联动收尾)
- [x] 架构师全盘(MDM 全模块互通黄金记录枢纽)
- [x] 消除孤岛(去重/管家/层级/存活 从死胡同→可下钻黄金记录)
- [x] 功能可运行(node--check + 33UI + Playwright 四模块渲染)
- [x] 自续(R38 继续)

## 下一轮 R38
项目活动审计下钻(蓝图 R33 余项) + 趋于全盘验证收尾。
