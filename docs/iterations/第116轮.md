# 第116轮 —— 修建错任务(ODS抽数应落数据开发ODS层) + 思考/结果展示对齐参考图

> 业主(读 tp/1 2 3.png): ①思考过程参考图1(已处理Xs+可展开步骤) ②结果展示参考图2(结构化修改点+验证) ③【关键】"我说的ODS数据同步, 你建错任务了, 应该在图3"——把数据拉到 Doris 数仓都在『数据开发 ODS层』, 不是『数据同步』模块。

## 一、核心修正:ODS 抽数落『数据开发 ODS层』(图3), 不是『数据同步』模块
**根因(深研全仓确认)**: R115 误用 create_sync_job → 落 DnSyncJob(『数据同步』模块, dn_sync_job, 通用库到库)。但"拉数到 Doris 数仓 ODS"在『数据开发→ODS层』, 实体是 **DnSyncTask**(dn_sync_task), 经 ScriptService.saveSyncTask 保存 + HiveService 一键建表 + DataX 运行拉数。两者是不同表/不同模块。
**改**:
- **CreateOdsTableTool** 升级为 ODS 抽数正解: 自动补 datasourceId → HiveService 建 Doris 表 → **ScriptService.saveSyncTask 落 DnSyncTask**(taskName/sourceDsId/sourceDb/sourceTable/targetDb=ods/targetTable=ods_xx_df/syncMode/partitionField=dt), 按 taskName 去重。结果含 location=数据开发→ODS层 + 提示"可在该处点运行拉数"。描述明确"把表抽到Doris数仓ODS层用本工具, 不要用 create_sync_job"。
- **CreateSyncJobTool 描述**加注: "这是『数据同步』模块通用库到库; 抽到数仓ODS请改用 create_ods_table"。
- **PromptBuilder 协议第17条 模块归属**: 抽到ODS/拉数到数仓一律 create_ods_table; create_sync_job 不用于抽数仓ODS。

## 二、思考/结果展示对齐参考图(图1/图2)
- 进度态(图1风格): 运行中"天工司辰运行中·正在{动作}（第N步·已Xs）"动画(R106/R108已带计时)。
- 结果折叠头(图1"已处理5m52s"): processToggle 头改为"✅ 已处理 {时长} · 执行了 N 步 · 点击查看过程"(runRequest 计 t0→完成耗时传入, fmtDur 秒/分秒)。展开见逐步工具卡+💭思考(已有)。
- 结果体(图2结构化): 终答走 markdown 渲染(标题/列表/表格/`file:line`/可点链接, 已有)。

## 三、QA(本地连远程库 E2E + pymysql 验库)
- 用对工具: "xh_dms.t_sales_order 新建ODS任务,抽到ODS层" → agent 调 **create_ods_table**(非 create_sync_job) → wait_approval。
- 落对地方: 批准+resume → create_ods_table ok → **dn_sync_task 新增 id=8**(ods_xh_dms_t_sales_order_df, source_ds_id=2/dms, xh_dms.t_sales_order→ods.ods_xh_dms_t_sales_order_df, df, status=1) = 数据开发 ODS层可见; Doris 表已建。

## 验收门禁自检
- [x] ODS 抽数落『数据开发 ODS层』(DnSyncTask), 不再误建『数据同步』DnSyncJob
- [x] 工具/协议双重路由(create_ods_table 抽数仓 / create_sync_job 通用同步)
- [x] E2E+pymysql: 用对工具 + dn_sync_task 落库 id=8 + Doris 建表
- [x] 进度/结果展示对齐图1/图2(已处理Xs头 + 结构化markdown)
- [x] 编译+打包+重启+E2E 全绿; JS r116

## 备注
- 真正"拉数据"(运行 DataX)是长任务(可达600s), 不宜在 agent 单轮内联跑; 本工具建任务+建表到位, 数据拉取由用户在数据开发ODS层点『运行』或后续加 run_ods_sync 工具(异步)触发。
