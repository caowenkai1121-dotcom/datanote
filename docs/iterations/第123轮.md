# 第123轮 —— 两模块全量UI梳理(含弹窗/抽屉) + 删/改/新各落一项

> 业主: 全新全量梳理两模块(含弹窗/抽屉不留死角), 专业视角优化(新增/修改/删除)。

## 一、全量梳理(20文件并行精扫)
工作流逐文件登记每个 抽屉/弹窗/确认框/内联表单/面板/按钮/表格 + 状态分类, 再综合出 删/改/新+联动缺口。完整方案见 docs/治理与主数据-UI全量梳理与优化.md。核心结论:
- MDM 与治理 **100% 隔离**(mdm.js 10处跳转全域内)= 最大孤岛。
- 数据标准↔MDM 属性、两套质量引擎 互不联动。
- 多处 report-only/redundant 报表残留; gov-security 角色只读、脱敏 REPLACE/RANGE 固定无参; gov-standard 缺编辑。

## 二、本轮落地(删/改/新 各一, 均验证)
1. **删(冗余报表)**: gov-overview「表数/字段数/库数」三块磁贴 onClick 全跳 assets 落点重复 → 合并为一块「数据资产」(表X · 字段Y · 库Z)。6磁贴→4。
2. **改(真bug)**: gov-quality 失败聚焦『立即复跑』通过后**只改按钮文案不刷新列表**→ 列表与真实状态不一致。修: 复跑通过后重新扫描, 已通过规则从未通过清单移除。
3. **新+联动(破最大孤岛)**: mdm-quality 不合规黄金记录加「升级工单」→ POST /api/gov/health/issues(issueType=MDM, dimension=主数据质量, objectRef=mdm:{entityId}:{recordId}), 把主数据质量问题纳入**统一治理工单闭环**(分配/流转/关单/SLA/排行)。原 MDM 不合规只能域内「修复」, 现可进治理工单跟踪。

## 三、QA(E2E)
- 联动验证: POST 该端点 → 建治理工单 id=10 type=MDM status=OPEN objectRef=mdm:1:11, 出现在治理工单列表(dimension=主数据质量)。验毕清理。
- gov-overview/gov-quality/mdm-quality JS node --check 通过, 版本 r123, 打包重启全绿。

## 验收门禁自检
- [x] 全量梳理含弹窗/抽屉不留死角(20文件), 产出删/改/新+联动方案文档
- [x] 删冗余磁贴 / 改复跑刷新bug / 新增MDM质量→治理工单联动, 各验证
- [x] 联动E2E: MDM不合规→治理工单入统一闭环
- [x] 编译+打包+重启+E2E 全绿

## 下一步(按方案优先级)
- P0: 主数据↔数据标准 落标绑定(MDM属性引用数据元+词根命名校验); 黄金记录变更历史/版本diff/来源追溯。
- 续: gov-standard 数据元/词根/码表 编辑入口; gov-security 角色 CRUD; gov-lineage 影响面芯片可点; 续砍 gov-standard 重复趋势/简评。
