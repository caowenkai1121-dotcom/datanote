# 第73轮 —— AI Agent 子系统多智能体对抗审查 + 加固(修真问题)

> Ultracode: 用 Workflow 对 R56-R72 全 agent 子系统(~34 文件)做 5 维(安全/并发/NPE/正确性/降级)并行评审 → 每条发现 3 视角对抗验证(默认证伪)→ 36 agent / 1.7M token / 5.7 分钟, 确认真问题 19 条(去重约 12 独立)、证伪驳回 12 条。本轮修最关键的安全/并发/健壮性批次。

## 一、已修(7 类, 含 1 HIGH)
| # | 严重 | 问题 | 修法 |
|---|---|---|---|
| 1 | **HIGH** | resume 并发双执行: 已批未执行行无原子占用, 写工具重复 invoke(重复建表/同步任务/指标) | invoke 前 `UPDATE ... SET executed_at=now WHERE id=? AND status=approved AND executed_at IS NULL`, 仅 rows==1 抢到者执行, 0 则跳过(at-most-once) |
| 2 | MED(安全) | MEDIUM 会话级豁免无视 args: 批一次良性写后, 同会话同工具任意 args 自动放行(审批绕过) | **删除会话级豁免**, 改为 session+skill+完全相同 args 命中 approved 才放行; 每个不同写动作各审一次 |
| 3 | MED(安全) | resume 重放身份取调用者而非发起人, createdBy/owner 冒名漂移 | 写入用会话发起人 writeCtx(session.getUserName()), 审计 actor 记实际触发者, 二者分离 |
| 4 | MED(并发) | @Async learn 落默认无界队列(queue=MAX_VALUE), 慢调用堆积致内存增长 | 新增 AiAsyncConfig 有界执行器(core2/max4/queue100/DiscardOldest), learn 改 @Async("aiLearnExecutor") |
| 5 | MED(降级) | 嵌入单条文本无截断, 超长 comment 致整批 422 静默丢点 | embedChunk 内每条 cap 到 MAX_INPUT_CHARS=2000 |
| 6 | MED(降级) | 达步数 grace 收尾 + resume 收尾未判 LLM 错误串, 原始错误当终答 | 抽 isAiError(raw) 统一判定(含"AI 返回格式异常"), 三处共用 + 友好兜底 |
| 7 | MED(NPE) | tool.invoke 异常 message 为 null(NPE)时问题被掩盖 | 抽 msgOf(throwable): message 空退化为异常类名 + log.warn 留痕 |

## 二、QA 结论 —— ✅ 通过
- 全量回归 `mvn -o test` **527 / 0fail / 1skip** → BUILD SUCCESS。编译干净。
- **豁免移除 E2E**: 同会话先批 create_metric(A) 执行 → 再发 create_metric(B 不同 args)→ **仍 wait_approval, B 审批前未落库**(绕过已堵)。
- **happy-path + 幂等 E2E**: 批 B → resume → create_metric ok → B 落库 1 条; **二次 resume → B 仍 1 条**(原子占行幂等不重复)。

## 三、本轮暂缓(已记, 后续轮)
- #12 resume 端点对象级授权(需先开启鉴权; 当前 DATANOTE_PASSWORD 空=匿名单用户态, authz 无从区分, 开鉴权后补 currentUser==发起人/运维角色 + 禁自执行)。
- #6(correctness) 多步任务 resume 只重放写动作不续跑后续(R69 设计取舍, 单写动作场景正确)。
- nextSeq 基于 count 的并发重复 seq; 审批唯一键(session,step_seq,skill) vs 幂等键(session,skill,args)错位; memory dedup 并发/语义近似重复; fullReindex 全表 selectList 无分页。均 LOW, 列入后续。

## 验收门禁自检
- [x] 功能可运行(527 绿 + 3 项 E2E)
- [x] 安全增强(审批绕过堵死 + 重放身份不冒名 + 并发不重复执行)
- [x] 健壮性(嵌入截断/错误串统一/NPE 不掩盖/异步有界)
- [x] 无回归(happy-path 写 + 幂等 E2E 通过)
- [x] 可回滚(均外科改动 + 1 新配置类, git revert)

## 下一轮
- 向量列索引(7629列分批, 内存恒定); 余 LOW 项(唯一键对齐/nextSeq/memory 语义去重)。
