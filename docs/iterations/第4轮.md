# 第 4 轮 · 调度/执行域 结构重构（首个真·结构重构轮）

> 业主纠偏后转入**真·结构重构**：架构师深度主导、行为不变、消除重复、重划边界、改完该域"像新系统"。
> 本轮 = `01-重构总体方案` 的调度统一首步（架构师评 ROI5×可靠5×单轮可绞杀5，三域中第一名）。**51 条变更清单 / 估算 58 处 / 零表变更 / 零前端改动 / 行为一字不变**。
> 蓝图见 `docs/blueprints/R4-调度域重构蓝图.md`。

## 架构师主导设计（蓝图见 docs/blueprints/R4-调度域重构蓝图.md）
深读三大重复域（调度/元数据资产/横切去重）后，架构师推荐**调度域首发**：
- 原 `LocalSchedulerController`（24 端点，527 行）把"脚本×同步 × 本地×远程"的 8 种上下线逐字重复实现（DS 结果回写 90-97 与 204-211 逐字重复、置 ONLINE/OFFLINE 四处重复、本地上线+依赖刷新两处重复，合计约 150 行 copy-paste），且与 `SchedulerConfigController` **共享 `/api/scheduler` 路由**、职责（上下线/配置/执行/监控）全揉在一个巨类。

## 目标架构（职责单一化，绞杀式保行为）
```
表现层（按职责拆 4 门面，路由前缀仍 /api/scheduler，路径一字不变 → 前端零改动）
├── ScheduleLifecycleController   上下线 8 端点（仅委托）
├── ScheduleExecutionController   引擎/执行控制 10 端点（start-daily/stop/retry/rerun/
│                                  stop-task/pause-downstream/resume/backfill/batch-retry/cron-preview）
├── ScheduleMonitorController     监控查询 5 端点（today/run-log/logs/log-detail/task-runs）
└── SchedulerConfigController     调度配置（既有，未动）
领域层
└── ScheduleLifecycleService（新建）  统一脚本/同步两类上下线为 4 方法
       onlineRemote / offlineRemote / onlineLocal / offlineLocal  + 私有 applyDsResult*/
       setStatus*/buildSyncShellScript/createOnlineVersion/require*（消 150 行重复）
枚举 ScheduleTargetType { SCRIPT, SYNC }  消除 if(script)/if(sync) 分叉
```
**路由冲突解法（架构师定）**：旧端点物理迁移到新门面、路径字面量不变（前端零改动），删除旧 `LocalSchedulerController`——避免新旧同路径导致 Spring Ambiguous mapping。

## 团队验证
- **【Domain】有条件通过**（行为红线已遵守）：①本轮**不碰运行表合并**（DnRunInstance），否则 `SyncJobScheduler.depsReady` 回查会形成孤儿、毁 DAG —— 已严守，零表变更；②上下线副作用必保：DS 上线回写 ds* 元组、脚本本地上线建版本快照 + `refreshAllDependencies`、同步本地上线 `refreshAllDependencies`、本地下线不刷依赖 —— 单测逐条锁定；③不改 taskType 语义 —— 未碰执行/依赖服务。
- **【Product】有条件通过**（功能不丢）：纠正调研误判——config/sync-config/logs/log-detail 等被 **dev.html** 实际调用，不能丢；本轮所有旧路径原样保留（迁到新门面、路径不变），前端 workspace.html/dev.html **零改动**。

## 改动清单（51 条蓝图 / 实际触及 ~58 处）
| 动作 | 文件 |
|---|---|
| 新建枚举 | `ScheduleTargetType.java` |
| 新建领域服务（吸收 8 上下线 + 5 私有助手，消 150 行重复） | `ScheduleLifecycleService.java`（11 方法） |
| 新建 3 门面控制器（迁入 23 端点逻辑，逐字不变） | `ScheduleLifecycleController`(8) / `ScheduleExecutionController`(10) / `ScheduleMonitorController`(5) |
| 删除旧巨类 | `LocalSchedulerController.java`（-527 行） |
| 行为保持单测 | `ScheduleLifecycleServiceTest.java`（5 测） |
| 前端 | **零改动**（绞杀式，路径不变） |

## QA 结论 —— ✅ 通过
服务器 E2E（pid 863812）：

| 项 | 实测 |
|---|---|
| 24 端点可达性冒烟（不存在 id，验映射未丢） | ✅ **24/24 返回 200，0 个 404**（拆分后所有旧路径仍在） |
| cron-preview（验执行门面真工作） | ✅ 返回未来 3 次执行时间 `2026-06-07/08/09 02:00:00` |
| 真实脚本 local-online | ✅ status=online + **版本快照生成**(versionType=online, commitMsg=上线版本快照) —— 副作用保住 |
| 真实脚本 local-offline | ✅ status=offline |
| 清理 | ✅ 删脚本 + 版本行，残留 script=0 / version=0 |

- Domain 复核：上下线 4 类副作用（DS 回写/版本快照/依赖刷新/状态位）E2E 验证与重构前一致；运行表未动、taskType 未碰。✅
- 行为不变铁证：@SpringBootTest 上下文成功加载（无 Ambiguous mapping）+ 全量 507 绿 + 24 端点零 404 + 生命周期 E2E 通过。

## 遗留 / 下一轮
- 调度域后续轮（架构师排期）：统一本地调度引擎竞态（P0）、4 张运行表合并 DnRunInstance（含数据迁移）、Dolphin 降可选适配器、6 种上下线组合的前端按钮收敛。
- 其后：元数据/资产域（DataMapService 648 行在线/离线拆分）、横切告警/审计/偏好三处归一。

## 验收门禁自检
- [x] 行为不变（507 全量回归绿；@SpringBootTest 上下文加载成功=无 Ambiguous mapping）
- [x] Domain 确认（E2E 复核副作用一致）
- [x] 消除重复（150 行脚本/同步上下线 copy-paste 归一为 4 方法）
- [x] 职责单一（巨类拆 4 门面 + 1 领域服务）
- [x] 零表变更、零数据迁移
- [x] QA：24 端点 24/24 可达(0 个 404) + 上下线 E2E 通过
- [x] 可回归（5 行为单测 + 507 全量）
- [x] 部署成功可回滚（active/http302/pid863812；纯代码 revert 即回滚，无 SQL）
