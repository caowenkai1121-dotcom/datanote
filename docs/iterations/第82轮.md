# 第82轮 —— 圆角魔法数字 → 令牌(全站圆角系统统一)

> 承色彩令牌化收口, 本轮收"圆角魔法数字"。全站散落 100+ 处硬编码 `border-radius:4/6/8/10/3px`, 与令牌(--radius-sm 4 / --radius 6 / --radius-lg 10 / --radius-xl 14)脱钩, 卡片圆角 8/10 混用不一致。全量令牌化。

## 一、映射(就近归并到令牌)
| 硬编码 | → 令牌 | 效果 |
|---|---|---|
| 4px | `var(--radius-sm)` (4px) | 同值, 纯令牌化 |
| 6px | `var(--radius)` (6px) | 同值, 纯令牌化 |
| 3px | `var(--radius-sm)` (4px) | +1px 微调归并 |
| 8px | `var(--radius-lg)` (10px) | +2px, 卡片圆角统一为 10(更现代一致) |
| 10px | `var(--radius-lg)` (10px) | 同值, 纯令牌化 |
> 保留 12/9/2/16/20px(少量特殊场景, 非主流圆角)。8px 经抽样确认绝大多为卡片/面板/图标盒, 归 lg 安全。

## 二、改动量(279 处, python newline='' 保 LF)
- workspace.html 内联 167 / app.css 73 / 各 gov-*·ai-agent 余。
- 令牌定义(:root `--radius*`)未受影响(正则 `border-radius:Npx` 不匹配 `--radius:` 定义)。
- workspace.html diff 干净(167/167 行, 无爆炸)。

## 三、QA(Playwright 真机, 0 console error, 527 测试绿)
- 残留硬编码 `border-radius:4/6/8/10/3px` = **0**。
- `--radius-lg` 解析 10px / `--radius` 解析 6px; 系统页正常渲染。
- 全 JS 语法 OK; 527 回归绿。

## 四、设计系统"无魔法数字"进度
- 色彩(R78/80/81): 100% 令牌化 ✓
- 圆角(R82): 100% 令牌化 ✓(卡片统一 10px lg)
- 阴影: 已走 --shadow*/--dash-shadow(R75 全局)✓
- 间距(padding/margin/gap): 仍有魔法数字(下一轮)

## 验收门禁自检
- [x] 全站圆角令牌化(残留 0 实测)
- [x] 卡片圆角统一 10px(8/10 混用消除)
- [x] 令牌定义未误伤; 0 console error + 527 绿
- [x] LF 保留 diff 干净; 可回滚

## 下一轮
- 间距魔法数字(padding/margin/gap)逐步收敛(需引入间距阶梯令牌 --space-* 或就近归并, 较圆角更需谨慎因上下文敏感); 手写组件→DN.*; 空态补齐。
