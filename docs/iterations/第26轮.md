# 第 26 轮 · MDM 闭环联动(检测→修复 / 码表→属性 / 黄金记录深链, 大版本)

> R21..R44 第六轮。架构师:MDM 域多处闭环断裂——质量监控检测出问题不能修复、参考数据码表与建模属性不打通、黄金记录无深链入口。本轮接通(复用 R21 MDM 深链 __mdmCtx)。Workflow 3 代理。

## 改动(3 个 mdm-*.js)
- **mdm-quality.js**: 质量监控不合规黄金记录行加「修复」→ `mdmGoModule('goldenrecord',{editId,entityId})`,检测→修复闭环。
- **mdm.js**: ①goldenrecord 渲染器消费 `ctx.editId/ctx.entityId` 自动选中实体并打开该黄金记录编辑抽屉(复用 goldenForm); ②建模属性表单 ENUM/REFERENCE 类型加 `buildRefPicker`——拉 `/api/mdm/refdata/categories` + `/list` 引用码表码值填入枚举(码表→属性打通)。
- **mdm-refdata.js**: 码表加「在建模中引用」(refInModeling→跳建模+提示) + 码值行「明细」抽屉(detailDrawer) + 渲染器消费 `ctx.category` 深链定位类别。(注:并行代理 socket 中断遗漏两函数定义, 已手工补齐 refInModeling/detailDrawer)

## QA —— ✅ 通过(Playwright)
3 文件 node--check OK(含手工补齐的两函数); 33 UI 绿; 本地运行。Playwright 遍历 MDM quality/goldenrecord/modeling/refdata 四模块: 全部渲染无「模块加载失败」。版本 mdm/mdm-quality/mdm-refdata q100r93。

## 验收门禁
- [x] 大版本(MDM 3 模块闭环 + 码表打通属性)
- [x] 架构师全盘(MDM 检测→修复→引用 闭环)
- [x] 消除孤岛(质量监控/参考数据/黄金记录 互通)
- [x] 功能可运行(node--check + 33UI + Playwright 四模块渲染; 修复了并行代理中断的残缺edit)
- [x] 自续(R27 继续)

## 下一轮 R27
项目资产纳管双向 + 活动审计下钻 + 多项目对比下钻(蓝图 R33)。
