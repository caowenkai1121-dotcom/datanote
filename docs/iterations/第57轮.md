# 第57轮 —— AI Agent 深度融入全系统(M0 上下文协议基座 + M1 全局唤起器三件套)

> 业主要求"AI 必须深度融入系统"。七角色 Workflow(9-agent: 全域调研每模块集成触点→业务/产品深挖→架构整合→集成蓝图)产出 `docs/blueprints/AI-Agent-深度集成蓝图.md`。本轮交付深度集成 M0+M1：让 AI 从"独立 tab"变"无处不在的情境智能"。

## 本轮交付

### 【M0】上下文协议基座(后端 4 + 前端 2)
- `AgentContext` 加 `bizCtx`(业务情境 Map)；`AiAgentController.chat` body 改 `Map<String,Object>` 取 `ctx` 注入 AgentContext。
- `PromptBuilder.build` 增 `bizCtxText` 形参，注入「# 当前所在场景」段。
- `AiAgentService` 加 `buildBizCtxText`(把 route/db/table/jobId/ruleId... 渲染中文 + 提示 LLM 用 `[表:库.表]/[规则:#id]/[任务:#id]` 标记产出可点深链)，3 处 build 调用传入。
- `ai-agent.js`：`initAiAssistant(opts)` 接 prefill/ctx/autoSend；send 请求体带 `ctx:pendingCtx`。
- `workspace.html` `ROUTES.assistant.init` 读 `window.__navCtx` 透传 opts。

### 【M1】全局唤起器三件套 + 深链回链 + P0 入口
- header-right `#aiLauncherBtn`(🤖 AI 按钮，全路由常驻) + `window.openAiLauncher/openAiAgent`(按 currentRoute 拼默认情境) + `.ai-launcher-btn` 样式。
- **Ctrl+K 全局唤起**(非 project/dbsync、无输入焦点 → openAiLauncher)。
- `ai-agent.js` 深链回链：toolCard 解析工具结果 `_deeplink` 渲染「↗ 在X中查看」按钮；assistantBubble 把 `[表:库.表]/[规则:#id]/[任务:#id]` token 转可点 chip(XSS 安全：DN.h text + document.createTextNode，绝不 innerHTML 拼 LLM 文本)→ navigateTo 跳模块。
- 数据地图表详情「🤖 AI 分析此表」P0 情境入口 `dmAskAi()`(传 {route:catalog,db,table} + autoSend)。

### 6 个已核实只读工具(全 readOnly/LOW，表向工具带 _deeplink)
asset_detail→AssetDetailService.assetDetail / table_profile→profile / lineage_trace→LineageEdgeService.trace / lineage_graph→graph / health_trend→HealthScoreService.trend / issue_stats→IssueService.stats+leaderboard。+ AgentArgs 共享助手(参数抽取 + _deeplink 构造)。

## 【QA】结论 —— ✅ 通过(真 LLM E2E)
- 全量回归：`mvn -o test` **527 run / 0 fail / 0 error / 1 skip** → BUILD SUCCESS。
- 重启 18s，`/api/ai/agent/tools` → **count=9**(3+6)。
- **情境 E2E①**：dmAskAi 同款 ctx 发起「分析 ods.dwd_order」→ agent 自主串 asset_detail→lineage_impact→lineage_trace→table_profile→FINAL；终答含深链 chip `[表:ods.dwd_order]`；table_profile 对不存在表优雅 error 不崩。
- **情境 E2E② (bizCtx 独立生效铁证)**：message「分析我当前正在看的这张表的下游影响面」**不含表名**，agent 仅凭「# 当前所在场景」注入即用 `{"db":"ods","table":"dwd_order"}` 调 lineage_impact。证明上下文协议端到端打通。

## 验收门禁自检
- [x] 功能可运行(527 全量绿 + 真 LLM 情境 E2E ×2)
- [x] 业务/产品确认(场景: 表详情一键 AI 体检; 全局 Ctrl+K 随处唤起)
- [x] 深度融入(全局唤起器全路由可达 + 情境入口 + bizCtx 注入 + 深链回链, 非孤立 tab)
- [x] 不重复(复用 AiAssistService.chat/navigateTo/__navCtx/DN.*；旧 AI 触点联动不并入；零新端点于 AiAssistController)
- [x] 零新第三方依赖；保 NUL/深链/437 端点零改
- [x] 安全(6 工具全只读 LOW；XSS：LLM 文本走 text 节点不 innerHTML)
- [x] 可回滚(新增工具 + 外科增量；git revert 即复原)

## 下一轮
深集成 M2：治理域 P0/P1 情境入口(gov-quality 规则行「AI根因」/gov-lineage 中心表「AI影响评估」/资产详情「问AI」) + 治理只读工具补全(sensitive_scan/classification_audit/issue_by_rule/issue_leaderboard/health_compute/standard_*)。再 M3 同步运维开发域。
