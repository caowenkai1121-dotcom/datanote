# 第 38 轮 · 项目活动审计下钻(大版本)

> R21..R44 第十八轮。项目详情「活动」是 HTTP 审计式只读列表(路径/方法/状态), 单条无去处。本轮按审计路径识别所属模块, 提供「前往」下钻。

## 改动(workspace.html)
- `projRenderActs` 活动表加「前往」列 → `projActRouteOf(path)` 按 `/api/*` 前缀映射模块(sync-job/dbsync→数据同步、quality→数据质量、consumption/gov→数据治理、metric→指标、mdm→主数据、metadata→数据地图、scheduler/task-execution→数据运维、rbac/system/auth→系统管理), navigateTo 直达; 未识别路径不显示链接。

## QA —— ✅ 通过(Playwright)
33 UI 绿; 本地运行。Playwright: `projActRouteOf` 映射正确(/sync-job→dbsync、/quality→quality、/mdm→mdm、/metadata→catalog、未知→null)。

## 验收门禁
- [x] 大版本(活动审计→各模块下钻)
- [x] 架构师全盘(审计路径→模块路由映射)
- [x] 消除孤岛(项目活动从只读→可前往相关模块)
- [x] 功能可运行(33UI + Playwright 映射验证)
- [x] 自续(R39 继续)

## 下一轮 R39
继续收尾 + 趋于 R40 全盘验证。
