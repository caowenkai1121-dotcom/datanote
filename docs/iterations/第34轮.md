# 第 34 轮 · 伪需求/死代码清理(精简 API 与认知噪音, 大版本)

> R21..R44 第十四轮。架构师/业务专家普查标记的"伪需求/冗余/死代码", 本轮安全清理(均经 0 调用方 + 0 测试引用 核实)。

## 改动
- **workspace.html**: 删死函数 `dmSaveFieldMeta`(census 确认无任何调用方, 且实现只弹 msgBox 不落库——伪保存)。
- **MetadataController.java**: 删冗余 REST 出口 `/api/metadata/all-tables`(前端零调用, getAllTablesSummary 仍供内部复用)+ `/api/metadata/favorite/check`(前端零调用, 收藏态已由 table-detail 内联返回)。服务方法保留, 仅去 HTTP 暴露。

## QA —— ✅ 通过
编译通过; **518 全量回归绿**(删除均无调用方, 行为不变); 本地运行。Playwright/curl 验: `/all-tables`→**404**、`/favorite/check`→**404**(已删除); `/databases`→200、`/table-detail`→200(保留功能无回归); workspace dmSaveFieldMeta 函数已移除(仅注释提及)。

## 验收门禁
- [x] 大版本(前后端伪需求/死代码清理, 精简 API 面)
- [x] 架构师/业务专家(基于普查 39 孤岛中的伪需求项, 0 调用方核实)
- [x] 消除伪需求(死函数 + 冗余端点, 减少认知噪音)
- [x] 功能可运行(编译 + 518全量 + 删除端点404/保留端点200)
- [x] 自续(R35 继续)

## 下一轮 R35
继续蓝图收尾(主题域挂资产后端字段/采集明细等) + 巩固联动闭环。
