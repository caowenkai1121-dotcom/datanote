# 第60轮 —— 向量库推到用户面前(语义搜索 UI + REST 端点 + DRY 抽取)

> 让向量库从"agent 内部工具"变"用户可直接用的系统能力"。数据地图加「🧠 语义搜索」,中文同义召回(如"支付"命中交易/订单/付款),点结果直达表详情。

## 本轮交付
### 后端
- **SemanticSearchService**(抽取语义检索核心,供 工具/REST端点/前端 共用,DRY):向量库+embedding 可用→向量召回(payload 拍平为 {kind,db,name,title,score});否则关键字 LIKE 降级。永不抛异常。
- **SemanticSearchTool** 重构为委托 SemanticSearchService(消重)。
- **StoreController** 加 `GET /api/ai/store/search?q=&kind=&limit=`(直连向量,不走 agent LLM 循环,UI 用)。

### 前端(workspace.html 数据地图)
- 搜索框旁加「🧠 语义搜索」按钮(紫蓝渐变)→ `dmSemanticSearch()` 调 `/api/ai/store/search` → 渲染结果(kind 彩签 + 库.表 + 描述 + score)→ 点击 table 项 `dmOpenTable(db,name)` 直达表详情。引擎标记(向量语义/关键字降级)+ 空/错态。纯 DN.* 组件,XSS 安全(DN.h text/DN.esc)。

## 【QA】结论 —— ✅ 通过(真 E2E)
- 全量回归 `mvn -o test` **527 / 0fail / 1skip** → BUILD SUCCESS。
- **REST 端点 E2E**: `GET /api/ai/store/search?q=用户支付交易` → **engine=vector, count=5**,召回 t_saobei_pay_info(0.663)/t_market_payment_account(0.661)/pay_center.payment_info(0.656)/t_customer_balance(0.648) —— 中文跨语言语义召回英文表名,直连端点(非 agent)。
- workspace.html 已含 `dmSemanticSearch` + `🧠 语义搜索` 按钮。
- semantic_search 工具仍走同一 SemanticSearchService(DRY,行为不变)。

## 验收门禁自检
- [x] 功能可运行(527 全量绿 + REST 端点向量召回 + UI 按钮)
- [x] 深度融入(向量库直达数据地图用户界面,非仅 agent 内部)
- [x] 不重复(SemanticSearchService 单一实现, 工具/端点/前端共用)
- [x] 降级优先(端点/工具/前端 向量不可用均关键字兜底,永不报错)
- [x] 安全(DN.h text 防 XSS; 端点只读)
- [x] 可回滚(新增 service+端点+前端按钮+函数,外科增量)

## 下一轮
- 列索引(7629 列, 分批限流); 术语/指标种子数据后语义搜按 kind 过滤。
- 血缘图谱视图直吃 graph_neighbors(图库渲染); 治理血缘/资产视图加图工具情境入口。
- RAG: agent 循环前向量召回相关资产注入 prompt(自动 grounding)。
