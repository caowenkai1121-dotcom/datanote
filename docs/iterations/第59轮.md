# 第59轮 —— 向量/图库深化 M2(向量索引扩术语+指标 + 增量同步钩子)

> 业主配百炼 embedding key 后(第58轮 7a37ecb 向量全链路打通, 429表入Qdrant), 本轮深化: 向量索引覆盖更多实体类型 + 采集后自动增量同步, 让两库随系统数据自维护。

## 本轮交付
### 向量索引扩展(VectorIndexService)
- 新增 `indexGlossary()`(dn_glossary_term → kind=glossary, 文本「业务术语 {term}(别名:{alias}) 定义:{definition} 分类:{category}」)
- 新增 `indexMetric()`(dn_metric → kind=metric, 文本「指标 {metricName}({metricCode}) 说明:{description} 口径:{calcFormula}」)
- `fullReindex()` = indexTables + indexGlossary + indexMetric, 各自 try/catch 隔离。point id = UUID(kind:id) 稳定更新。
- 字段均 Grep 核实(DnGlossaryTerm: term/alias/definition/category; DnMetric: metricName/metricCode/description/calcFormula)。

### 增量同步钩子(MetadataCrawlerService)
- `scheduledCrawl()`(每日01:00)采集后挂钩 `graphMirrorService.fullSync()` + `vectorIndexService.fullReindex()`, 各自 try/catch, 失败不影响采集主流程。让图库/向量库随每日元数据采集自动刷新。

## 【QA】结论 —— ✅ 通过(真 E2E)
- 全量回归 `mvn -o test` **527 / 0fail / 1skip** → BUILD SUCCESS。
- 现状: dn_glossary_term=0 / dn_metric=0(无种子数据)→ 两索引正确返 0(非 bug); dn_table_meta=429。
- **术语路径实证**: 插入测试术语「月活跃用户/MAU」→ reindex **vectorPoints 429→430**, 日志「术语索引完成: 1 点」→ 证 indexGlossary 路径生效(indexMetric 同构代码)。
- **清理无残留**: 删测试行 + 删对应 Qdrant 点(UUID(glossary:1)=6704316f… 删成功 200, 反证 pointId 方案与 Java UUID.nameUUIDFromBytes 一致), points 回 429, glossary 行=0。
- 既有: graph_impact engine=graph; semantic_search engine=vector(429表跨语言语义召回)不变。

## 验收门禁自检
- [x] 功能可运行(527 全量绿 + 术语索引真 E2E + 清理无残留)
- [x] 深化(向量覆盖表/术语/指标三类; 采集后自动增量同步两库)
- [x] 不重复/降级优先(复用 VectorIndexService/GraphMirrorService; 钩子 try/catch 不影响采集; 向量/embedding 不可用 fullReindex no-op)
- [x] 字段真实(Grep 核实, 无臆造)
- [x] 可回滚(VectorIndexService 扩方法 + MetadataCrawler 注入2服务+尾部钩子, git revert 即复原)

## 下一轮
- 列索引(dn_column_meta 7629 列, 嵌入开销大需分批/限流或按需, 留档)。
- 前端: 全局/数据地图 语义搜索框(调 semantic_search); 血缘图谱视图直吃 graph_neighbors; 治理血缘/资产视图加图/向量情境入口。
- RAG: 向量召回治理文档/术语注入 agent prompt。
