# 第 36 轮 · 资产详情显示/设置所属主题域(主题域读侧闭环, 大版本)

> R21..R44 第十六轮。R35 在数据地图为表设主题域(写); 本轮在治理「资产详情」抽屉显示并可设置主题域(读+写), 让主题域归属在资产视角可见可改, 闭合 R35。

## 改动(gov-assets.js, 纯前端复用 R35 端点)
- `openAssetDetail` 抽屉概要区(R22 治理联动按钮排下方)新增 `renderSubjectRow(db,table)`:
  - 并发 GET `/api/metadata/table/subject`(当前 subjectId)+ GET `/api/gov/subject`(列表), id→name 映射显示"所属主题域: X [设置]"。
  - 「设置」→ 内联下拉(主题域列表+取消归属)→ POST `/api/metadata/table/set-subject` 保存, toast + 刷新。
  - 独立 try/catch, 端点失败不影响抽屉其它小节。

## QA —— ✅ 通过(Playwright)
gov-assets node--check OK; 33 UI 绿; 本地运行。Playwright: 深链开资产详情, 抽屉含「所属主题域」行 ✓; R22 治理联动按钮无回归 ✓; 模块渲染无报错。版本 gov-assets q100r96。

## 验收门禁
- [x] 大版本(资产详情主题域读+写, 闭合 R35)
- [x] 架构师全盘(主题域归属 数据地图设 + 资产详情设/看, 双入口一致)
- [x] 消除孤岛(主题域归属在资产视角可见可改)
- [x] 功能可运行(node--check + 33UI + Playwright 抽屉主题域行)
- [x] 自续(R37 继续)

## 下一轮 R37
继续蓝图收尾(基线/活动下钻等) + 趋于全盘验证。
