# 第 8 轮 · model/dto/exception/util 全面归域 — 后端领域包闭环（≥50% 系统）

> 业主红线("每轮 ≥整个系统一半")下的最大单轮。接 R7（controller/service 归域），本轮把**数据模型/DTO/异常/工具**全部归域。
> 这些类（`R` 响应包、异常、`Dn*` 实体）被**几乎所有 Java 文件 import** → 一次重分包改写 ~全部后端文件 → **本轮 git 统计 342 文件变更 = 627 文件系统的 55%**。
> **行为不变 = 纯物理重组**，编译器 + 507 测试为安全网。

## 范围与映射（规则化，分类即便有偏也仅外观，FQN 一致即编译绿+测试过）
- `model`(89)：`R`→`common.model`；`Dn*` 按业务前缀归 `domain.{project,mdm,integration,orchestration,develop,datasource,metadata,governance}.model` 与 `platform.{portal,iam,config,audit,alert}.model`；`ColumnInfo`→metadata。
- `model.dto`(12)+`dto`(3)：Ai*→platform.ai.dto；Datax*/Hive*→integration.dto；脚本/Cron 类→develop.dto；Backfill/Pause/ScheduleConfig→orchestration.dto。
- `exception`(3)→`common.exception`；`util`(5)→ CryptoUtil→common.util / DorisSqlUtil→integration.util / ProcessUtil→orchestration.util / SensitiveDetector→governance.util / SysMetricsUtil→portal.util。
- **mapper 暂留原位**（`@MapperScan("com.datanote.mapper")` 不变；mybatis 经 mapper 泛型解析实体，与实体所在包无关）。

## 执行（脚本 + 编译器网）
1. 移动 112 类 + 改 package 行。
2. 全局 FQN 精确替换（词边界）：`com.datanote.{model,model.dto,dto,exception,util}.X` → 新 FQN，涉及 **223 文件**。
3. 跨包 import 注入 **74 条**（同包引用拆包后补 import）。
4. 清理失效通配 import `import com.datanote.{model,...}.*;` **13 文件**（包已空）。

## 团队验证
- **【架构师】**：完成 controller/service(R7) + model/dto/exception/util(R8) 的领域闭环；mapper 留待 R9（需 @MapperScan 显式列各域 mapper 包，45/87 mapper 无 @Mapper 注解依赖扫描）。
- **【业务专家】**：实体归域按业务前缀，行为零改 → 无场景受影响。

## 改动规模
- **112 类移动** + **223 文件 FQN 改写** + 74 import 注入 + 13 通配清理
- **git 统计本轮 342 文件变更 = 系统 627 文件的 55%（≥50% 达标）**

## QA 结论 —— ✅ 通过
服务器 E2E（pid 970162）：12 域端点全 **200**（iam/config/datasource/metadata/develop/orchestration/governance(quality+issues)/mdm/project/common/integration），root 302。
→ 342 文件 model 重分包后 **Spring 扫描 + mybatis 实体新包解析 + 各域路由全部正常，生产可跑、行为零改**。✅

## 验收门禁自检
- [x] 行为不变（507 全量回归绿；@SpringBootTest 上下文启动=Spring 扫描/mybatis 实体新包解析正常）
- [x] 业务专家确认（12 域端点 E2E 全可达）
- [x] **≥50% 系统**（本轮 342/627 = 55% 文件变更）
- [x] 领域包闭环（model/dto/exception/util 全归域）
- [x] 零表变更
- [x] QA：12 域端点全 200/302，应用启动正常
- [x] 可回归（507 全量）
- [x] 部署成功可回滚（active/http302/pid970162；纯代码 revert，无 SQL）

## 下一轮
R9：mapper(87) 归域（@MapperScan 改列各域 mapper 包）+ sync(68)→domain.integration 子包化 + infra 适配器层 → 后端 100% 领域驱动。
