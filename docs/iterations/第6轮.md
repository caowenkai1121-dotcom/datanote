# 第 6 轮 · 前端整体重构 — 全新设计系统 + CSS 外置 + 砍孤儿（UI设计师主导）

> 业主红线升级后首个**前端大重构**：UI设计师 + 架构师 + 业务专家深度主导（6-agent 工作流测绘+设计）。
> 覆盖**全部 10 个一级模块的视觉**（≥50% 功能块）；workspace.html 内联 3310 行 CSS 外置 + 全新设计系统 + 删 6990 行孤儿 dev.html（大幅代码调整）。**行为不变**：不改任何 class/id/DOM/事件/路由/端点。

## 设计产出（docs/_fe_raw 工作流原件 → docs/设计规范.md）
- **【UI设计师】全新设计系统 "Slate & Indigo / 石墨靛蓝"**：
  - 品牌主色 Ant 天蓝 `#1890ff` → **石墨靛蓝 `#3457d5`**（hover `#4f6ef0`），全站经 `var(--primary)` 级联换新（含治理/MDM，因 gov-modern.css 用 var 自动继承）。
  - 重定义 :root **令牌值不改名**（保证 19771 行内联 `style=...var(--xxx)` 全部级联生效）；补全缺失令牌 `--bg-main/--text-secondary/--primary-bg`；圆角 4 档(4/6/10/14)、阴影 3 档、焦点环 `--ring`。
  - 状态色降刺眼 + **WCAG AA**（正文/按钮/药丸对比度 ≥4.5:1）。
  - **暗色主题** `[data-theme=dark]` + `prefers-color-scheme`（预留，开关后续接）。
  - 组件全新视觉**复用现有类名**(.dev-header/.header-tab/.btn/.left-panel/树/表格/卡片/药丸/抽屉/表单)，零 DOM 改动。
- **【架构师】**：内联 `<style>`(L8-3317, 3310行) 外置；CSS 资源版本统一 `?v=q100r80`；dev.html 下线；外壳/布局结构注释化分区（DOM 不变）。
- **【业务专家】红线**：保全状态态(加载/空/错误/边界)、信息密度(表格行高/排序/sticky表头/影响面)、危险操作可见(删除红/暂停/阻断)、状态色语义、实时(dbsync SSE 日志/自动刷新/aria-live)、无障碍(focus-visible/Esc/aria)、低频入口可见、骨架占位防 CLS。

## 本轮改动（行为不变，CSS/资源层）
| 动作 | 文件 |
|---|---|
| 内联 3310 行 CSS 外置 + 追加全新设计系统覆盖层 | 新 `css/app.css`(124KB / ~3826 行 = 3310 legacy + 设计令牌 + 组件) |
| 内联 `<style>` 块替换为 `<link app.css>` + 全站 `?v=` 统一 q100r80 | `workspace.html`(1023KB→926KB，字节级处理保 4 个 NUL) |
| 砍孤儿（0 引用，已被 workspace 取代） | 删 `dev.html`（-6990 行 / -272KB） |
| 设计规范落档 | 新 `docs/设计规范.md`(191 行) |
| 测试随迁（CSS 选择器移到 app.css） | `WorkspaceDbSyncUiTest`（compact 规则改读 app.css） |

> 可靠性保障（Playwright MCP 当前不可用，不依赖截图）：① 只改令牌值+组件样式，**保 DOM/class/id/事件/路由/端点全不变** → JS 行为零影响；② 设计令牌经 CSS 变量级联，gov/mdm 自动继承；③ 字节级处理保住 workspace.html 的 4 个 NUL；④ 507 全量回归绿（含断言 workspace 内容的 web UI 测试）；⑤ 部署后 E2E 验资源加载 + 端点。

## QA 结论 —— ✅ 通过
服务器 E2E（pid 926028）：

| 项 | 实测 |
|---|---|
| app.css 加载 | ✅ 200，含新主色 `#3457d5` |
| gov-modern.css | ✅ 200（版本 q100r80） |
| workspace.html | ✅ 200，引用 `css/app.css`，内联 `<style>` 由 3+ 降为 2（block1 已外置） |
| dev.html | ✅ **404**（孤儿已删） |
| 端点存活 | ✅ quality/metadata/scheduler 全 200，root 302 |

- 业务专家复核：全部 10 模块经 CSS 令牌级联换新视觉（石墨靛蓝），DOM/事件/路由/端点零改动 → 功能与状态态全保留。✅
- 限制说明：Playwright MCP 不可用，未做像素级截图；以"资源加载+新色生效+行为零改+507回归+端点E2E"作可靠性佐证。

## 遗留 / 下一轮
- 架构师建议续：app.css 进一步拆 tokens/base/shell/views 分层；内联 onclick(301处)事件委托化；JS 按域拆 ES Module；暗色主题开关接 UI。
- 后端结构续：横切 告警/审计/偏好 三处归一；调度运行表 4 合 1。

## 验收门禁自检
- [x] 行为不变（507 全量回归绿；零 DOM/class/id/事件/路由/端点改动）
- [x] 业务专家确认（E2E 复核 10 模块换新、功能态保留）
- [x] 覆盖≥50%功能块（全部 10 模块视觉换新）+ 大幅代码调整（外置 3310 行 + 删 6990 行 + 新设计系统）
- [x] UI 符合统一设计规范（设计令牌体系 + WCAG AA）
- [x] 状态/密度/危险操作/实时/无障碍 红线（保 DOM 即保功能态）
- [x] QA：app.css 200/新色生效 + dev.html 404 + 端点全 200
- [x] 可回归（507 全量 + CSS 括号校验）
- [x] 部署成功可回滚（active/http302/pid926028；纯前端资源，回滚=代码 revert）
