# 第 20 轮 · 全局视觉升级铺到治理/MDM 全模块（可见大重构续）

> 接 R19(驾驶舱首页 + redesign.css 视觉层)。R19 的色边玻璃卡 / 升级阴影 / 悬浮质感只作用于首页 `#homeCockpit`。本轮**一处作用域扩展 → 全部 18 个治理/MDM 模块页同时焕新**。

## 角色
- **【Architect】** R19 的 `--dash-*` 令牌 + `.gov-stat`/`.gov-card` 质感升级原限定 `#homeCockpit`。治理(11 模块)与 MDM(7 模块)页内容容器是 `#govModuleContent`/`#mdmModuleContent`，且渲染同样用 `.gov-stat`/`.gov-card` 类 → 把 redesign.css 中 `#homeCockpit` 作用域扩展为 `:is(#homeCockpit,#govModuleContent,#mdmModuleContent)`，**零新代码、一处改动、全模块生效**。
- **【Design】** 升级后所有治理/MDM 页的 KPI 卡获得：左侧语义色条(ok 绿/warn 橙/err 红/品牌蓝)、玻璃阴影、悬浮上浮、等宽大数字；卡片获得圆角+阴影+hover。与首页驾驶舱视觉统一。
- **【QA】** Playwright 截图实证。

## 改动
- **css/redesign.css**：20 处 `#homeCockpit` → `:is(#homeCockpit,#govModuleContent,#mdmModuleContent)`(令牌块 + `.gov-stat`/`.gov-card` 升级规则)。注释平衡校验通过。
- **workspace.html**：redesign.css 版本 q100r87 → **q100r88**。

## QA 结论 —— ✅ 通过（Playwright 截图实证）
本地运行。访问「数据治理 → 治理总览」：KPI 卡呈**色边玻璃质感**(治理健康分 28.1 红边 / 表数 424 蓝边 / 字段数 7,581 / 库数 14 / 质量分 0% 红边 / 待办工单 0 绿边)，真实数据；治理健康 gauge+雷达卡升级样式。一处 CSS 改动 → 治理 11 + MDM 7 共 18 个模块页视觉统一焕新。✅

## 验收门禁自检
- [x] 功能可运行（518 + 33 UI 绿，纯 CSS 叠加不改结构/逻辑）
- [x] **用户一眼可见**（治理/MDM 全部模块页焕新，与首页统一）
- [x] 不破坏现有页（redesign.css 后加载叠加，仅增强 .gov-* 外观）
- [x] 高杠杆（1 文件 20 处替换 → 18 页生效）
- [x] QA：Playwright 截图实证治理页色边卡+真实数据
- [x] 本地运行验证（免上传）

## 下一轮
R21：Workflow 逐模块加可见功能widget + 首页/治理卡片下钻联动 + MDM 页视觉抽检。
