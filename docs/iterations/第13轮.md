# 第 13 轮 · 数据集 / 数据产品 — 精选 SQL 注册为受治理可复用查询（新增功能）

> 接消费层(R10-12)。新增**数据集/数据产品**：把一段精选 SQL 注册为命名、可复用、**受治理(脱敏+审计)**的查询，对外提供稳定数据出口。
> 业务红线（遵守）：复用 `HiveService` 执行 + `MaskingService`/`SqlMaskRewriter` 脱敏改写(**fail-closed**) + 消费审计；只读 SELECT；只消费 status=1 启用数据集——**不另造查询通道、不绕治理**。

## 新增内容（domain.consumption）
- **表 sql/68_dataset.sql（幂等）**：`dn_dataset`（dataset_code 唯一, dataset_name, query_sql, default_db, owner, status, tags）。
- **模型/Mapper**：DnDataset / DnDatasetMapper（在 consumption.mapper，已入 @MapperScan）。
- **DatasetService**：CRUD + `query(id, consumer)` —— 校验只读(`validateReadOnly` 纯函数,去注释后仅 SELECT/WITH)→ `MaskingService.resolveColumnMasks/resolveRowFilters` + `SqlMaskRewriter.rewrite`(脱敏改写,失败 fail-closed 拒绝执行)→ `HiveService.executeSQL` 执行 → 写 `dn_consumption_log`(targetType=DATASET) 审计。
- **DatasetController（/api/consumption/dataset，5 端点）**：list / {id} / save / {id}(DELETE) / {id}/query。
- **前端**：gov-consumption.js 加「数据集/数据产品」卡——列表(名称/编码/负责人/状态)+ 运行(抽屉展示脱敏后结果表)+ 删除 + 新建数据集抽屉表单。

## QA 结论 —— ✅ 通过
服务器 E2E（pid 1055427）：注册数据集(r13_e2e, SELECT 7 AS n, status=1) → list=1 → **query 返回 columns=[n] rows=[[7]] rowCount=1**(经 HiveService+脱敏执行) → **写 SQL(DELETE)注册被拒**(code=-1「仅允许只读 SELECT/WITH」) → **DATASET 审计日志 1 条** → MySQL 清理。
→ 精选SQL→可复用、只读守卫、受脱敏、带审计的查询，**数据产品成立、不绕治理。** ✅

## 验收门禁自检
- [x] 功能可运行（518 全量回归绿，+3 只读校验单测）
- [x] 业务专家确认（注册/查询/只读拒写/审计 全过）
- [x] 新增真实业务功能（数据产品:精选SQL→可复用受治理查询）
- [x] 不绕治理（复用 HiveService+MaskingService 脱敏 fail-closed + 消费审计 + 只读校验）
- [x] 契合领域驱动结构（domain.consumption）
- [x] QA：query rows=[[7]] + 写SQL被拒 + DATASET审计
- [x] 可回归（3 单测 + 518 全量）
- [x] 部署成功可回滚（active/http302/pid1055427；表幂等、代码 revert）

## 下一轮
R14 候选（自行抉择）：数据集→指标/血缘联动、预警规则前端配置 UI、资产消费热度榜进数据地图、MDM 11→4 controller 合并。
