# 第61轮 —— AI Agent 完美融入全系统(RAG 自动 grounding + 全域工具覆盖 + 治理情境入口)

> 业主令"agent 完美融入整个系统, 架构师深度参与"。七角色 Workflow(5-agent 审计现状缺口→架构师双视角整合→执行蓝图)产出四位一体方案(RAG + 全工具覆盖 + 入口矩阵 + 回链闭环, 复用既有协议链不另起炉灶)。本轮交付前三支柱核心。

## 一、RAG 自动 grounding(第一支柱)
- `AiAgentService` 注入 `SemanticSearchService`, 主循环**前**调 `buildRagText(userMessage)` 向量召回 topK=5 相关资产(仅 engine=vector 才注入, 关键字噪音大跳过), 字符上限 800 控 token, 异常返 null 降级。
- `PromptBuilder.build` 加第 6 参 `ragText`, 在「# 当前所在场景」后注入「# 相关数据资产(语义召回, 供参考, 需工具核实后再断言)」段(幻觉护栏措辞)。
- 效果: agent 首轮即得候选表、直接产 [表:库.表] 深链 token、省一次 semantic_search step。向量不可用→ragText=null→主循环行为完全不变。

## 二、全域工具覆盖(第二支柱)+10 只读工具(工具数 13→23)
全部比照 AssetDetailTool 样板(@Component+AgentArgs 取参+readOnly/LOW+try/catch 永返 ok+回退+_deeplink), **只映射 Grep 核实存在的真实 Service 方法**, AiToolRegistry 自动收集零注册:
- 同步: `sync_job_detail`(SyncJobService.getById)、`sync_job_checkpoints`(getCheckpoints)
- 调度: `sched_run_log`(TaskSchedulerService.getRunLog)、`sched_today_status`(getTodayStatus)、`sched_downstream_tree`(TaskDependencyService.getDownstreamTree)
- 开发: `sql_parse_tables`(parseSQLTables)、`sql_explain`(AiAssistService.explainSql)、`sql_optimize`(optimizeSql)
- 项目: `project_overview`(ProjectOverviewService.overview)
- 指标: `metric_detail`(MetricValueService.latest+history+inputQuality)
- AgentArgs 增 `longOrCtx/strOrCtx`(缺参从 AgentContext.bizCtx 回退)+ `dbsyncLink/metricLink`; buildBizCtxText 补 taskId/taskType/projectId 渲染。

## 三、治理情境入口(第三支柱, P0)
统一 `window.dnAskAi(prefill, ctx)`(workspace.html)→ navigateTo('assistant',{agentPrefill,agentCtx,agentAutoSend:true}):
- gov-lineage.js `tableActionMenu` acts 加「🤖AI」(一处覆盖中心表+影响面表)
- gov-assets.js `renderGovLinks` links 加「🤖AI分析」(openAssetDetail 全资产覆盖)
- gov-quality.js 失败行 `_op` 加「🤖AI诊断」(传 ruleId/db/table)
- gov-*.js 版本 bump q100r105(破缓存)。

## 【QA】结论 —— ✅ 通过(真 E2E)
- 全量回归 `mvn -o test` **527 / 0fail / 1skip** → BUILD SUCCESS。
- 工具数 **23**(/api/ai/agent/tools 全列出, AiToolRegistry 零注册自动发现)。
- **新工具 E2E**: agent 自主调 `sched_today_status` → ok → FINAL。
- gov-quality.js?v=q100r105 含「🤖AI诊断」; workspace.html 含 dnAskAi。
- RAG 激活(buildRagText 循环前注入, 未破坏主流程; 向量可用时注入语义召回段)。

## 验收门禁自检
- [x] 功能可运行(527 全量绿 + 23 工具 + 新工具 E2E + 治理入口服务)
- [x] 完美融入(RAG 自动 grounding + 全域工具覆盖 + 治理处处可唤)
- [x] 不臆造(10 工具方法全 Grep 核实真实存在)
- [x] 降级优先(工具永返 ok+回退; RAG 向量不可用跳过; ctx 缺参回退)
- [x] 不重复/外科增量(复用 AiTool/AiToolRegistry/SemanticSearchService/navigateTo/dnAskAi; gov 入口就近嵌锚点不重写 render)
- [x] 安全(只读工具; 写禁区不变)
- [x] 可回滚(新增工具/RAG参/入口按钮, git revert 即复原)

## 下一轮
- 剩余 P0 入口: operations 调度失败行(schedAskAi)、dbsync 任务详情(dbsyncAskAi)、develop 转交智能体(develAskAgent, workspace.html 深处)。
- P1: gov-health/metrics/project 入口 + 首页今日 AI 简报。
- 向量深链: semantic_search 结果逐项 chip; 列索引。
