# 第 35 轮 · 主题域挂资产(完成 R25 悬挂, 后端+UI 大版本)

> R21..R44 第十五轮。R25 已做主题树→按主题域筛资产的前端, 但缺数据基础(表未挂主题域)。本轮补齐: 表元数据可归属主题域。

## 改动
- **sql/69_table_subject.sql(幂等, 已应用线上)**: dn_table_meta 增 `subject_id BIGINT`(信息架构条件式 ALTER, 可重入)。
- **DnTableMeta.java**: + `subjectId` 字段。
- **MetadataController.java**: + `POST /api/metadata/table/set-subject`{db,table,subjectId}(无行则插占位行 datasource_id=0; 有行则更新)+ `GET /api/metadata/table/subject`。注入 DnTableMetaMapper。
- **workspace.html**: 数据地图表详情头加「主题域」按钮 → `dmSetSubject()` 轻量 overlay(从 /api/gov/subject 拉主题域列表 + 当前值, 选择保存)。

## QA —— ✅ 通过(E2E 实测)
sql/69 应用线上(subject_id 列已建); 编译通过; **518 全量回归绿**; 33 UI 绿; 本地运行。E2E: set subjectId=7→get=7(insert 路径)→update=9→get=9(update 路径)→ MySQL 清理测试行(残留0)。workspace 含 dmSetSubject(2)。
- 修复: 首测 insert 因 dn_table_meta.datasource_id NOT NULL 报错, 改为 insert 占位 datasource_id=0。

## 验收门禁
- [x] 大版本(schema 迁移 + 模型 + 2 端点 + UI, 完成 R25 悬挂)
- [x] 架构师全盘(表↔主题域数据基础, 主题域不再孤立)
- [x] 消除孤岛(主题域可挂资产; 数据地图可设表主题域)
- [x] 功能可运行(sql应用线上 + 编译 + 518全量 + E2E insert/update round-trip)
- [x] 自续(R36 继续)

## 下一轮 R36
继续蓝图收尾 + 巩固。运维须知: 服务器须已应用 sql/69(本轮已自动应用)。
