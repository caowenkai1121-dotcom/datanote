# 第 47 轮 · 深度优化·后端安全/调度服务 6 个(大范围)

> 深优阶段第七轮。深优 RbacService/MaskingService/ScheduleLifecycleService/TaskSchedulerService/TaskExecutionService/MaturityService。Workflow 12 代理(6 优化→6 对抗审查)。

## 深优内容 + 两处关键
- 6 类加固: 校验/异常log/空安全/事务/性能/边界。RbacService 防全表误操作(updateUser/deleteUser/assignRoles/setRolePerms id 非空校验)、HashSet→LinkedHashSet 稳序去重。
- **★安全增强(MaskingService)**: 行级权限 `resolveRowFilters` 的 catch 由 **fail-OPEN**(异常→空过滤→受限用户拿全量)改为 **fail-CLOSED**(log.error+抛 BusinessException→拒绝执行)。3 调用方(DatasetService.query / HiveDdlController executeSQL+SSE)均正确承接异常拒绝执行, admin/bypass 路径不变。列脱敏 N+1 改 selectBatchIds(O(表数)→1)等价。
- **★审查捕获并修复 1 真回归(ScheduleLifecycleService)**: 优化者给 onlineLocal 加 @Transactional, 致跨 bean `refreshAllDependencies()`(默认 REQUIRED 传播)并入外层事务——循环依赖抛错时连带回滚"版本快照+置 ONLINE", 状态机语义变(原:ONLINE 已落库, 仅依赖刷新回滚)。5 单测 mock 不抛→假绿未覆盖。**修复: 移除该 @Transactional, 各步独立提交(恢复原语义)**。

## QA —— ✅ 通过(对抗审查 + 编译 + 518 + 冒烟)
- 6 审查: 5 PASS + 1 捕获回归(已修)。RbacService 鉴权未放宽、MaskingService fail-closed 收严(安全增强非回归)、调度状态机语义经修复后保持。TaskScheduler reviewer 实跑 7 单测绿。
- 自验: 编译 BUILD SUCCESS; **518 全量绿**(含 ScheduleLifecycle/TaskScheduler/RbacService 单测); 运行时冒烟 6 端点(rbac/scheduler/task-execution/maturity/health)200。

## 验收门禁
- [x] 大范围深优(6 安全/调度服务×6类)
- [x] 对抗审查捕获并修复真回归(ScheduleLifecycle 事务误扩)
- [x] 安全增强(masking fail-OPEN→fail-CLOSED)且鉴权未放宽
- [x] 功能可运行(编译+518全量+冒烟6端点200)
- [x] 自续(R48 继续)

## 下一轮 R48
深优剩余后端(CDC/同步引擎域 谨慎、控制器层入参校验统一)或转横切(全局异常/日志/性能)。
