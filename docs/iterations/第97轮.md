# 第97轮 —— hermes 复刻 Wave 4(部分): 大结果按需取全量 + 死循环 nudge

> 无人值守自驱。Wave 4 取两件【零 schema 改动】的高价值项: ①超大工具结果落盘的取数侧(复用 dn_ai_step.result_data)②重复同参调用拦截+nudge(死循环护栏)。缓解 38 工具 + 子代理放大的上下文/失控压力。

## 一、落地
### 大结果按需取全量(read_tool_result)
- **appendTrace 折叠提示**: 工具结果超 TRACE_RESULT_CAP(1500) 时, trace 只留预览 + 追加"(结果较大已折叠, 需全量可 read_tool_result(seq=N))"。完整结果(至 STORE_RESULT_CAP=8000)已在 dn_ai_step.result_data, 无需新表。
- **ReadToolResultTool**(name=read_tool_result, group=agent, readOnly 元工具): 参数 seq → 读本会话该步 result_data 返全量。让 agent 上下文里只放预览, 需要细节时再按需取, 省 token。

### 死循环护栏 + nudge
- 主循环对每个工具调用算签名 `工具名|参数`; 相同签名第 2 次出现 → 拦下不执行(结果不会变)+ 退还预算(不计生产步)+ trace 注入 nudge"已用相同参数调用过 X, 请换工具/参数或直接给结论" + 记 NUDGE 步。
- 防模型反复同参空转烧预算; 与 IterationBudget/HARD_ITER_CAP 互补(细粒度去重)。

## 二、QA(本地连远程库 E2E)
- **read_tool_result**: 令 agent 取回步骤 seq=1 全量 → 调用 rs=ok args={"seq":1}, 答复正确报出该步是 asset_detail。tools 37→**38**。
- **无回退**: 普通多工具会话 status=done; 折叠提示/nudge 路径不破坏正常流程。

## 天工开物 / 自由意志
- 天工开物: 大产出工序结果落盘+预览是务实 token 经济(上下文只放摘要, 全量按需取); nudge 防空转务实止损。
- 自由意志: agent 自主决定 read_tool_result 取全量(按需规划), 重复空转被温和纠偏(护栏内自主)。

## 验收门禁自检
- [x] read_tool_result 取回步骤全量(E2E rs=ok 正确)
- [x] 大结果 trace 折叠提示(复用 dn_ai_step, 零新表)
- [x] 重复同参调用拦截+nudge+退还预算
- [x] 无回退; 编译+打包+重启+E2E 全绿; 可回滚

## 复刻进度(6 波)
✅ W1 自主可控内核 · ✅ R94 人在环卡片 · ✅ W2 压缩+分类 · ✅ W3 子代理并行 · ✅ **W4(部分): 结果落盘+死循环nudge**

## 下一步(剩余)
- W4 余项: 渐进式工具披露(tool_search 桥接, 38工具schema渐大) + 技能 commands/bundles。
- W3 微项: /goal 跨轮自驱 + nudges 停滞催办(无进展时催办, 已有重复拦截基础)。
- W5 cron 自治 · W6 模型热切+后台复盘+Smart审批。
