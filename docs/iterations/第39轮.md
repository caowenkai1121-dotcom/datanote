# 第 39 轮 · 主题域→资产筛选端到端补全(服务端, 大版本)

> R21..R44 第十九轮。R25(主题树→assets ctx.subjectId)+R35(dn_table_meta.subject_id)+R36(资产详情设/看)已就位; 本轮补服务端筛选, 使主题域→资产链端到端完整。

## 改动
- **MetadataCenterController.searchTables**: +`subjectId` 查询参数 → `qw.eq("subject_id", subjectId)`。(因 /tables 直返 List<DnTableMeta>, R35 加的 subjectId 字段已随返回)
- **gov-assets.js loadAssets**: assetState.subjectId(主题树下钻 ctx.subjectId)存在时, 带 `?subjectId=` 调 /tables, 服务端筛选。

## QA —— ✅ 通过(E2E)
编译 + 518 全量绿; 33 UI 绿; 本地运行。E2E: set 表 subjectId=88 → `/tables?subjectId=88` 命中该表(数量1, subjectId=[88]); `?subjectId=99`→0 → MySQL 清理。主题域→资产链 R25+R35+R36+R39 端到端打通。

## 验收门禁
- [x] 大版本(主题域→资产链端到端补全)
- [x] 架构师全盘(主题树→筛资产 服务端+客户端双保险)
- [x] 消除孤岛(主题域真正能筛出其下资产)
- [x] 功能可运行(编译+518全量+33UI+E2E 筛选 round-trip)
- [x] 自续(R40 收尾)

## 下一轮 R40
全盘 E2E 验证 capstone: 遍历 R21-R39 关键深链/闭环, 汇总联动 campaign。
