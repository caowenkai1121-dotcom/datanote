# 第125轮 —— 全新生产化进程(并行两路): 数据标准行级编辑 + 血缘可操作化

> 业主令全新开始(甩开历史轮次, 只记部署+环境), 四角色生产级重构, 加大力度。

## 并行两路(子代理实现, 主线统验)
1. **数据标准(gov-standard.js)**: 数据元/词根/码表/码表项 四清单全部补「行级编辑」——回填既有新增表单、编辑态带 id(后端 save 本就 upsert: id!=null→updateById, 零后端改动)、唯一编码(element_code/dict_code)编辑禁改防破坏引用、保存修改/取消编辑、下拉回显防丢值。生产痛点(改字段须删重建)消除。
2. **数据血缘(gov-lineage.js)**: 影响面受影响表芯片改可点(navigateTo('catalog',{openTable})复用既有契约, 独立页降级纯文本)+「+N 张」点击原地展开; 删孤儿表清单「上游边/下游边」恒0死列。

## QA
- node --check 两文件通过; 打包重启全绿(:8099)。
- 数据元 upsert E2E: 建(nameCn=原名)→带 id 改(nameCn=改后名)→回读=改后名→删。注意: 数据元字段为 **nameCn**(非 elementName), 前端编辑代码已用对(13处)。
- JS 版本 gov-standard/gov-lineage → r125。

## 验收
- [x] 四清单行级编辑(回填/upsert/编码禁改) E2E 验证
- [x] 影响面可点下钻 + 展开; 死列删除
- [x] 打包+重启+冒烟全绿

## 下一批(生产化继续)
- MDM 属性绑定数据元+词根命名校验(标准落地闭环)。
- 黄金记录变更历史/版本 diff。
- UI 收尾: 硬编码色→令牌、旧式表单底座统一。
