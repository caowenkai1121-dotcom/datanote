# 第 7 轮 · 后端按 8 限界上下文全面分层重构（架构师+业务专家主导）

> 业主红线("≥50% 功能块 + ≥50% 代码调整")下的后端大重构。把扁平按技术分包的 **102 个 controller+service 类**重组进领域/平台/公共包，触及 **100% 的 API+业务层**（全部 10 个功能域）。
> **行为不变 = 纯物理重组**（只搬包 + 改 package/import，零逻辑改动），编译器 + 507 测试为安全网。

## 架构师设计（4-agent 工作流：3 清点 + 1 架构师蓝图）
扁平 `com.datanote.{controller,service,mapper,model,...}` → 领域驱动分层：
```
com.datanote.platform.{iam,config,audit,alert,ai,portal}   基座(跨切)
com.datanote.domain.{datasource,metadata,integration,develop,
                     orchestration,governance,mdm,project}   8 限界上下文
com.datanote.common                                          公共(Group/Subject/清理/广播)
```
**本轮范围（务实分批，降风险）**：只移 **controller(44) + service(58) = 102 类**；`model/mapper/dto/sync/config/util/exception/common` **保持原位**（→ `@MapperScan("com.datanote.mapper")` 不变、CdcSyncEngine 字符串 FQN 不受影响，blast radius 可控）。model/mapper 归域留后续轮。

## 执行（脚本化，编译器为网）
1. 校验 controller/service 全 102 类已映射（防漏）。
2. 移动文件 + 改 `package` 行 → 目标包。
3. 全局 FQN 替换（44 文件）：`com.datanote.{controller,service}.X` → 新 FQN（修 import 与 FQN 用法）。
4. **跨包 import 注入（61 条）**：同包互引用拆到不同包后自动补 import（难点 A）。
5. 测试随迁（12 个）：访问移出服务**包私有**静态方法的单测，迁到服务新包（StandardCheck/QualityJudgeStatus/AssetDetail/Lineage*/MetadataCrawlerMerge/DataMapPartition 等）。

## 团队验证
- **【架构师】**：域边界按业务归类；执行策略=单脚本全量 + 编译器逐轮收敛；类名全局唯一无歧义、0 个 XML mapper、@SpringBootApplication 默认扫 `com.datanote` 覆盖新包 → 安全。
- **【业务专家】**：域划分符合业务（治理 13 service / 项目 17 service / 编排 9 / MDM / 集成 / 元数据 / 数据源 / 开发 / 平台基座 / 公共）；行为零改 → 无场景受影响。

## 改动规模
- **102 类移动**（44 controller + 58 service）→ 10 个新领域/平台/公共包
- 44 文件 FQN 重写 + 61 import 注入 + 12 测试随迁
- 净影响：触及全部后端功能域（≥50% 功能块），约 ~160 文件改动

## QA 结论 —— ✅ 通过
服务器 E2E（pid 936742，重分包后应用正常启动）：

| 域 | 端点 | 结果 |
|---|---|---|
| platform.iam | /api/auth/status | ✅ 200 |
| platform.config | /api/system/config/doris | ✅ 200 |
| datasource | /api/datasource/list | ✅ 200 |
| metadata | /api/metadata/databases | ✅ 200 |
| develop | /api/script/tree | ✅ 200 |
| orchestration | /api/scheduler/today | ✅ 200 |
| governance | /api/quality/rules、/api/gov/health/issues | ✅ 200 |
| mdm | /api/mdm/overview | ✅ 200 |
| project | /api/project/list | ✅ 200 |
| common | /api/subject | ✅ 200 |
| integration | /api/sync-job/list | ✅ 200 |

- 全部 10 域 + 基座端点正常 → 重分包后 Spring 组件扫描/依赖注入/@MapperScan/路由映射全部正确，**应用生产可跑、行为零改**。✅
- 业务专家复核：各域功能入口全部可达，无场景受影响。✅

## 遗留 / 下一轮
- 后端续：model/mapper 归域(model.governance / mapper.mdm 等)、sync→domain.integration 子包化、infra 适配器层抽取。
- 治理域续：MDM 11→4 controller 合并、横切 告警/审计/偏好 归一。

## 验收门禁自检
- [x] 行为不变（507 全量回归绿；@SpringBootTest 上下文启动=Spring 扫描/注入/@MapperScan 新包结构全正常）
- [x] 业务专家确认（10 域端点 E2E 全可达）
- [x] ≥50% 功能块（全部 10 域的 controller+service 重组）+ 大幅代码调整（102 类移动+~160 文件）
- [x] 职责分层（技术扁平 → 领域驱动 8 上下文 + 基座 + 公共）
- [x] 零表变更
- [x] QA：12 域端点全 200/302，应用启动正常
- [x] 可回归（507 全量）
- [x] 部署成功可回滚（active/http302/pid936742；纯代码 revert，无 SQL）
