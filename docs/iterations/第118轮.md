# 第118轮 —— 两模块优化(第2轮): 续砍报表 + 澄清脱敏非占位

> 业主续: 砍报表堆砌, 要具体需要。

## 一、【QA→澄清】脱敏/行级权限不是占位(R117 遗留疑点已查实)
SqlMaskRewriter 确已接入查询执行链路:
- HiveDdlController(Doris SQL 执行 /api/doris/execute): resolveColumnMasks + resolveRowFilters(按当前用户) → rewriteMultiStatement 改写后再执行。
- DatasetService(数据集查询): 同样 resolve + SqlMaskRewriter.rewrite 后执行。
→ 脱敏策略/行级权限【真实生效】于 SQL 执行与数据集消费路径, 非空架子。(注: AI 的 table_data 直查源库样例未走该改写, 属另一路径, 后续按需加。)

## 二、【Domain/Product→Dev】续砍报表(要具体需要)
1. **删整模块『联动地图』(govmap)**: 纯前端 5 条业务闭环链 + 实体枢纽可视化, 无任何操作能力——从 GOV_MODS 移除(治理 12→11 模块)。其导航价值已被总览 KPI 磁贴覆盖。
2. **治理健康分·工单分析概览**(状态环图/严重级别/问题维度Top): 移除(buildIssueAnalytics 不再渲染), 保留工单列表本身(新建/流转/指派/删除/批量=操作主体)。
3. **主数据总览**精简: 砍掉"按业务类别分布柱状图"+"各域建模规模表"(纯报表), 只留可点 KPI 磁贴(主数据域/实体/属性/黄金记录, 各直达建模/黄金记录)+新增"数据管家待办→前往"磁贴。
4. 治理总览 desc 同步改为"关键指标直达操作 + 治理待办行动中心"。

## 三、QA(Playwright 真机)
- 治理侧栏 11 项(联动地图已移除); 治理健康分: 工单分析环图全消失, 工单列表保留。
- 主数据总览: 5 个可点 KPI 磁贴(含数据管家待办前往), 类别柱状图/各域规模表已消失。

## 验收门禁自检
- [x] 脱敏/行级权限查实为真实生效(SqlMaskRewriter 接入 Doris执行+数据集)
- [x] 删除纯展示模块 govmap; 删 gov-health 工单分析环图(留列表)
- [x] MDM 总览砍报表→KPI 直达 hub
- [x] 打包+重启+Playwright 真机全绿; JS r118

## 遗留(后续轮)
- 治理续砍: 质量覆盖矩阵热力、安全态势分布环图、数据消费热度/排行/僵尸指标、数据标准落地概览、各模块概览磁贴中纯展示项。
- MDM: 数据管家(纯聚合待办, 已有跳转价值, 暂留)、发布订阅 publish 模拟记账非真 webhook 推送(按需补真实推送或如实标注)。
- 角色新增缺 UI(gov-security 角色只读)。
