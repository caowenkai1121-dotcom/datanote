# 第71轮 —— 情境入口收尾: 首页今日 AI 简报 + 项目详情 AI 分析(agent 织入全模块)

> 深度集成蓝图最后两个情境入口。R57-63 已把 agent 织入 数据地图/治理(质量/血缘/资产/健康)/同步/运维/开发/指标/全局Ctrl+K, 本轮补 首页 + 项目, agent 自此覆盖所有核心模块。纯前端, 复用既有 dnAskAi→navigateTo(assistant, autoSend) 协议。

## 一、改动(纯前端, 无 Java)
- workspace.html: 新增 `window.homeAskAi`(今日简报 prompt, route home) + `window.projAskAi(id,name)`(项目分析 prompt, route project+projectId)。
- home-dashboard.js(q100r102→q100r110): 驾驶舱 hero 加「🤖 今日简报」按钮 → homeAskAi()。
- workspace.html 项目详情抽屉头(dbsync-drawer-head-actions)加「🤖 AI」按钮 → projAskAi(_projDetail.id, _projDetail.projectName)。

## 二、QA 结论 —— ✅ 通过(Playwright 真机 E2E, 0 console error)
- **首页今日简报 全链路**: 点 hero「🤖 今日简报」→ 跳 assistant + 自动发起 → agent **自主串 gov_overview→quality_score→sched_today_status** → 产出真简报:
  - 健康总分 31(偏低)、资产 15库/429表/7629字段/442MB、工单 0;
  - **质量满分警示**: "近24h 质量检查运行0次, 满分非真无问题而是无检查, 伪健康风险"——**复用了 R67 自学习记忆中『虚假满分』那条经验**, 自学习闭环在真实输出中兑现;
  - 调度今日 0 任务 → SLA 断档风险提示; 并主动指出缺"同步大盘"工具并给建议。
- **项目详情 AI**: 详情抽屉头「🤖 AI」按钮存在(text=🤖 AI), projAskAi/homeAskAi 均为 function。

## 验收门禁自检
- [x] 功能可运行(真机 E2E + 0 console error; 无 Java 改动, 527 测试不受影响)
- [x] agent 织入全模块完成(首页/项目补齐, 至此 数据地图/治理/同步/运维/开发/指标/项目/首页/全局 全覆盖)
- [x] 联动(复用统一 dnAskAi→navigateTo assistant autoSend 协议 + 既有 read 工具, 非孤岛; 简报实证记忆召回生效)
- [x] 无重复(沿用既有协议与工具, 未新增端点/对话框)
- [x] 可回滚(仅 workspace.html + home-dashboard.js 版本号)

## 下一轮
- 向量列索引(7629列分批, 提升 RAG 粒度); 记忆抽屉归档/置顶; 同步大盘只读工具(简报里 agent 主动反馈缺口)。
