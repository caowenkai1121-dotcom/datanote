# 第 23 轮 · 指标三视图互通 + 历史趋势(大版本)

> R21..R44 第三轮。架构师:指标(dn_metric)是枢纽实体, 此前"列表↔驾驶舱↔影响面"三视图互不下钻。本轮打通指标全联动 + 补历史趋势可视。

## 改动
- **workspace.html**: 指标列表(renderMetricList)每行加「驾驶舱」→ `metricGotoBoard(code)` → `navigateTo('governance',{gov:'consumption',focusMetric})` 聚焦该指标; ROUTES.metrics.init 消费 `__navCtx.editId` → 自动开编辑框(消费僵尸「编辑」深链回指标管理闭环)。
- **gov-consumption.js**: 指标驾驶舱行加「趋势」→ `metricTrendDrawer(r)` 调 `/api/consumption/metric/{id}/history` 用 `DN.line` 画历史折线 + 最近12条明细表。

## 业务闭环(本轮)
指标可信度/消费三视图: 指标列表→【驾驶舱】聚焦取值/新鲜度/输入质量/预警/趋势 ↔ 僵尸/影响面→【编辑】回指标管理。一气呵成。

## QA —— ✅ 通过(Playwright 建临时指标实测)
gov-consumption node--check OK; 33 UI 绿; 本地运行。建临时指标 r23_verify 后实测: 指标列表行「驾驶舱」按钮渲染 ✓; 消费驾驶舱行「趋势」✓ + 「输入质量」无回归 ✓; 临时指标已清理。版本 gov-consumption q100r91。

## 验收门禁
- [x] 大版本(指标↔消费三视图互通 + 历史趋势)
- [x] 架构师全盘(指标枢纽三视图打通)
- [x] 消除孤岛(指标列表/驾驶舱/僵尸/影响面 互相下钻)
- [x] 功能可运行(node--check + 33UI + Playwright 建删实测)
- [x] 自续(R24 继续)

## 下一轮 R24
数据地图横向深化 + 运维统计下钻(手动/集成→详情) + 系统监控大屏可跳处理(蓝图 R28/R29)。
