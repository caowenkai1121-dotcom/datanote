# 第 42 轮 · 深度优化·治理剩余 7 模块(大范围)

> 深优阶段第二轮。深优 gov-lineage/standard/classification/security/audit/subject/govmap。ultracode + Workflow 14 代理(7 优化→7 对抗审查 pipeline)。

## 深优内容(每模块 6 类加固, 同 R41)
健壮性(fetch 骨架/空/错/重试+判空)、输入校验+confirm、边界(分页/截断/0负空)、UX(去抖/toast/防重复提交)、性能(去重 fetch/getOnce/并发守卫)、修隐藏bug(闭包 var 陷阱、定时器清理、重复抽屉、tone 枚举、a11y 键盘可达)。

## 对抗审查发现并修复 1 真回归
- **gov-security**: 优化者保留了 line30 `buildSecurityOverview(ovBox)`(无参→自取4端点)+ 新增 line64 `loadAllOnce(ovBox)`(再取4+渲染)→ 实际 8 请求 + 同 ovBox 渲染竞态。**修复**: 删除 line30 重复调用, 概览统一由 loadAllOnce 一次拉取驱动(真正 8→4)。
- 其余 6 模块审查均「PASS 无回归风险」(深链/端点/DN契约/闭包/tone 逐项核实)。

## QA —— ✅ 通过(对抗审查 + Playwright)
7 模块 node--check 全过; 7 审查(6 PASS + 1 发现并已修)。33 UI 绿; Playwright: 7 模块全渲染无报错 + gov-security 概览单次渲染正常。版本 7 文件 q100r100。

## 验收门禁
- [x] 大范围深优(7 模块×6类加固)
- [x] 对抗审查捕获并修复真实回归(gov-security 双取竞态)
- [x] 深度优化每个功能 + 不破坏(深链/端点保留)
- [x] 功能可运行(node--check×7 + 33UI + Playwright sweep)
- [x] 自续(R43 继续:消费/数据地图/MDM 模块深优)

## 下一轮 R43
深优消费层(gov-consumption 已 R41)+ 数据地图(workspace.html dm* 函数)+ dn-common 组件库。
