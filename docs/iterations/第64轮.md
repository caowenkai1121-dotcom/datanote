# 第64轮 —— 前端 AI 配置(对话 DeepSeek / 嵌入 百炼 分离管理)

> 业主需求①: 前端做 AI 配置, 普通任务用 DeepSeek(对话), 仅 embedding 用百炼。

## 本轮交付
### 后端 StoreController
- `GET /api/ai/store/config`: 返嵌入配置(provider/baseUrl/model/dim/apiKeyMasked/available)+ 向量库/图库运行态(ready/collection)。密钥脱敏("已配置(加密)")。
- `POST /api/ai/store/config`: 保存嵌入配置(密钥 AES 加密存 system_config ai.embedding-*)+ **热重载** embeddingService.reloadConfig()。复用 CryptoUtil/DnSystemConfigMapper。

### 前端(系统管理 → AI 配置)
- 改双卡布局: 「💬 对话模型(普通任务)」→ `openAiConfig()`(既有, DeepSeek/Anthropic/OpenAI 等)+「🧠 嵌入/向量库/图库」→ `openStoreConfig()`。
- `openStoreConfig()` 弹窗: 嵌入 provider(百炼/OpenAI/硅基流动/自定义,切换自动填 baseUrl/model/dim)+ API Key(脱敏)+ Base URL/模型/维度 + 运行态(嵌入/向量库/图库 ✓✗)+「重建索引」(调 /store/sync)+ 保存(热生效)。
- 嵌入独立于对话模型: **对话走 DeepSeek(无 embeddings 端点), 嵌入走百炼**, 两者前端分开配。

## 【QA】结论 —— ✅ 通过(真 E2E)
- 全量回归 `mvn -o test` **527 / 0fail / 1skip** → BUILD SUCCESS。
- `GET /api/ai/store/config` 返 embedding{provider:bailian, model:text-embedding-v3, dim:1024, 已配置(加密), available:true} + vector.ready + graph.ready。
- workspace 服务 openStoreConfig + 配置对话模型 + 配置嵌入与存储。
- 对话配置(openAiConfig,DeepSeek)与嵌入配置(openStoreConfig,百炼)前端分离。

## 验收门禁自检
- [x] 功能可运行(527 全量绿 + /store/config 端点 + UI 双卡服务)
- [x] 满足①(前端配 AI; 对话 DeepSeek / 嵌入 百炼 分离; 密钥加密热生效)
- [x] 不重复(复用 openAiConfig/CryptoUtil/system_config/showModal)
- [x] 安全(密钥 AES 加密存 + 脱敏返回)
- [x] 可回滚(StoreController 加端点 + 前端弹窗, git revert 即复原)

## 进行中(业主需求②)
AI 完成开发任务(建表/ODS/同步任务等写操作)+ 数据入库 + 记忆 + 自学习 —— 架构师 Workflow 全局规划中(写工具矩阵/Guardrail三态/审批门/fail-closed审计/记忆自学习架构),完成后落地。
