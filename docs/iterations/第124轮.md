# 第124轮 —— 生产化升级(第1轮): 角色管理闭环 + 生产路线图

> 业主: 以业务专家/架构师/产品/UI四角色深度重构升级, UI统一+功能自由增改删, 目标【可交付生产】。

## 一、【四角色】生产差距评估(基于 R123 全量梳理)
- 【Domain/Product】生产阻断: ①角色只能 SQL 建(安全闭环断)→本轮修 ②数据标准缺编辑(删重建丢关联) ③MDM↔标准未绑定。
- 【Architect】脱敏/行级已真实生效(SqlMaskRewriter); REPLACE/RANGE 固定行为需 UI 明示或参数化; 30处原生 confirm 改造为统一弹窗属异步重构(30调用点), 风险大收益低→列入路线缓做。
- 【Design】UI统一遗留: toneColor 等少量硬编码 hex、xrefForm 旧式底座、confirm 风格不一; 整体已 Slate&Indigo v2 + DN.* 体系。

## 二、本轮落地: 角色管理 CRUD(安全闭环)
后端 /api/rbac/roles POST/PUT/DELETE + /roles/{id}/perms 早已存在, 纯前端缺口。gov-security.js:
- 角色卡加「+ 新建角色」; 行级「编辑」「删除」(delLink 两步确认防误删)。
- openRoleForm 抽屉(drawerForm 统一底座): 编码(新建必填校验 ^[a-zA-Z0-9_]{1,64}$, 编辑禁改防破坏行级策略/用户分配引用)/名称/描述/权限点(逗号清单→/perms 同步, 编辑可清空)。
- 与用户管理(link/delLink/ops)同款交互风格, UI 一致。

## 三、QA
- JS check + 打包重启全绿; API 冒烟全链: 建角色(id=5)→配权限(asset:read,quality:write)→列表回显→删除。

## 四、生产化路线图(后续轮次执行)
P0(生产必备):
1. 数据标准 数据元/词根/码表「编辑」入口(高频维护操作)。
2. MDM 属性绑定数据元+词根命名校验(标准落地闭环)。
3. 黄金记录变更历史/版本diff(可信可证明)。
P1(体验/一致性):
4. gov-standard 删重复落标率趋势+纯文字简评; gov-lineage 删孤儿表恒0两列+影响面芯片可点。
5. xrefForm 统一 drawerFoot; toneColor 等硬编码色→令牌。
6. 脱敏 REPLACE/RANGE 参数化(maskingParam 全链)或 UI 明示固定行为。
P2(生产工程):
7. 统一确认弹窗 DN.confirm(30调用点异步改造, 需专轮回归)。
8. 登录/会话强化、操作审计覆盖核查、慢查询与分页核查(资产/审计大表)。

## 验收门禁自检
- [x] 角色平台内可建/改/删/配权限(安全闭环), API 全链冒烟
- [x] 表单校验+编码禁改+两步删除确认(防误操作)
- [x] 与既有用户管理交互风格一致(UI 统一)
- [x] 打包+重启全绿; JS r124
