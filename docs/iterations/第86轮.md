# 第86轮 —— workspace.html 内联模块 + 弹窗 表单输入统一升级

> 抽屉表单(gov+mdm)R83-R85 已收口。本轮收 workspace.html 内联模块(系统/同步/开发/项目)与弹窗(g-modal 等)的表单输入——它们用 `dbsync-form-*/g-modal-input/intg-*/ds-form-row` 类(非 DN.* 助手), 故用 CSS 一处升级到舒适 36px + 焦点环, 与抽屉表单同款体验。

## 一、CSS 升级(app.css, 表单输入类对齐新标准)
| 类 | 用处 | 升级 |
|---|---|---|
| `.dbsync-form-input/.dbsync-form-select` | 新建同步任务弹窗等 | 32→36px + `--ring` 焦点环 + 标签 12→12.5px/text-regular |
| `.g-modal-input` | 全局弹窗输入 | 32→36px + 焦点环 + bg-card |
| `.intg-select/.intg-input` | 同步源/目标连接面板 | 30→34px(密集面板适中)+ 边框令牌 |
| `.ds-form-row input` + label | 系统-数据源/环境配置 | 32→36px + 焦点环; 标签 muted→text-regular/500 |

## 二、QA(Playwright 真机, 0 console error, 527 测试绿)
系统管理-数据源管理表单截图确认: 输入框由扁平 32px 变舒适 36px、标签清晰加粗、保存配置蓝色可见、聚焦有焦点环。对比最初截图(图3 朴素表单)明显提质。527 回归绿。

## 三、表单视觉统一收口
- 抽屉表单(gov+mdm 20+): DN.* 全新系统(分组/必填红星/两列/sticky 底栏)— R83-R85。
- workspace.html 内联表单 + 弹窗输入: CSS 升级 36px+焦点环 — R86。
→ **全站表单输入现统一为舒适 36px(密集面板 34px)+ 靛蓝焦点环 + 令牌化**, 与设计系统一致。

## 验收门禁自检
- [x] workspace.html 4 类表单输入升级(同步/弹窗/连接/系统)
- [x] 与抽屉表单输入同款舒适度 + 焦点环
- [x] 0 console error + 527 测试绿
- [x] 纯 CSS, 零 JS 风险; 可回滚

## 下一轮
- workspace.html 内联表单进一步结构化(分组/必填红星/sticky 底栏需逐个 JS 改, 如项目新建/开发脚本配置/同步任务多步表单); 或转其他打磨项(间距令牌化/空态补齐)。
