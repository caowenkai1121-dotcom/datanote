# 持续迭代轮次记录（目标 ≥100 轮）

业主令：持续扫码做 6 类改进 —— ①BUG调整 ②现有功能增强 ③UI设计调整(风格一致) ④重复功能合并 ⑤无用/无意义功能删除 ⑥新增有用功能。每轮验证(编译+545测试绿/JS语法)。

> 此前已完成：全代码库安全审计修复(80)、零散优化(15)、三大主流程深度迭代(17高+若干medium)，详见 memory `security-audit-fix-2026-06-13`。本表从 /goal 设定后的"持续扫码迭代"起计。

| 轮 | 类别 | 改动 | 状态 |
|---|---|---|---|
| 1 | ⑤删无用 | AiAssistService.testConnection 去除完全相同的 if/else 死分支 | ✅ |
| 2 | ④合并重复 | DolphinService doPost/doPut → 共用 doForm(method,...) | ✅ |
| 3 | ①bug/②增强 | baselineType 做实：DnBaseline 加字段 + sql/84 补列(原前端收集后端丢弃，列表永远显示"核心") | ✅ |
| 4 | ①/②perf | MdmReferenceController 3 处 N+1(xref/by-entity/hierarchy)→ 批量 selectBatchIds | ✅ |
| 5 | ①/②perf | MdmGovernanceController 2 处 N+1(订阅/发布日志补名)→ 批量 | ✅ |
| 6 | ①/②perf | DatasourceExploreService.profile 双 Doris 连接 → 合并为一次 | ✅ |
| 7 | ①/②perf | IssueService leaderboard/trend 全表拉内存 → DB 侧聚合/时间过滤(stats 因含 SLA 逐行逻辑保留) | ✅ |
| 8 | ①/②perf | ProjectAssetService.resolveName 全表扫 → 按 id 精确 selectById | ✅ |
| 9 | ①/②perf | ProjectOverviewService 单次 overview 资产查 7 次 → 1 次复用 | ✅ |
| 10 | ②增强 | MaskingService 脱敏清单 60s TTL 缓存 + 保存入口 evict | ✅ |
| 11 | ③UI | datamodel.js 模型列表 名称/编码/主题域 长文本截断+title(防撑破布局) | ✅ |
| 12 | ①bug | dmGenDDL/dmGenSelect 靠下标[3]定位 Tab(实为"数据探查")错位 → 按 onclick 目标定位 | ✅ |
| 13 | ①bug | CryptoUtilTest 错误密钥用例 AES ~1/256 概率 flaky → 改测安全不变式(确定性) | ✅ |
| 14 | ①bug | AiAssistService.chat: MODEL_OVERRIDE 连读两次→读一次; 空响应日志用 activeModel 而非 c.model(覆盖生效时误导排障) | ✅ |

| 15 | ①bug/perf | LifecycleService.costRanking 无界全表 selectList → 加近90天时间窗界定扫描(保留活跃表最新快照) | ✅ |
| 16 | ④合并重复 | QualityService querySampleJson/WithParam 两份逐字重复的 ResultSet→JSON → 抽 rsToSampleJson 共用 | ✅ |

**运维须应用**：`sql/84_baseline_type.sql`(ALTER dn_baseline ADD baseline_type)。

| 17 | ①bug | MetadataCrawlerService.crawlAll 加单飞 AtomicBoolean 守卫(手动 /crawl/all 裸线程与每日定时可能并发重复全量采集) | ✅ |
| 18 | ④合并重复 | DolphinService createWorkflow/updateWorkflow 的 relation+params 构建逐字重复 → 抽 buildProcessDefParams 共用 | ✅ |

(R19 gov-standard.js govStdImpact 经查已用 DN.get+catch, 非问题, 跳过)

| 19 | ③UI | datamodel.js renderEntities 实体名/编码长文本截断 | ✅ |
| 20 | ①bug | DataModelService uniqueCode selectCount NPE 守卫 | ✅ |
| 21 | ①bug | DataModelService validateModel entities null 安全 | ✅ |
| 22 | ①bug | DataModelService toCamel 空结果兜底"T" | ✅ |
| 23 | ②增强 | Validation number 正则加 ^$ 锚定(语义明确, 拒 '123abc') | ✅ |
| 24 | ②增强perf | RbacController 用户列表 roleIds N+1 → 一次 in 批量查 | ✅ |
| 25 | ②增强 | ClassificationService 热力图 tid 去重(LinkedHashSet) | ✅ |
| 26 | ①bug | IssueService ageHours Math.max(0,…) clamp 负值 | ✅ |
| 27 | ⑤删无用 | SystemConfigController 删死代码 testHdfs()(永远 fail, HDFS 已下线) | ✅ |
| 28 | ③UI | project.js 卡片标签 chip 长文本 max-width+省略号+title | ✅ |

(经核实跳过非问题: gov-standard govStdImpact已用统一信封 / MdmCore变更类型校验非重复 / gov-subject排序已有tiebreaker+主题名已有ellipsis+Enter已排除textarea / ScriptService NPE已用Long守卫 / home超期红色为合理约定)

| 29 | ④合并 | RbacService 新增 resolvePerms(Authentication), AuthController/RbacController 删重复实现改调用(3文件去重) | ✅ |
| 30 | ①bug | MetadataService Oracle 空表名静默查空 → 明确抛 IllegalArgumentException | ✅ |
| 31 | ②增强 | AssetDetailService.profile 两次 getConnection → 复用单连接 | ✅ |
| 32 | ④合并 | ConnectionManager 抽 normalizeType 统一 trim/upper/空判, 收口类型识别重复 | ✅ |
| 33 | ③UI/bug | ai-agent.js 数据预览 sticky 表头补 z-index:1(原滚动被数据行盖住) | ✅ |
| 34 | ⑥新功能 | project.js 筛选加「清空筛选」一键复位(筛选激活时显示) | ✅ |

(Fix-batch-3 另4项经核实已正确跳过: getUserPerms已批量in / PermInterceptor正则已静态预编译 / AuditService toLowerCase已只算一次 / DatasetService已selectOne+LIMIT1空安全)

| 35 | ②增强/清理 | DataReconciliationService 校验和分桶 magic 16 → 命名常量 CHECKSUM_BUCKETS+注释 | ✅ |
| 36-58 | 前端打磨Workflow 23项 | 6代理读14个JS文件应用真实小改进, 全部 node 语法校验通过 | ✅ |

**R36-58 明细(前端打磨, 14 文件)**:
- bug(10): gov-quality 状态色硬编码大写→小写无关; gov-security buildSecurityOverview 补 .catch 错误态; gov-govmap 步骤pill补tabindex(键盘可达); gov-overview 双重转义去除; mdm 重复簇 inactive 误显草稿→三态; mdm-approval/hierarchy(×2) [object Object]兜底; mdm-ingest 空列隐藏导入按钮; mdm-survivorship 源优先规则 filter 空指针守卫
- ui(8): gov-govmap hover阴影硬编码→var(--shadow-md); gov-subject 分层徽标 flex:none 防压缩; gov-standard 码表编码列补title; mdm-ingest 源列截断; mdm-pubsub 空status不渲染[]; mdm-quality 业务主键 cursor:help; mdm-refdata 名称列截断; mdm-survivorship 属性名列复用truncCell截断
- merge-dup(2): gov-quality 复用trunc; mdm-pubsub successRate兜底抽变量
- enhance(2): mdm-ingest 加载源列防重复点击; mdm-quality 升级工单失败toast兜底
- delete-dead(1): mdm-pubsub 删未调用的 entityName()

| 59-72 | 前端打磨-2 14项 | gov-classification/consumption/health/assets/dn-common, 全 node 语法 OK | ✅ |

**R59-72 明细(前端打磨-2)**: 删死代码(gov-health drawSparkline/buildIssueAnalytics、gov-assets magBar冗余分支)、bug(gov-classification 失效disabled守卫→_busy、gov-consumption 未跑徽标红色矛盾、gov-assets 删除回调漏done解锁、dn-common DN.heat null标签)、UI截断/一致(gov-classification审计表ellip、gov-consumption fmtNum口径、dn-common DN.bars/statTile/heat 补title、alertNode aria-label)、合并(gov-consumption flash抽取)。

进度: 72/100。验证: 6 JS 文件 node 语法 OK。

| 73-86 | 后端打磨-1 14项 | governance/ai引擎/mdm/integration/project 服务内部, 编译+545测试绿 | ✅ |

**R73-86 明细(后端打磨-1)**: bug(QualityScheduleService 判空、MdmIngestService 源ID列越界守卫)、删死代码(OverviewService BigDecimal 不可达分支)、去重(AiFileService ownerDenied、MdmPublish getDataJson、MdmIngest blankVal、FullSyncEngine buildWriteRow、AlertService notEmpty、SyncJobExecutor buildExecution)、增强(OverviewService 双cast、ContextCompressor 魔法值常量、MdmMatch @SuppressWarnings、ProjectTaskService REF_TYPES常量、ProjectTagService 全表扫→selectBatchIds)。4项已稳健跳过。

| 87-97 | 前端打磨-3 11项 | home-dashboard/datamodel/project/ai-agent, node语法OK | ✅ |
| 98-109 | 后端打磨-2 12项 | 编排/取数/治理/平台/common 服务内部, 编译+545测试绿 | ✅ |

**R87-97(前端打磨-3)**: bug(datamodel dmDelRelation/dmAddRelation/dmEditAttrs 补.catch、dmSaveRelation NaN守卫; project projWikiEdit/projFromTemplateModal 补.catch; ai-agent uploadFiles 补 r.ok 检查)、UI(home待审批补title、project 表格行描述截断×2、datamodel ER连线#4f6cf7→var(--primary))、删死代码(ai-agent renderRich/RICH_RE)。
**R98-109(后端打磨-2)**: 删死代码(LifecycleService hasTaskRef、MetricValueService 死@SuppressWarnings、StandardService 无用import)、去重(BaselineCheck receiver、MetricValue success、MetricAlert objectRef、AuditService buildLog)、增强(SqlLineage/LineageEdge 时间戳一致+TABLE_CONFIDENCE常量、AlertConfig/SubjectService 魔法值常量)。8项已稳健跳过。

---

## ✅ 目标达成: 109 / ≥100 轮

全程6类全覆盖: ①BUG调整 ②现有功能增强 ③UI设计调整(保持令牌一致) ④重复功能合并 ⑤无用功能删除 ⑥新功能增加。
**最终验证**: 编译 BUILD SUCCESS + 545 测试 0 失败 0 错误 + 全部改动 JS 文件 node 语法校验通过。
累计 118 文件 +1784/-716(含本次/goal前的安全审计+主流程迭代)。**运维须应用 sql/84_baseline_type.sql**。git 未提交。
方法: 已知发现清单逐项核实(Explore精度有限, 多项过时已甄别跳过) + 2轮全代码库6类扫描(95项) + 6个并行打磨Workflow(每簇文件不相交, 读真实代码做安全小改, 行为不变), 每批编译/语法验证。

---

## 续: 业主令 ≥5000轮/每轮≥10点

| 110 | 打磨×10 | integration连接器/ai向量图: 魔法值→常量(Postgres/SyncJob/AuditLog/TableData/SemanticSearch)、去重(EmbeddingService/GraphStoreClient)、删死代码(ScriptService重复import/MetadataService死参数)。8文件经核实已最优跳过 | ✅ |
| 111 | 补测试×12断言 | 新增 DorisSqlUtilTest(列名归一/标识符转义/字面量转义6例) + ValidationTest(required/number/string/boolean粗校验6例), 测试数545→557全绿 | ✅ |

**现实判断(诚实记录)**: 经安全审计80 + 主流程迭代 + 110轮打磨 + 111个既有测试类, 代码库已高度优化且测试充分。打磨Workflow跳过率已达50%。故转向**真实新功能**(业主指方向: UI人性化 / 数据开发增强 / 模块合并 / 自由加有用功能)。只做真实/可验证/零回归, 不为凑数堆砌churn。

| 112-114 | 数据开发增强·脚本上线审批+版本回滚(~18处) | **B线特性**: 脚本上线由直接置online改为审批制——DnScriptChange实体/Mapper/sql85 + ScriptApprovalService(提交/审批/禁自批/列表/角标) + ScriptApprovalController + develop:approve权限点+拦截器规则; 前端"上线"改弹窗提交审批 + 工具栏审批工单队列(通过/驳回)+待审角标; 版本回滚改服务端原子端点(回滚前先存当前为历史版本防丢失)。编译+557测试绿+workspace JS校验通过 | ✅ |

**运维须应用**: sql/85_script_approval.sql。**新增权限点 develop:approve** 须授予审批角色。

| 115 | 补测试×11 | ScriptApprovalServiceTest(测提交校验/禁自批SoD/状态机/批准执行上线), 测试557→568全绿 | ✅ |

---

## 无人值守自主迭代(业主睡觉, 全权限)

业主授权全程无人值守、自主决定所有轮次方向。原则不变: 只做真实/可验证/零回归, 不堆churn。节奏: 真功能(建+测) + 新鲜文件组打磨Workflow(controller→model→剩余service) + 阶段性合并分析(C线), 每批编译/测试/JS校验。

| 116 | controller打磨×18 | 空白参数穿透守卫(防500/脏数据)、空密码NPE、null body NPE、limit钳制、死字段/import删除、魔法值常量、ConsumptionController去重。编译+568绿 | ✅ |
| 117 | controller+DTO打磨×22 | Long.valueOf→500守卫、异常处理一致性(BusinessException/ResourceNotFound重抛)、空白参数守卫、死代码/DTO死字段删除、MdmGovernance全表→DBcount。**误删DataxController.resolveDatasource(String)被单测引用→已恢复**(验证机制拦截), 编译+568绿 | ✅ |
| (B线续) | 审批队列增强 | workspace.html: 审批前查看SQL内容快照 + 批量通过/驳回选中(复选框), JS校验通过 | ✅ |

**教训**: 打磨Workflow删"死代码"须同时 grep 测试引用(不止前端)。后续workflow指令已强调。
**进度 ~118轮**, 568测试绿。控制器层2轮共40处真实修复, 数据开发审批特性端到端可用(提交[弹窗]→审批[查看SQL/批量/禁自批]→执行 + 版本回滚 + 11服务测试)。

| 118 | service打磨×8 | **真bug**: ErrorClassifier 分类优先级括号缺失致误判、CronJobTool AgentArgs.intVal→longVal(任务ID超Integer.MAX_VALUE溢出)、TaskExecutionService 类型字面量→Constants.TASK_TYPE_SCRIPT; 余为去重/魔法值。integration/util 15文件经核实已最优且测试充分→跳过。编译+568绿 | ✅ |

**矿脉判断**: 后端打磨(integration/util)跳过率已>50%, 该层高度优化。转回 **A线 UI人性化**(业主明确点名: 该下拉却让手输)+ **B线数据开发增强**。

| 119 | B线数据开发增强·脚本审批撤回(×11) | **真实闭环缺口**: 提交后若已有待审工单, submit() 拒绝重复提交 → 申请人卡死(无 approve 权限开不了审批队列)。补 **撤回**: ScriptApprovalService.withdraw(禁非本人/非admin·仅pending) + pendingChange(查当前待审, develop:edit 可达) + 2端点(GET pending-change / POST withdraw-change, 均避开 /changes 前缀以走 edit 权限) + 前端 submitScriptApproval 先查待审→有则弹"撤回工单"否则提交 + 3单测(非pending拒/非本人拒/本人撤回置withdrawn)。编译+571测试绿+JS校验通过 | ✅ |

**A线核查结论**: 枚举字段该下拉却手输的情况经 placeholder 关键词全站扫描, 仅命中"标签(逗号分隔)"与"cron(带预览)"等合理自由文本, 主表单枚举均已用 select(与早前结论一致)。故 A线无真实改点, 转 B线。

| 120 | B线数据开发增强·版本回滚可见性(×6) | **真实边界bug**: 上线版本本应永久保留作关键回滚点, 但 listVersions 仅取 committed_at 最近10条, 连续保存>10次会把上线版本挤出列表→UI 不可见/不可回滚。修 listVersions: 最近10条 ∪ 全部上线版本, 按 id 去重(避开 ne null 陷阱) + committed_at 降序(null末尾, compareTimeDesc 助手)。+ 修 R119 引入的显示bug(审批队列 withdrawn 误显"已驳回"→"已撤回")。+ 3单测(上线版本被挤出仍可见/同id不重复/null空)。编译+574测试绿+JS校验 | ✅ |
| 121 | B线数据开发增强·审批漂移护栏(×5) | **真实治理缺口**: submit 快照提交时内容, 但 review→approve 上线的是脚本**当前**内容; 提交后若编辑过, "审批所见≠上线内容"(SoD 形同虚设)。review() 加漂移护栏: ONLINE 通过前比对现内容 vs payloadJson 快照(norm 归一去空), 不一致则拒绝并提示撤回重提; OFFLINE 不比对。+ 修既有 approveOnline 测试补 payload + 2单测(漂移拒绝·下线忽略漂移)。编译+576测试绿 | ✅ |
| 122 | 数据地图·AI搜索提示词上限(×4) | **真实健壮性**: DataMapService.aiSearch 把**全部**表塞进 LLM 提示词, 大元数据库会撑爆上下文/烧token/失败。加 AI_SEARCH_MAX_TABLES=800 上限 + 截断提示("共N张, 仅列前800")。元数据≤800时行为不变(零回归)。getAllTablesSummary 经核查已 TTL 缓存、searchTables 已 cap50→均无需改。+1单测(1000表→截断提示+只到t_799)。编译+577测试绿 | ✅ |
| 123 | 治理·数据标准落标稽核范围生效(×4) | **真实功能bug**: 前端落标稽核传 scope(db / db.table)但 StandardService.runCheck 仅入库未用, 始终全量稽核→"范围"选择器形同虚设。加 resolveScopeTableIds(解析 scope→查 dn_table_meta 得 id 集, 空范围返 null=全量) + 遍历列时按 tableMetaId 过滤。+3单测(范围限定2列/空范围全量且不查表/范围无匹配表→0)。编译+580测试绿 | ✅ |

> **业主新指令**: 后续每轮**必含一个 UI 功能优化**(很多 UI 功能仍有问题)。

| 124 | 数据模型审批·原生prompt→弹窗+驳回必填(UI功能×4) | **UI功能瑕疵**: datamodel.js dmReview 用原生 `prompt()` 收审批意见——阻塞、丑、与全站弹窗体系不一致, 且驳回可空原因。改 projShowModalBox 弹窗(textarea+自动聚焦, 复用 dbsync-form-input 风格) + dmReviewSubmit, **驳回原因前端必填** + **DataModelService.review 服务端兜底**(驳回空原因抛错)。+1单测(空原因驳回拒绝且不更新)。JS校验+编译+581测试绿 | ✅ |
| 125 | 工作台·原生confirm/prompt→应用弹窗(UI功能×2) | **UI功能瑕疵**: ①closeTab 未保存守卫用原生 `confirm()`(阻塞/不一致)→改 `msgConfirm` 承诺式 + 拆 `doCloseTab` 续延(两调用方均容异步, 与原行为平价零回归); ②umResetPwd 用原生 `prompt('设置新密码')` 明文输入→改 projShowModalBox 弹窗 + `type="password"` 输入框(不暴露明文)+ umResetPwdSubmit + 自动聚焦。纯前端, Java 未动→测试仍581。JS校验通过(30块0错) | ✅ |
| 126 | 数据同步·最后一处原生prompt→弹窗 + 全站原生弹窗清零(UI功能) | **UI功能瑕疵**: dbsyncTriggerIncrSnapshot 用原生 `prompt('源表名')`→改 showModal 弹窗 + 输入框 + dbsyncDoIncrSnapshot + 自动聚焦 + 空校验。**至此全站 prompt/confirm/alert 原生弹窗清零**(node 正则全扫 0 命中), 弹窗体系统一。顺带核查 Dataset/DataModel/Standard list 的 keyword/type/status/subjectId 过滤参数**均已正确消费**(无 ignored-param bug)。JS校验通过 | ✅ |
| 127 | 脚本审批工单·状态筛选下拉(UI功能) | **UI功能缺失**: 审批工单弹窗只显待审+最近8条已审, 无法按状态浏览全部历史(后端 listChanges(status) 早支持却没前端入口)。拆 openScriptApprovalQueue+renderScriptApprovalQueue(statusFilter), 加状态下拉(待审批/已通过/已驳回/已撤回/全部, 各带计数), 非待审视图展示审批人+审批意见(原先完全不可见)。数据已全量加载→前端切换零额外请求。owner类输入经核查已全站挂 dnUserList 数据列表(A线已一致)。JS校验通过 | ✅ |
| 128 | 数据同步实时日志·过滤即时生效(UI功能bug) | **UI功能bug**: dbsyncLogFilter 任务过滤只对**后续新行**生效, 切换过滤时已显示的其它任务旧行残留→用户以为没生效。给每行打 data-task-id + select 加 onchange=dbsyncApplyLogFilter, 切换时即时 show/hide 已有行。JS校验通过 | ✅ |
| 129 | 弹窗·键盘提交(UI功能) | **UI功能缺失**: 新建的输入弹窗只能点按钮提交, 键盘党无法回车提交。umResetPwd/dbsync增量快照单行输入加 Enter 提交; dmReview 文本域加 Ctrl+Enter 提交(占位符提示)。JS校验通过 | ✅ |
| 130 | MDM变更审批·驳回必填原因(UI功能) + 列表N+1(×3) | **UI功能瑕疵**: mdm-approval reviewDrawer 驳回原因仅"建议填写"(可空)→改"必填" + doSave 前端校验(空则 toast+focus) + **MdmCoreController.review 服务端兜底**(驳回空原因抛错)。**附带真实perf**: approval/list 逐行 selectById 填实体名=N+1(最多500查询)→改 distinct 收集 + selectBatchIds 批量。mdm-approval 经核查已有状态筛选/错误重试/防并发锁(均成熟)。JS校验+编译+581测试绿 | ✅ |
| 131 | 脚本上线审批·驳回必填原因(UI功能×4) | **UI功能缺失**: 三审批流中唯独脚本审批驳回**完全不收原因**(scriptReview 只发 {target})→申请人不知为何被拒。单条驳回弹原因框(必填+Ctrl+Enter)、批量驳回弹统一原因框(必填), 拆 doScriptReview/doScriptBatchReview/doScriptRejectSubmit/doScriptBatchReject; **ScriptApprovalService.review 服务端兜底**驳回空原因抛错。+1单测。**至此 datamodel/mdm/script 三审批流驳回原因全部必填且前后端双重校验**。JS校验+编译+582测试绿 | ✅ |
| 132 | 脚本审批工单·列表内搜索(UI功能) | **UI功能缺失**: R127 让工单可按状态浏览全部历史后, 列表可能很长却无搜索。加搜索框(脚本名/申请人/审批人), 每行打 data-search, oninput 原地 show/hide 行(不重渲染→不失焦)。JS校验通过 | ✅ |
| 133 | 质量·失败聚焦扫描上限透明化(UI功能) | **UI功能误导**: gov-quality 失败聚焦只扫前30条启用规则却 silent cap, 完成 toast 报"扫描完成"令用户以为全扫了。>30 时进度条+完成 toast 均追加"(共N条启用, 仅扫前30条)"。JS校验通过 | ✅ |
| 134 | 项目成员·用户名输入加下拉(UI功能, A线收尾) | **UI功能·该下拉却手输**(业主原话): 项目添加成员 projMemberUser 是纯文本输入用户名→挂 list="dnUserList" 全局用户数据列表, 内联自动补全真实用户名(原"从用户列表选"picker保留)。全站 placeholder"用户名"输入再扫: 仅 login 不挂(防泄露用户名, 正确)→**A线用户名/owner类下拉全站一致收尾**。后端 addMember 经核查已校验(空/角色合法/RBAC存在/去重, 错误清晰)。JS校验通过 | ✅ |
| 135 | 数据元·防重复编码 + 中文名前端必填(UI功能×4) | **真实数据完整性bug**: StandardController.saveElement 不查重复 element_code, 但该编码是落标稽核类型映射的键(loadElementTypes 后者覆盖前者→映射歧义/稽核错判)。加查重(insert/update[排除自身]均拦, 重复抛错)。**UI**: gov-standard 数据元表单原仅校验编码格式, 中文名缺失只靠后端报错→加前端中文名必填校验(即时 toast+focus)。+3单测(重复拒不insert/唯一insert/空名先于查重拦)。JS校验+编译+585测试绿 | ✅ |
| 136 | 词根/码表·防重复编码 + 必填前端校验(UI功能×6) | 承 R135 同类: saveRoot 查重 word_en(落标词根标识)、saveDict 查重 dict_code(码表引用键), insert/update[排除自身]均拦。**UI**: 词根表单补"中文必填"、码表表单补"名称必填"前端即时校验(原仅靠后端报错)。+4单测。**至此数据标准三实体(数据元/词根/码表)编码唯一性+必填校验前后端一致**。JS校验+编译+589测试绿 | ✅ |
| 137 | 指标·防重复编码 + 冲突就近标红(UI功能×4) | **真实完整性bug**: MetricController.save 不查 metric_code 唯一性, 但该编码是预警/取值/关联的引用键(重复→引用歧义); 启停{id,status}不带code则跳过。加查重(insert/update[排除自身]均拦)。**UI**: 保存失败若为"编码已存在", 就近把 mCode 输入框标红(复用 markErr, 与前端内联校验一致)。+3单测(重复fail不insert/唯一insert/启停跳过查重)。**至此 element/root/dict/metric 四类编码唯一性全做实**。JS校验+编译+592测试绿 | ✅ |

> **Ultracode·全代码库缺陷猎取 Workflow**(16 finder 并行扫全模块 → 每条对抗式 skeptic 核验): 53 发现 → **18 确认**(35 误报被核验枪毙)。以下 R138 批量修复全部 18 条确认缺陷。

| 138 | Ultracode缺陷批量修复×18(13前端UI+5后端) | **前端UI(13)**: datamodel.js 4处加载弹窗补 .catch+res判空(dmShowChanges/dmShowVersions/dmViewVersion/dmShowER 原静默失败/可NPE); workspace.html runTask 提交execute补.catch(原RUNNING永久卡死)、doOnline外层configApiPost补.catch(原按钮永久禁用锁死)、saveScript补.catch、质量删除/单跑fetch补.catch、质量历史 startedAt 原始ISO去T格式化、runTask isPartial死变量接上"仅运行选中代码"日志; gov-classification focusScanOnTable 选择器缺失错误兜底打开无关审计抽屉→改错误toast; dn-common dbTablePicker 库/表加载空catch吞错→报错toast; home-dashboard _refreshing 同步复位致防抖失效→延时复位。**后端(5)**: ProjectReleaseService 发布质量门禁只判failed漏error(执行异常本最该拦却放行)→补error入hard/soft(与全站口径一致); ConsumptionController 导出文件名 metric_code 未净化→正则净化防响应头注入; MdmGoldenController 更新黄金记录不校验存在→补 old==null 抛404(防空更新谎报成功); MetadataController clearSearchHistory 硬编码"default"永不匹配→改当前登录用户(原清空失效); ScriptService updateDatabaseName 无存在性校验→补404。+3单测。编译+**595测试绿** | ✅ |

> **Ultracode·第二轮缺陷猎取 Workflow**(12 finder, 安全/IDOR·并发/事务·资源泄漏·无界查询·注入 五维 + 后端鉴权层): 58 发现 → **8 确认**(50 误报枪毙, 验证更严)。核验层还纠正了多条 finder 提出的"修复方案会引入回归"。

| 139 | Ultracode第二轮·安全+健壮性修复×5(2安全+3健壮) | **安全(2)**: ①DataMapService.deleteComment **IDOR越权**——任意登录用户按自增ID枚举删他人表评论(catalog:edit是功能级非行级)→补行级归属(仅作者/admin可删)+3单测; ②ScheduleMonitorController run-log/log-detail/logs **GET越权读**——调度日志含SQL/库表/连接信息却仅要登录, 低权号可枚举runId读他人任务日志→加 SENSITIVE_GET_RULES gate(operations:schedule, 与本模块写权限一致)+单测。**健壮(3)**: ③MdmCoreController.stats 全表selectList内存filter→改4次selectCount下推SQL; ④MdmIngestService.importFromTable 新建黄金记录后XREF失败留孤儿→补偿删除(保部分导入语义, 非整方法事务[核验警告会破坏批处理契约]); ⑤DataMapService.getComments 无界→LIMIT 50。**采纳核验意见跳过3条low**(ScriptVersion并发去重[良性·需DDL唯一索引]、HealthScore/Standard无界查询[元数据量级·naive LIMIT会算错指标实为回归])。编译+**598测试绿** | ✅ |

> **Ultracode·第三轮 UX质量猎取 Workflow**(11 finder, 原始格式/空态/危险无确认/长列表无搜索/可供性/反馈/无障碍): 43 发现 → **13 确认**(30 枪毙)。

| 140 | Ultracode第三轮·UX批量修复×13(纯前端) | **危险操作补确认(4)**: ai-agent 文件删除/定时任务删除、数据地图评论删除 原单击即永久删→补 DN.confirm/msgConfirm danger; **原始格式(4)**: gov-assets 月成本表格列+热力格裸数字→fmtInt千分位、数据地图评论时间戳裸ISO→replace('T')格式化、home-dashboard 超期判断用 toISOString(UTC)→toLocaleDateString本地(UTC+8凌晨少标超期bug); **反馈/防连点**: intgRunSync/intgCreateTable 慢操作(DataX/建表)按钮无 busy 态可连点重复派发→加 id+disabled 守卫+完成恢复; **可供性/无障碍**: gov-subject 清除链接像说明文字→btn样式、project 卡片置顶图标补 title/aria-label、home-dashboard 待办行补 hover 反馈、dbsyncFormModal 补 Esc 关闭; **长列表无搜索**: 质量规则列表→加搜索框(原地过滤防失焦, 复用 saprApplySearch 范式)。纯前端 Java 未动→测试仍598。workspace 30块+5模块 JS 全校验通过 | ✅ |

> **Ultracode·第四轮 六桶猎取 Workflow**(16 finder × 6桶[bug/增强/UI/合并/删除/新增]): **81 候选 → 仅 9 确认**(72 枪毙, 89%误报率)。`合并=0 删除=0 增强=0`——对抗验证全部判定为不真实/有回归风险。**实证: 此成熟度下六桶各≥10 真实改点不存在。**

| 141 | Ultracode第四轮·六桶可得的真实改点×11 | **bug(3)**: QualityController runAll/syncIssues/trend 三处 selectList 未判空(全仓既定 null 兜底约定的反例)→补 `if(null)=ArrayList`; **UI(7)**: gov-consumption 三处 busy 态裸"…"→"计算中…"/"停用中…"(与同文件约定一致)、gov-assets 字段数/无用表候选/环形中心数 三处裸数字→fmtInt 千分位、**workspace msgBox 未转义 msg 反射型XSS**(文件名/依赖名/res.msg 可注入)→escapeHtml(confirmBox 因2调用方已自escape, 避双重转义不动); **新增(1)**: gov-health 五维明细表加 exportName→CSV导出按钮(汇报用)。编译+598测试绿。**合并/删除/增强桶 0 真实改点(诚实记录)** | ✅ |

> **策略转向(自主决定)**: bug/UI 桶递减 + 合并/删除/增强桶=0 → 不伪造 churn, 改**建真正有用的新功能**合法产出体量。

| 142 | 新功能·SQL片段库(数据开发)端到端 ×20+ | **真实新增功能**(devs 复用常用 SQL): 后端 DnSqlSnippet实体+Mapper+sql/86建表(按创建人隔离/同名唯一)+SqlSnippetService(list关键词过滤/save校验+查重+owner/delete行级归属/incrementUse原子自增)+Controller(list/save/delete/use 4端点)+PermInterceptor(/api/snippet→develop:edit); 前端 工具栏「片段」按钮+openSnippetPanel(搜索+列表)+saveCurrentSnippet(选中/全文存为片段)+doSaveSnippet+insertSnippet(光标处插入+热度计数)+deleteSnippet(确认)+snippetFilter(原地搜索)。+7服务单测+3权限断言。编译+**605测试绿**+JS校验。**运维须应用 sql/86_sql_snippet.sql**(无新权限点, 复用 develop:edit) | ✅ |

> **Ultracode·第五轮定向猎取**(治理计算正确性+血缘+安全/lineage/mdm前端+**自审新建SQL片段库**): 38候选 → **3确认**(35枪毙, 92%; **片段库自审0问题=新代码干净**)。

| 143 | 定向修复×3(bug+UI联动) | **bug**: MdmGoldenController.merge 被合并集为空(全是自身/不存在)时空跑→只产生无意义版本号自增+误导合并审计快照→补 `if(mergedRecs.isEmpty()) throw`; **UI功能(medium×2同根)**: 编辑脱敏策略/行级策略时 DN.dbTablePicker 只回填库、表/列下拉空白→保存被校验拦死/被迫重选→**扩展 DN.dbTablePicker 支持 defaultTable/defaultColumn**(异步级联回填 库→表→列, 拆 loadTables/loadColumns 助手, 6调用方零回归) + 两处 gov-security 表单传入已存表/列。编译+605测试绿+JS校验 | ✅ |

| 144 | 新功能·表结构对比(数据地图)端到端 | **真实新增功能**(ODS↔DWD/跨环境/迁移核对): 后端 SchemaDiffService(纯函数 compare: 按字段名小写对齐算 added/removed/changed[类型]/sameCount, 类型与名归一忽略大小写空格 → 高度可测)+ diff(空校验+取在线列)+ SchemaDiffController(GET /api/metadata/schema-diff, 登录可达与其他元数据读一致); 前端 表详情工具栏「结构对比」按钮 → 弹窗(当前表只读+对比表「库.表」输入+Enter提交)→ 渲染差异(新增/缺失/类型变更三色分区+变更表格)。+5纯函数单测。**无需建表/无新权限**。编译+**610测试绿**+JS校验 | ✅ |

| 154 | 数据血缘增强·数据地图字段modal也接图谱(业主令:图像查看) | 承 R152/R153: 数据地图字段血缘弹窗(dmFieldLineage)除文字穿透外, 顶部加**紧凑自包含 SVG 图谱**(dmRenderColGraph: BFS 算列位中心居中/下游右上游左, 节点 db.table.column, 上游红/下游绿箭头/中心★, 节点双行显示 列名+库.表)。复用 R153 的 /column-graph 端点。两入口(治理血缘抽屉+数据地图字段)均可图像查看。纯前端622测试不变。JS校验 | ✅ |
| 153 | 数据血缘增强·字段级血缘图谱可视化(业主令:图像查看) | 表级图谱本就有, 缺字段级图。**重构** buildSubgraph→键无关的 buildSubgraphFromKey(表="db.table"/列="db.table.column" 通吃), 加 columnGraph(以字段为中心双向BFS取子图)+ splitColKey + /column-graph 端点。前端 字段血缘抽屉**复用通用 renderGraph**(分层SVG有向图, 节点=db.table.column)渲染该字段多跳血缘图谱(上游红/下游绿箭头/中心★)。+3单测(splitColKey/列中心双向子图)。编译+**622测试绿**+JS校验。无迁移/无新权限 | ✅ |
| 152 | 数据血缘增强·数据地图字段列表联动血缘 | 承 R151: 数据地图表详情的字段列表每行加「血缘」按钮→弹窗展示该字段多跳上游来源+下游影响(复用 R151 的 column-trace/column-impact 端点)。dmFieldLineage/dmRenderFieldFlow, 按跳数分行+MASK/TRANSFORM 标记+源头/未覆盖提示。**数据地图↔字段血缘联动**(项目原则:功能联动)。纯前端619测试不变。JS校验 | ✅ |
| 151 | 数据血缘增强·字段级穿透(业主令:表级+字段级) | 先 Explore 全面摸清血缘子系统: 表级血缘(存储/Druid SQL解析+同步映射构建/impact/trace/graph/前端图)**已完整**; 字段级**边已存储(dn_lineage_edge.src/dst_column 由 SQL解析+同步映射写入)但缺穿透查询与可视**=真实缺口。**补字段级穿透**: LineageEdgeService.columnImpact/columnTrace(纯函数 computeColumnBfs 在 COLUMN 边集上多跳BFS, 大小写不敏感/深度限/去重防环) + 2端点(column-impact/column-trace, GET登录可达与表级一致) + 前端字段血缘表加「穿透」列→抽屉展示该字段多跳上游来源+下游影响(按跳数+变换类型MASK/TRANSFORM标记)。+6纯函数单测。编译+**619测试绿**+JS校验。无迁移/无新权限 | ✅ |
| 150 | UI·原始时间戳/数字格式收尾(×2) | 扫"原始ISO时间戳(带T)显示给用户"类: 全前端 grep 仅 gov-assets 采集历史子标题 startedAt 一处裸显→.replace('T')。partition createTime/lastModified 系 Doris 格式(已可读)不动。顺手 partition numRows 裸数字→formatNumber(1.2M/3.4K)。**原始时间戳类基本收尾**(前几轮已修多处)。纯前端613测试不变。JS校验 | ✅ |
| 149 | MDM·实体列表N+1批量化 + steward补2守卫 | grep 全后端找 selectById-in-loop 的 N+1。**MdmCoreController.listEntities**(高频列表端点)原逐实体 selectById 填域名=N+1 → distinct 收集 domainId + selectBatchIds 批量映射(并补 entities null 守卫, R148 漏的 var 形)。stewardOverview 补 entities/recs 两处 NPE 守卫(R148 漏的 var 形)。DataModelService 物理表落地的 selectOne-in-loop 系一次性建表的固有逐行操作非热点→不动(诚实判断)。编译+613测试绿 | ✅ |
| 148 | 全后端·selectList空指针守卫系统清零×14(8文件) | 承 R141/R147 同类: grep 全后端找出"selectList/selectBatchIds 结果未判空即内联遍历"的系统性缺口, 委派 agent 机械补 null 守卫(提取局部变量+`if(!=null)`+约定注释), 覆盖 DataModelService(4)/MetadataCrawlerService/ScriptApprovalService/MdmCoreController(2)/MdmGovernanceController(2)/MdmIngestService(2)/DataMapService/ProjectTaskService。行为非空时不变, 仅加守卫。编译+613测试绿。**至此 R141(3)+R147(9)+R148(14)=26 处 NPE 守卫, 未判空 selectList 缺陷类全库清零** | ✅ |
| 147 | 编排·TaskDependencyService 防NPE×9(复验上轮限额中断候选) | 上轮 finder 反复点名该文件(5次验证因限额中断)。直读 563 行核实: 9 处 selectList/selectBatchIds 结果未判空即遍历(allUpstreamsCompleted/InBatch 的 upRuns、collectDownstream 的 downs、getDownstreamTree 的 all、fillTaskNames 的两处 batchIds、pause/resumeDownstream 的 runs、searchOnlineTasks 的两处)——与本文件自身既有 null 守卫(scripts/syncTasks/deps)及全仓"selectList 理论可返回 null"约定不一致, 调度热路径(15s tick/补数/依赖搜索)可触 NPE。全部补 `if(null)` 兜底(emptyList/return/continue/跳过)。ScheduleLifecycleService 同步直读→干净无改点。编译+613测试绿 | ✅ |
| 146 | 第六轮hunt·前端确认修复×6(含XSS) | 先修1处UI-honesty(结果CSV导出静默截断500行→明确披露"超出上限未全导出")。第六轮hunt(46候选→**5确认**, 后端验证因会话限额中断未完成→那批候选未应用): **security(medium)**: formatAiReply 把 AI 回复直接 innerHTML 无转义→DOM-XSS(onerror等载荷可执行)→先整体 escapeHtml 再做 markdown(``` /\n/** 标记不受转义影响, 仍渲染); **perf(low)**: renderDag 循环内 innerHTML+= 致 O(n²)→拼字符串(defs+边+节点)一次赋值, z序不变; **UI(low×3)**: depRefreshCurrent/depAddManual/depDeleteManual 补 .catch + 缺失的 else(网络/业务失败原静默)。纯前端, 测试613。JS校验 | ✅ |
| 145 | 新功能·质量规则批量启停 | **真实缺口**(原状态只能逐条进编辑表单改, 无快捷启停): 后端 QualityController 加 `/rules/batch-status` 批量端点(ids+status, 入参校验+UpdateWrapper.in 批量更新+计数); 前端 规则列表加复选框列+全选(仅勾可见行)+批量启用/停用按钮(接续R132搜索)+ qualityBatchStatus/qualityToggleAll。+3单测(空ids拒/非法status拒/有效更新计数)+权限断言。**无迁移/无新权限**(复用 governance:quality)。编译+**613测试绿**+JS校验 | ✅ |
