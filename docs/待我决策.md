# 待业主决策 / 知会事项

> 绝不因此暂停推进；仅记录想跟业主说的话，供你醒来后处理。

## 🔴 头条：团队对"推翻重建"行使一票否决权（请醒后首先复核）
深度调研后，团队三位独立角色（代码核实）一致反对"字面删光重写"：
- **【Domain 业务专家】行使 charter 赋予的一票否决权**：主干（脚本开发+调度运维+数据地图）真实有用、生产验证；系统病根是**伪需求过多**（MDM 全家桶/项目协作 12 表/CDC）+ **治理闭环 4 断点**，属产品定义问题而非技术债；推翻重建只会把伪需求用新框架原样再造，最糟。
- **【Architect】**：工程上"推翻"≠"停机重写"，应绞杀式分轮迁移，每步可运行可部署可回滚。
- **【Product】**：校正了"MDM/治理是空壳"的误判（实为实装最重），正解是"保留验证过的闭环 + 砍重复/孤立/低值 + 重组边界 + 前端模块化"。

**PM-Lead 拍板（按 charter 岔路口机制，不停推进）**：采纳**绞杀式渐进重构**。它仍达成你"彻底重构为清晰、联动、无冗余目标架构"的目标（该推翻的伪需求照砍：MDM/项目协作/DataX/dev.html/dolphin），同时满足你全局红线（小步可运行/外科式/禁整文件重写）。**唯一不采纳的是"删光已验证主干从零重写"——你自身全局红线亦禁止此。**
👉 若你坚持要"字面删光重写"，醒来一句话，我立刻切换；否则我按 10 轮绞杀路线推进。

## 待你拍板的功能取舍（我先做无争议部分，这些暂缓）
1. **DataX 体系去留**：Domain 主张"保留分工"（DataX=开发侧一键采集，sync_job=运维侧生产管道，两个真实场景）；Product 主张"自研为唯一标准、DataX 整体废弃"。**我暂不动 DataX**，等你定。
2. **MDM 全家桶**：Domain 主张整体冻结/下线（无数据来源、无订阅者）；我暂只做"11→4 Controller 合并"（不删能力），是否冻结待你定。
3. **项目协作 12 表**（里程碑/任务/Wiki/公告/邀请/模板）：Domain 主张砍，保留 project/member/asset/release。暂保留待你定。
4. **DolphinScheduler** 是否彻底弃用、**前端是否上 Vue3**、**dn_mdm_hierarchy** 去留。

## 安全隐患
5. **deploy.env 之前未被 gitignore（已修）**：`.gitignore` 只忽略 `.env*`，漏了 `deploy.env`（含 root 密码）。核实其**从未被提交**（历史干净），本轮已把 `deploy.env` 加入 `.gitignore` 防误提交。
6. **deploy_jar.py 明文密码（本地，未入库）**：该文件第 3 行硬编码 root 密码，但**已 gitignore 且未被 git 跟踪**（更正：之前误判为"已跟踪"）——密码不在 git 历史里，仅是本地明文。建议仍：轮换密码 + 改为从 `deploy.env` 读取。R3 配置清场处理。
7. **生产硬编码**：`ARCHITECTURE.md`/代码内有 Doris 默认口令 `123456`、服务器 IP 等。R3 凭据外置时一并处理。

## 安全隐患（建议但未擅自改）
2. **明文密码入库**：`deploy_jar.py` 第 3 行硬编码了服务器 root 密码且该文件已被 git 跟踪（`deploy.env` 已 gitignore 但 `deploy_jar.py` 没有）。团队手册要求"凭据严禁写入任何文件或提交记录"。建议：①轮换该密码；②`deploy_jar.py` 改为从 `deploy.env` 读取；③把 `deploy_jar.py` 也加入 .gitignore 或清理历史。为保持外科式，暂未改动，等你确认。

## 基建约束（可能需要你关注）
3. **磁盘 90% 满**：/opt 仅剩 5G，backups 堆了 159 份。反复部署前我会按需清理旧 backups（保留最近若干份）。如不希望我动 backups，请告知。
4. **内存吃紧**：15G 用 13G。重构若引入更重的进程需评估；当前策略是本地编译、远程只跑单 jar。

## 2026-06-10 UI重构v3 待决策
1. workspace.html 本体无 ?v= 版本参数, 浏览器可能缓存旧 HTML——建议后端给 *.html 加 Cache-Control: no-cache 响应头(需改 Java 配置, 等你点头)
2. 暗色主题令牌已全量就绪(dn-design.css), 但无切换入口——要不要在系统管理或顶栏加主题开关?
3. 6套遗留弹窗视觉已统一, 但标记仍是6套(W3-full标记级迁移工程量大风险高)——按现状收口还是继续深改?

> 业主裁决(2026-06-10): 第2条【不做】主题切换入口——暗色令牌保留在 dn-design.css 作为技术储备, 不加UI开关。第1/3条仍待定。

> 业主裁决(2026-06-10第二次): 第1/3条【都做】——已交付: ①WebMvcConfig 给 *.html 加 Cache-Control: no-cache,must-revalidate(实测生效) ③三个裸遮罩弹窗(moveScript/aiScriptName/iwResult)迁 dn-modal 类+动画显隐, msgBox 叠加 dn-modal 重皮; 另 iw-form 全站清零(86处→dn-form体系)、emoji 全清(空态→DN.icon新增pin/flag/bot图标)、mdm diff/kv 组件化。
